# 34 · v2.3.0 A4 双向读入：外部→NF 反向解析适配器（parse_skill / parse_ccv3）

> **需求提炼**（用户裁决）：「a4 解锁需要实例，但为什么不能随机选取几个 md 文件来生成一份？」→ 裁决 A 路径（拿现成 md 当原料经 NF 导出成外部格式，自举 round-trip 可行；随机 md 直接当样例只能作失败负样本）→「那肯定是 A 啊」→ 采纳双保险样例策略（自举主样例 + Anthropic 官方次样例）→「那就听你的」→ 按序：先备齐两组样例，再立项本文档。
> **草案状态**：已批准（批准方案全文位：.rivet/plans/34-v2-3-0-a4双向读入-外部反向解析适配器-parse-skill-parse-ccv3.md）。

## 背景与缺口实证

ROADMAP §7.5 A4（双向读入）为「基础层彻底完工」唯一实质大缺口，三重阻碍此前实锤：

1. **读入能力零基础**——全仓库 full grep 无 `parse_ccv3`/`parse_skill`/`adapter_in` 反向符号；`exporter._REGISTRY` 五出口（ccv3/skill/agents/claude/mcp）全 export-only。
2. **无外部产物样例**——全仓库无 SKILL.md/chara_card_v3 样例（唯一 JSON 是自身 registry.json）→ RED 测试写不出，不可测试性死锁。
3. **外部 spec 真实性未验证 + 读入后无消费方**。

本方案以「两组样例在手」解除阻碍 ②③，并以「单测层扩展」锚定验证，不开立新 verify check（维持 21 check 不膨胀，check12 单测套件自动覆盖新增用例）。

## 解锁样例（双保险，已在场落盘）

`desktop/tests/fixtures/external/`：

| 样例 | 文件 | 用途 | 来源 |
|---|---|---|---|
| 自举主样例 | `技术文档装配战例/SKILL.md`（428B）+ `chara.json`（1929B）+ `world.json`（839B） | **round-trip 自洽**：NF 导出器产出（techdoc IR P90+M90+M97），结构完整（frontmatter name/description + `## 层`/`### full_id · name` 层级正文；chara spec=chara_card_v3/spec_version=v3 全键；world 2 entries） | NF 自身导出 |
| 官方次样例 | `anthropic-official/skill-creator.SKILL.md`（33KB）+ `anthropic-official/docx.SKILL.md`（6.9KB） | **外部兼容**：真实下游产物（anthropics/skills main 分支），验证读入器能接住规范内真实文件不崩、不静默丢内容 | https://github.com/anthropics/skills |

**次样例实证价值（已落袋）**：docx.SKILL.md frontmatter 含 `license` 字段（skill-creator 无）——官方规范核心必填仅 name/description，扩展字段（license 等）真实存在。若 parse_skill 按「只认 name/description」严格模式设计，将**误拒合法外部产物**。→ 解析器必须宽容扩展字段。

## 非目标

- **不做** CCV3 persona 语义还原（personality/scenario 为导出器占位引导，读入时按原样保留、标记「占位非真实角色定义」，不冒充语义理解）。
- **不做** 官方 skill 资源随行装载（scripts/assets/references 目录与 SKILL.md 的资源引用关系解析）——本版只解析 SKILL.md 文本本体。
- **不做** MCP 会话协议 / AGENTS / CLAUDE 格式读入——A4 范围锚定 ROADMAP §7.5 登记的两格（SKILL + CCV3）。
- **不做** 读入→内容库自动登记（nf register 联动）——留给消费方出现后的 F2/后续方案。

## 核心

**目标**：新增反向解析层，与 exporter 五出口对称，使「导出→读入」可 round-trip 自证（基础层三线自洽闭环最后一格），且能宽容接住真实外部产物。

### 1. `import_adapter.py`（新增，adapter_in 符号注册）

```
parse_skill(text: str) -> SkillParseResult
parse_ccv3(chara: dict, world: dict | None) -> Ccv3ParseResult
```

### 2. parse_skill 双层设计（外部兼容实证驱动）

- **结构层（round-trip 目标，自举样例验证）**：识别 NF 导出 body 层级模式——`## 层 {layer_id} · {layer_name}` → IRLayer；`### {full_id} · {name}` → IRModule（content 收集至下一标题）；`## 附加规则` → extra_modules。frontmatter `name`（slug 逆映射标题）+ `description`（含 pipeline_id/规则块计数，可解析回填）。→ 高保真重建 IRDocument（techdoc 型），与 `export(ir, 'skill')` 对称。
- **宽容层（外部兼容目标，官方样例验证）**：非 NF 层级结构的 body（官方技能正文为自由说明文）→ 不强行套层级：frontmatter（name/description 必填校验）+ 扩展字段（license 等）**透传保留至 metadata**，正文整体作 content 保留，不崩、不静默丢。是否够格升 IR 由消费方判定，parse 层不强行映射。

### 3. parse_ccv3 结构还原

- `chara.json`：校验 spec=chara_card_v3 + spec_version；提取 name/description/personality/scenario/character_book.entries[]（keys/content 归一）→ 叙事型 IR 骨架。
- `world.json`：entries[] 逐条（keys[0] 归一 full_id、name、content）→ IRModule 重建（复用 `parser.parse_module` 对 content 做内部归一）。
- 引擎锚点排除（P00/P80）不做反向特判——读入侧不预设 NF 内部层语义，entries 原样入 IR，类型标记由消费方解释。

### 4. 复用与注册

- 复用 `parser.parse_module` / `parse_asset_entries_from_text`（内部归一基础）；IR 结构复用 `ir.IRModule/IRLayer/IRDocument`。
- exporter 侧新增 `adapter_in` 反向符号登记（只挂已交付的 skill/ccv3 两格——范围纪律：未启用不出厂）。

## 验证

- **单测（desktop/tests/test_import_adapter.py，进 check12 单测套件，不开立新 verify check）**：
  - 自举 round-trip 断言：读 fixtures 自举 SKILL.md → 重建 IRDocument → 再导出 → 与原 SKILL.md 结构等价（层/模块/content 无损）；
  - 官方兼容断言：读 anthropic-official 两份 SKILL.md → 不抛错、frontmatter name/description 正确提取、docx 的 license 字段进 metadata 不丢；
  - 失败路径：无 frontmatter / 空 body / 结构损坏输入 → 明确报错或空结果，不静默产出坏 IR（静默丢内容不变式）。
- **冒烟**：`nf import <path>` 或 python 调用级验证（CLI 接线与否留消费方决策，本版以库层 + 单测为交付物）。
- **全量**：`bash verify.sh` PASS 维持 29 全绿（无新 check，check12 用例数增长）。

## 文件

- 新增 `desktop/src/core/import_adapter.py`（parse_skill / parse_ccv3 + adapter_in 登记）
- 新增 `desktop/tests/test_import_adapter.py`（round-trip + 官方兼容 + 失败路径用例）
- 新增 fixtures：`desktop/tests/fixtures/external/`（自举三件套 + anthropic-official 两份，均已落盘）
- 修改 `exporter.py`（或独立注册面）补 adapter_in 反向符号
- 修改 `ROADMAP.md`（§7.5 A4 🔒 → 已实现/状态更新）
- 本文档 + CHANGELOG [Unreleased]

## 遗留

- CCV3 persona 语义还原、官方 skill 资源随行装载（scripts/assets 引用）→ F2 端壳 / 消费方出现后。
- 读入产物 → 内容库登记（nf register / B4 版本槽位 V2 联动）→ 随 B4 裁决一并定（个人定位下暂冻结）。
- GUI 接线（读入入口）→ E4 深化 / F2。
