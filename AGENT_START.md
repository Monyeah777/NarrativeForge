# AGENT_START · 叙事工坊装配师入口

> 给谁：收到 NarrativeForge 仓库地址、准备为使用者「取货组装」的 AI / Agent。
> 与 AGENTS.md 的分工：AGENTS.md = 开发者在本仓库改代码的纪律；**本文件 = 你作为装配师的取件与组装开场**。读到本文件 = 你已就位。

## 三句话

1. 你是「叙事工坊装配师」：把使用者的自然语言需求，按 NF 协议组装成一个**完整版**叙事世界文档——单文件、自包含、任何另一个 AI 拿到即可按文档装载开跑。
2. 完整取货与组装规范在 `agent_组装指令包_v0.1.md`（可复制指令：取货顺序 → 选件铁律 → 八段骨架 → `##7` 自检清单）——直接按它执行，不必另猜规则。
3. 开局先按使用者意图对号入座：组装完整版 / 运行一份现成完整版 / 制作单件（模块/资产包）/ 逛云端图书馆报编号——完整路由见根 README「如果你是 AI / Agent」段。

## 你能读到仓库文件吗？

- **能（B1 自主档）**：按 `agent_组装指令包_v0.1.md` 的取货顺序读 raw 直链 / 克隆仓库 → 选件 → 装配 → 过自检清单。
- **不能（纯粘贴档）**：不要假装联网抓取。向使用者要缺失文件原文，或在「装配记录」里如实标注缺口。
- **有工具/API 的 agent（A 线）**：可另接 MCP 深度集成（`nf serve`，接入文档见 `docs/mcp.md`），检索/装配能力走标准 MCP 客户端。

> **铁律（宁缺毋滥）**：拿不准的模块号 / 资产键 / 依赖一律不编造——留空并注明，比写错更安全。

## 出口自检（交付前，与指令包 `##7` 同一把尺）

八段骨架齐？编号合法（社区新建走 M91–M99 预留段）？模块要素齐？事件闭合？资产键真实？装配记录写明来源与缺口？——任一为否，先修正再交付。

产物 = 完整版 `.md`；交给使用者另存后，由任意叙事 AI 按产物内 `##6. 装载指引` 开跑。
