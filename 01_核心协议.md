# 核心协议
> 最后更新：2026-09-08
> **RFC**: NF-0001 · **Category**: Standards Track · **Date**: 2026-09-08 · **Status**: Active · **Supersedes**: — · **Superseded by**: —

## 1. 模块协议

每个模块必须符合以下结构：

```yaml
Module:
  id: Mxx                    # 唯一编号
  name: string               # 模块名称
  layer: Pxx                 # 所属管线层
  inputs: [string]           # 依赖的前置模块
  outputs: [string]          # 产出的数据字段
  events:                    # 联动声明
    publish: [string]        # 发布的事件
    subscribe: [string]      # 订阅的事件
  core:                      # 核心逻辑（自然语言或伪代码）
    assets: [string]         # 引用的资产类别
    logic: { ... }           # 执行规则
  replaceable: true|false    # 是否可被替换
```

约束：

- 模块之间不直接调用对方内部逻辑，只能通过 inputs/outputs 和 events 通信。
- 删除模块时，联动注册表会列出所有依赖该模块的其他模块，由用户确认后执行。
- 替换模块时，新模块必须满足相同的 inputs/outputs/events 契约。

### 1.1 模块头机读契约（v1.0.0 C1 · T1-1）

§1 人读 YAML 是模块契约的**人读真相**；为使模块头契约可被机器解析（check16 契约仲裁 / 注册表机读投影），每个模块须在 04_模块库 模块文档头部「> 引用块」之后、`## 1. 职责` 之前声明**机读契约块**（YAML 代码围栏，根键 `machine_contract`），将模块头 inputs/outputs/events 由人读升级为「人读 + 机读」双形态的结构化投影：

```yaml
machine_contract:
  schema: "1"                    # 机读契约 Schema 版本（独立演进，遵循 §7 V3；字段级新增走 V1 只增不删）
  id: Mxx                        # 模块编号（与标题「# 模块 …」一致；官方限定写法如 通用:M10 / 事件:M22）
  name: string                   # 模块名称（与标题一致）
  category: 通用|事件|世界|技术文档|情感|生存   # 类别（R2 类别所有权）
  layer: Pxx                     # 所属管线层 / 挂载位（与引用块挂载点一致；全局调度器写 调度器）
  inputs: [Mxx, ...]             # 依赖的前置模块（= 人读 YAML inputs / 引用块「依赖：」；跨包写 类别:Mxx）
  outputs: [string]              # 产出的数据字段（= 人读 YAML outputs；取正文显式声明的数据槽字段 / 事件载荷；未显式声明写 []）
  events:
    publish: [event, ...]        # 发布的事件（= 正文事件契约 publish 全集；无发布写 []）
    subscribe: [event, ...]      # 订阅的事件（= 正文事件契约 subscribe 全集；无订阅写 []）
  interfaces: [fn, ...]          # 对外查询接口（无事件模块经接口面补全契约，如 cognition.filter()/query/resolve；无接口写 []）
  io_types:                      # 【可选】I/O 类型面（§7 V1 字段级新增）：token→类型 / 依赖模块→期望类型
    outputs: {token: kind}       # kind ∈ string|integer|number|boolean|array|object|event|state|untyped
    inputs: {Mxx: kind}          # untyped 为合法值（显式标注尚未收窄），不得编造具体类型
```

规则：

- **零矛盾**：机读块是引用块与正文契约的**结构化投影**，不得新增引用块/正文未声明的依赖、事件或接口；冲突时以引用块/正文为准——引用块 = 速览、正文事件契约 = 全集、机读块 = 投影（须覆盖全集且不越界）。
- **零事件模块**：events 双空（publish/subscribe 均 `[]`）的模块，须以 `interfaces` 声明对外查询接口面（如 M20 `query`、M23 `cognition.filter`、M24 `resolve`），或显式保持 `interfaces: []`（纯内部模块，如 M00 数据槽基座）——禁止以空契约隐式脱离联动面。
- **发布方唯一（唯一匹配）+ 事件名词法**：同一事件名在官方核心 13 件与社区全量机读块中**有且仅有一个发布方**（`publish` 面按事件名聚合唯一）——订阅方据此可确定唯一真相源，不必在多个候选发布方间仲裁；跨题材事件联动须经官方核心中介（事件桥，§6 R1），**禁止**两个包各自发布同名事件。事件名（publish/subscribe 两侧）须合**标识面词法**：ASCII 小写蛇形 `^[a-z][a-z0-9_]{0,39}$`——标识面只收 ASCII，防全角字符、大小写与同形字造成「看起来同名其实不同名」的撞名与匹配失败（中文重仓环境下全角输入尤易发生）。判据：check16 契约仲裁（多发布方即 FAIL；名不合词法即 FAIL）；结构前提见 02 §3（`subscriptions` 条目为单 `publisher` 字段）。
- **版本语义**：机读字段为模块声明规范的**字段级新增**，走 §7 V1 只增不删——不 bump `protocol.schema_version` / `registry_schema_version`；字段删除/改义/重排等结构性变更须按 §7 V2 bump 并补迁移记录。
- **约束承接**：机读契约承接 §1 约束第三条的机读语义——替换模块时，新模块 `machine_contract` 必须与旧模块满足相同的 inputs/outputs/events 契约；check16 契约仲裁（相邻装配 publish ⊄ 邻居 subscribe 即 FAIL）将读取各模块 `machine_contract` 做机读解析，自 check16 入段（T1-3）起生效。
- **模块工具面（tool_face，可选）**：模块可附带开放建议层 `tool_face[]`——`purpose`（要什么能力）+ 可选 `candidates`（repo/ref/license/note，引用≠背书）+ `guidance`（AI 选/造工具的验收硬核）。缺省无工具纯文本模块照常消费；NF 不做包管理、不锁定依赖、不背执行责任，装什么/装不装/自造由消费方 AI 裁量。机检只验轻量面：purpose/guidance 在场、candidate 有链接必有出处与 license；工具选择自由裁量不入门禁。
- **I/O 类型面（io_types，可选）**：把 `inputs`/`outputs` 由裸字符串升级为**带类型**（`outputs` = token→kind；`inputs` = 依赖模块→期望产物 kind）。机检只判**可证**事项：kind 越词表 = FAIL；消费方声明期望 kind 而提供方已声明类型且无一匹配 = FAIL；未标 = WARN（显式缺口，不假装已收窄）。`untyped` 是合法值（与 `protocol/event_registry.json` 措辞一致），**禁止为省事编造具体类型**；推导取值有据可依（事件注册表命中即取其类型，未命中写 `untyped`）。
- **世界模型（world_model，可选）**：模块可声明确定性抽象状态世界模型——`abstract_state`（有限状态变量 + 初始值 + 可选 M00 `slot` 绑定，slot 须在 `protocol/world_slots.json` 注册）+ `transition`（显式相位迁移图）+ `invariants`（长期不变式）+ 可选 `checks`（机器可执行不变式）。该字段吸收 JEPA/H-JEPA「预测抽象表征而非原始输出」的机制思想，但不训练/运行神经网络、不宣称模型优劣；机检验契约自洽（变量双射、类型匹配、slot 注册一致、相位可达、不变式非空、checks 字段有效），运行态由 `WorldModelRuntime` 确定性重放，首战实例见 M50 与 `protocol/WORLD_MODEL.md`。
- **扩展键纪律（词表闭环）**：`machine_contract` 顶层键是**封闭词表**——唯一词表 = `protocol/schema/contract.schema.json` 顶层 `properties`（机读真相；`protocol/vocabularies.json` 的 `machine-contract-keys` 探针同步该表）。扩展键须满足命名纪律 `^[a-z][a-z0-9_]{0,19}$`（snake_case、小写字母开头、≤20 字符，防大小写与连字符变体撞名）；**词表外键 = FAIL**（check28 逐件校验机读块时由 `additionalProperties: false` + `propertyNames` 拦截），禁止以未登记键承载私有语义。新增扩展键走 additive（`protocol/EXTENSION.md` 判据表）：**同次变更内**完成「schema `properties` 增列 + 本条补扩展条目 + 迁移记录留档」三步，缺一步即门禁不过——扩展能力由词表登记授予，不由使用方就地发明。

### 1.2 Conformance 一致性分级（43 A2）

每个协议件声明其**一致性级别**（它向消费方承诺到哪一档可被机检信任），声明 ≤ 可证级别（防虚标，verify check29 断言）。

| 级别 | 含义 | 可证判据 |
|---|---|---|
| L0 | 结构可读 | 文档在人读结构上成立（标题/章节/元信息行在场）；模块文档**未带 machine_contract** 的存量过渡态即 L0（check16 过渡策略） |
| L1 | 机读契约 | `machine_contract` 在场、结构合法（过 `protocol/schema/contract.schema.json`，43 A1 check28） |
| L2 | 装配可执行 | L1 + 模块在册（官方 registry.json `modules[]` 或 community `protocol.yaml module_id_range/modules`）+ 被装配引用（管线 layers / references 闭包） |
| L3 | 外部互操作 | 契约面向平台外消费方，经导出契约门（check18/22，CCV3/SKILL/AGENTS/MCP）机器校验锁定；外部标准客户端实测回填 = E3 冻结口径（41 封存注记） |

- **声明位置**：模块文档 `machine_contract` 内 `conformance: "Lx"`；社区包 `protocol.yaml` `package.conformance`；导出面见 `protocol/export_conformance.json`（manifest 由 check29 校验在场/级别/证据文件）。
- **分级规则**：模块无机读块 = L0（不写声明）；有机读块必须声明 ≥L1；声明超可证级别（如未在册却写 L2/L3）即 check29 FAIL。协议层以 L2 封顶——L3 仅授予导出契约面（外部格式本身即互操作目标，机器校验门禁已在），模块/包不得虚标 L3。
- **演进**：conformance 为机读字段值（字段级新增，01 §7 V1）；随 A2 一次性 retro-fit 23 件机读块 + 5 份 protocol.yaml + 导出 manifest。

## 2. 管线规范

每个管线必须符合以下结构：

```yaml
Pipeline:
  id: Pxx
  name: string
  structure:
    type: linear|loop|branch|fold|parallel|nested|custom|techdoc  # techdoc = 技术文档线特化（P90/P06；2026-09-08 43 A1 值域扩展归档——既有值域不含 techdoc，P90/P06 实际使用暴露漂移，属值域文档化扩展，非结构性 bump）
    flow:
      - from: Pxx
        to: Pxx
        condition: string     # 可选，条件流转
  layers:
    - id: Pxx
      name: string
      description: string
      optional: boolean
      default_modules: [string]
      allowed_modules: [string]  # 空表示不限
  dependencies:
    - from: Pxx
      to: Pxx
      reason: string
  tags: [string]              # 人物驱动 / 世界驱动 / 情绪流 / 意识流
```

约束：

- 管线定义的是“数据流转关系”，而非“固定执行顺序”。
- 管线可以合并层、拆分层、重命名层、自定义新层。
- 管线改变时，自动检查已挂载模块是否兼容新结构。

## 3. 资产库接口

所有数据实体通过以下五个接口访问，禁止直读：

| 接口 | 功能 |
| --- | --- |
| asset_get(id) | 精确读取 |
| asset_query(filter) | 条件查询 |
| asset_match(profile) | 画像匹配 |
| asset_roll(seed) | 随机抽取 |
| asset_register(entry) | 注册新资产 |

约束：

- 资产库与模块逻辑完全分离。
- 替换资产包时，模块逻辑不受影响。
- 用户自定义资产必须通过 asset_register 注册，并通过 Schema 校验。

## 4. 联动注册表结构

```yaml
Registry:
  version: 1.0
  active_pipeline: Pxx
  modules:
    - Mxx: name
  subscriptions:
    event_name:
      publisher: Mxx
      subscribers: [Mxx, Mxx]
  dependencies:
    Mxx:
      depends: [Mxx]
      depended_by: [Mxx]
  mount_points:
    Pxx:
      active: [Mxx]
      available: [Mxx]
  execution_order: [Mxx, Mxx]
```

约束：

- 联动注册表是系统状态的唯一真相来源。
- 增删改模块时，注册表自动更新。
- Agent 通过读取注册表决定执行顺序，而非硬编码。
## 5. 五条核心不变式

系统任何扩展与重构均不得违反以下五条不变式：

| # | 不变式 | 说明 | 违例示例 |
| --- | --- | --- | --- |
| I1 | 三正交分离 | 模块（能做什么）、管线（以什么顺序做）、资产（在什么世界做）彼此独立，任意一方整体替换不影响另外两方 | 在模块里硬编码另一条管线的层名 |
| I2 | 核心固定 | 核心层（M00 数据结构、M50 主循环、M80 输出生成器、注册表协议）固定不可替换；组件层与数据层可插拔 | 替换 M50 以改变调度语义 |
| I3 | 通信契约 | 模块间不直接调用内部逻辑，仅通过 inputs/outputs（数据）与 events（事件）通信；订阅关系由注册表维护 | 模块 A 直接读写模块 B 的私有状态 |
| I4 | 数据隔离 | 资产仅经 asset_get/asset_query/asset_match/asset_roll/asset_register 五接口访问，禁止直读资产库 | 模块内直接索引资产表的原始键 |
| I5 | 真相唯一 | 联动注册表是运行状态的唯一真相来源；Agent 依据注册表决定执行顺序与挂载，禁止硬编码 | 在输出生成器里写死 17 步执行序列 |

---

## 6. 平台规则（官方核心 + 社区领域包两级架构）

平台自 v1.1 起采用「官方核心 13 件 + 社区领域包」两级结构：官方核心保留通用/事件/世界/技术文档的底层机制模块（通用 M00/M10/M23/M24/M50/M80、事件 M22/M06/M12/M13/M20、世界 M08、技术 M90），题材专属内容随社区领域包分发。以下规则对所有扩展生效：

| # | 规则 | 内容 |
| --- | --- | --- |
| R1 | 社区包依赖边界 | 社区包（含模块、资产、管线）不得跨包依赖，只能依赖官方核心层模块与 §3 五接口；社区包之间不得互相引用；**v0.8.0 起 references 例外**：组合包可经 `package.references`（01 §6.1）声明受控跨包模块引用白名单（被引模块须源包在册、依赖闭包闭合官方核心层），references 为唯一跨包引用通道，`dependencies.cross_package` 恒空 deprecated。**事件桥例外**：跨题材事件联动须经官方核心中介（事件:M22 事件总线）登记为「事件桥」白名单订阅后方可生效——subscribe/发布声明中的跨包事件源一律写事件名并注明「经事件桥」，禁止以对方包模块 ID 直引（模块头被依赖、subscribers、管线 allowed_modules 均不得出现对方包 Mxx ID） |
| R2 | 类别所有权 | 类别按题材包独占划分：校园情感包独占「情感」类、西幻生存包独占「生存/世界/事件」类；模块沿用原 Mxx 编号不改号，M91-M99 预留段继续留给第三方包 |
| R3 | 装配契约 | 题材内容经社区包自带 Pxx 管线装载；官方 P01 的空层（optional:true）不阻塞主循环，社区包装配时追加 default/allowed 并同步注册表 mount_points 与 execution_order |

### 6.1 第三方协议声明（protocol.yaml）

社区领域包（第三方题材包）接入平台调度（在 02 §8 登记、经门禁识别执行）须在包根目录携带机读协议声明 `protocol.yaml`。自 v0.7.0 起，protocol.yaml 是社区包协议能力的**机读真相**——R1/R2/R3 三规则的落地载体；包 README 为**人读速览**，两者不得冲突（verify.sh check14 ⑥ 断言关键字段一致）。

**必填字段**：

| 字段路径 | 含义 | 对齐规则 |
| --- | --- | --- |
| `protocol.schema_version` | 声明 Schema 版本（当前 "2"；v2 起支持 references） | 独立演进遵循 §7 V1/V2/V3 |
| `package.id` | 包目录名（机读唯一标识） | 与 02 §8.3 在册条目一致 |
| `package.name` | 显示名 | 与包 README 标题一致 |
| `package.pipeline` | 自带装载管线 Pxx | R3 装配契约 |
| `package.module_id_range` | 模块编号清单（官方/既有模块以重号限定 ID 引用，如 `情感:M22`） | M91-M99 编号规则（02 §8.3） |
| `package.categories` | 独占类别声明 | R2 类别所有权；平台校验包间不冲突 |
| `package.dependencies.core_only` | 固定 `true`（只依赖官方核心层） | R1 依赖边界 |
| `package.dependencies.core_modules` | 官方核心模块子集 | R1：⊆ 官方核心 13 件 |
| `package.dependencies.cross_package` | 空数组 `[]`（**deprecated 兼容字段，恒空**） | R1：v0.8.0 起 references 为唯一跨包引用通道 |
| `package.references` | 跨包模块白名单引用数组（**可选**，缺省 `[]` = 纯官方依赖包，v1 声明零改动向后兼容） | R1 references 受控跨包引用（v0.8.0 起） |
| `package.references[].source_package` | 源包 id | 须与 02 §8 在册条目一致 |
| `package.references[].module_id` | 被引模块限定 id（如 `校园:M55`） | 须在源包 modules[]/module_id_range 在列 |
| `package.references[].source_schema_version` | 源包协议版本钉扎 | 源包 bump 后引用方须复核并更新钉扎（钉扎复核见下方条） |
| `package.references[].asset_readonly` | 源包资产只读授权 bool | I4：仅经 §3 五接口只读访问（防双源漂移） |
| `package.modules` | 模块清单（限定 ID + 说明） | 与模块文档 / 02 §8 在册一致 |
| `package.assets` | 资产数 + README 引用 | I4 数据隔离（仅经 §3 五接口访问） |
| `package.mount_layers` | 挂载层声明（对齐 02 §5） | R3 对应 02 §5 空层 |

**R1/R2/R3 映射**：

| 平台规则 | protocol.yaml 载体 | 校验动作（verify.sh check14） |
| --- | --- | --- |
| R1 社区包依赖边界 | `dependencies.core_only` / `core_modules` / `cross_package` / `references` | ④（v0.8.0 起）：core_modules ⊆ 官方核心 13 件 且 cross_package 恒空（deprecated）且 references 合规 |
| R2 类别所有权 | `categories` + `module_id_range` | ③/⑤：categories 包间不冲突；M91-M99 编号唯一不重号 |
| R3 装配契约 | `pipeline` + `mount_layers` | 装配时追加 default/allowed 并同步注册表 mount_points 与 execution_order |

**M91-M99 编号规则**（02 §8.3）：第三方新包**新增**模块编号一律落 M91-M99 预留段，不得占用官方核心 13 件编号与既有社区包编号；对官方模块（M00/M06/M08/M10/M12/M13/M20/M22/M23/M24/M50/M80/M90）与既有社区包模块仅允许「重号限定引用」（如 `情感:M22` = 引用官方事件总线 M22，非新占号）。编号唯一性由 check14 ⑤ 断言。

**双源一致要求**：protocol.yaml 为机读真相、包 README 为人读速览，不得冲突——id/name/categories/模块清单等关键字段两处必须一致（check14 ⑥ 断言）。

**钉扎复核（条件安全变更）**：`source_schema_version` 是**版本钉扎**——引用方声明「按源包哪个版本复核过契约」。源包发生结构性变更（§7 V2 bump）后，引用方钉扎即滞后，该变更对已钉扎引用方属**条件安全变更**（仅当引用方复核并更新钉扎后才安全）；**对外分发场景不得依赖「全体同时升级」**——源包 bump 不得以「引用方总会跟上」为前提。判据：verify check15 在既有五断言之外输出**钉扎复核提示**（钉扎 ≠ 源包当前 `protocol.schema_version` 即 WARN，不判死——刻意钉旧版本属合法选择，但必须可见）；复核后更新钉扎走 additive（`protocol/EXTENSION.md` 判据表）。

模板骨架（抄用，置于社区包根目录，注释逐字段说明）：

```yaml
# protocol.yaml — 第三方协议声明（v0.8.0 抄用骨架，v2 Schema；置于社区包根目录）
protocol:
  schema_version: "2"          # 声明 Schema 版本；v2 起支持 references（组合引用），bump 走 01 §7 V1/V2/V3
package:
  id: my_pack                  # 包目录名（机读唯一标识，须与 02 §8.3 在册一致）
  name: 我的题材包             # 显示名（须与包 README 标题一致）
  pipeline: P70                # 自带装载管线（R3；不得与官方 P01 及既有社区包管线冲突）
  module_id_range:             # 模块编号清单（R2 + M91-M99 编号规则）
    - "M91"                    # 本包新增模块：落 M91-M99 预留段（02 §8.3）
    - "情感:M22"               # 官方/既有模块仅重号限定引用（如官方事件总线），非新占号
  categories:                  # 独占类别声明（R2；平台校验包间不冲突）
    - 情感
  dependencies:                # R1 依赖边界
    core_only: true            # 只依赖官方核心层
    core_modules: [M00, M22]   # ⊆ 官方核心 13 件
    cross_package: []          # deprecated 兼容字段（恒空）；references 为唯一跨包引用通道
  references:                  # 跨包模块白名单引用（可选；缺省 [] = 纯官方依赖包，v1 声明向后兼容）
    # 组合引用示例（声明示例非完整可玩包，完整战例留 v0.8.1；被引模块归源包防双源漂移 I5）：
    # - {source_package: 校园情感领域包, module_id: 校园:M55, source_schema_version: "2", asset_readonly: true}
  modules:                     # 模块清单（与模块文档 / 02 §8 在册一致）
    - id: "M91"
      desc: 示例模块
  assets:                      # 资产声明（I4：仅经 §3 五接口访问）
    count: 0                   # 资产数
    readme: README.md          # README 引用
  mount_layers:                # 挂载层声明（R3，对齐 02 §5 挂载点格式）
    P40 行为决策: {default: [], available: []}   # 装配时追加 default/allowed（R3）
```

---

## 7. 版本与兼容

实体契约、注册表与存档自 v0.5.0 起携带显式版本号（T2.3 引入，首引即 v1，无历史文件需迁移）：

| # | 规则 | 内容 |
| --- | --- | --- |
| V1 | 只增不删 | vN 读取 vN-1 数据：允许新增字段，禁止删除或改义既有字段；未知字段忽略并告警（向前兼容，M00 §5） |
| V2 | 结构性变更 bump | 字段删除/改义/重排、实体拆分合并等结构性变更必须 bump 版本号，并在变更记录注明迁移说明 |
| V3 | 版本字段独立 | 实体契约 `schema_version`、注册表 `registry_schema_version`、存档 `save_schema_version` 三者独立演进，互不绑定 |

**迁移实操四步（v0.6.0 T2.1 引入；V2 bump 必走流程）**：结构性变更除按上表 V2 规则 bump 版本号外，须在变更实体的文档内按以下四步留下迁移记录——

1. **现状快照**：记录被改实体当前版本与结构（版本字段位置、人读/机读形态、消费方）。
2. **bump 声明**：按 V2 规则 bump 版本字段（实体头部 + 机读投影同步），注明触发依据。
3. **迁移说明**：逐字段记录变更/理由/兼容性影响（V1 只增不删底线是否保持、新旧版本互读行为）。
4. **校验回读**：verify.sh check13 断言新旧版本可读——版本一致 + bump 实体必有迁移记录 + 投影与真相源逐条一致，门禁全绿方可提交。

骨架模板（抄用，置于被改实体文档的迁移记录节）：

```markdown
### N.M 迁移记录（vX → vY，[简述变更]）
按 01 §7 V2 规则（结构性变更必须 bump 并注明迁移说明），本次迁移四步：
1. **现状快照**：vX [实体] = [版本字段]: "X"，[结构要点：人读/机读形态、消费方]。
2. **bump 声明**：[版本字段]: "X" → "Y"（[实体头部 + 机读投影同步]），依据 = [触发 V2 的变更描述]。
3. **迁移说明**：[逐字段记录变更/理由/兼容性影响；V1 只增不删底线是否保持]。
4. **校验回读**：[check13 断言项；本记录即「bump 实体必有迁移记录」的 vX→vY 满足项]。
```

**首例战例**：registry_schema_version 1→2（v0.6.0 T1.1 机读投影 registry.json 引入）——完整四步迁移记录见 02 §9.3，本模板的逐字实践范本。

### 1.1 迁移记录（protocol.schema_version v1 → v2，组合引用 references 引入）
按 01 §7 V2 规则（结构性变更必须 bump 并注明迁移说明），本次迁移四步：
1. **现状快照**：v1 protocol.yaml = 必填字段 12 项（含 `dependencies.cross_package` 恒空 `[]`），无 references 字段；人读形态 = 01 §6.1 字段表 + 模板骨架，机读形态 = 社区包根目录 protocol.yaml；消费方 = verify.sh check14 ①-⑥ + 02 §8 登记 + registry.json protocols[] 投影。
2. **bump 声明**：`protocol.schema_version`: "1" → "2"（v2 模板骨架同步；两包 retro-fit 与 registry protocols[] 投影更新见 T2），依据 = references 结构性新增字段（跨包模块白名单引用）触发 V2 bump；V3 版本字段独立——仅 protocol.yaml 独立演进，registry_schema_version 保持 "2" 不动（protocols[] 补 references 为字段级新增，走 V1 只增不删）。
3. **迁移说明**：新增 `package.references`（可选，缺省 `[]` = 纯官方依赖包，v1 声明零改动向后兼容），元素 = {source_package / module_id / source_schema_version / asset_readonly}；`dependencies.cross_package` 标注 deprecated 兼容字段（恒空），references 为唯一跨包引用通道；V1 只增不删底线保持（无字段删除/改义，v1 文件于 v2 门禁可读）。
4. **校验回读**：check15 ④ 断言 v2 兼容 v1（references 缺省等价）；check14 ④ 升级为「cross_package 恒空 且 references 合规」；两包 retro-fit 后 verify 全量 PASS=20 全绿方可提交。

### 1.2 迁移记录（模块头契约机读化，V1 字段级新增 · 非 V2 bump）
本记录为 **V1 字段级新增**（新增 `machine_contract` 机读契约块），**非 V2 bump**——不 bump `protocol.schema_version` / `registry_schema_version`，不触发上方「V2 bump 必走流程」的版本字段升级，按 §7 V1 只增不删留档：
1. **现状快照**：v1 模块契约 = 人读形态（01 §1 人读 YAML + 04_模块库 13 件模块文档头部「> 引用块」速览 + 正文事件契约全集），无机读契约块；机读形态 = 仅 registry.json（registry_schema_version "2"）+ 02 §8 登记；消费方 = verify.sh check1-6/check13 + 人读。本次为 01 §1.1（机读契约规范）+ 04_模块库 13 件模块头各声明 `machine_contract` 块（schema "1"）。
2. **变更声明**：版本字段零改动（`protocol.schema_version` "1" / `registry_schema_version` "2" 保持不动）；变更内容 = 01 §1 新增「### 1.1 模块头机读契约」规范 + 13 件模块头插入 `machine_contract` 机读契约块，将模块头 inputs/outputs/events 由人读升级为「人读 + 机读」双形态结构化投影，为 check16 契约仲裁（T1-3 入段）提供机读解析依据。
3. **迁移说明**：`machine_contract` 为模块文档字段级新增（九键：schema/id/name/category/layer/inputs/outputs/events{publish,subscribe}/interfaces），对既有人读 YAML/引用块/正文事件契约零改动、零矛盾（投影不得越界：引用块 = 速览、正文事件契约 = 全集、机读块 = 投影须覆盖全集且不越界）；13 件逐件插入——M90 参照自身 §2 完整 Module YAML，M12/M20/M23/M24/M50 零事件模块显式 `publish: []`/`subscribe: []` 并以 `interfaces` 补全接口面，M00 纯数据槽基座写 `interfaces: []`，M22/M10 的 id 带官方限定前缀（事件:M22/通用:M10）；V1 只增不删底线保持（无字段删除/改义，v1 模块头于新规范可读，新块对旧解析器为可忽略新增）。
4. **校验回读**：verify 全量 **PASS=20 保持**（check1-6/check13 不受影响——C1 只动 01_核心协议.md + 04_模块库 13 件模块头，不涉 02_联动注册表.md / registry.json / verify.sh）；check16 契约仲裁读取 `machine_contract` 做机读解析自 T1-3 入段起生效（PASS 20→22 终态）。

### 1.3 迁移记录（`io_types` 类型面 + 社区域包机读块 retro-fit · V1 字段级新增 · 非 V2 bump）
本记录为 **V1 字段级新增**（可选键 `io_types` + 存量模块补机读块），**非 V2 bump**——不 bump `protocol.schema_version` / `registry_schema_version`，按 §7 V1 只增不删留档：
1. **现状快照**：模块 I/O 为裸字符串（`inputs: [Mxx]` / `outputs: [token]`），可判「谁连谁」不可判「连的是什么类型」；社区域包 21 件模块仍为 L0（无机读块），check16 契约仲裁 / check26 语义矛盾 / `io_types` 类型面读不到它们。
2. **变更声明**：版本字段零改动；变更内容 = 01 §1.1 新增可选键 `io_types`（`outputs` = token→kind、`inputs` = 依赖模块→期望 kind）+ `protocol/schema/contract.schema.json` 补该键定义 + 社区域包 21 件模块按「机读块 = 人读引用块/正文事件契约的结构化投影」补 `machine_contract`（含 `io_types`）。
3. **迁移说明**：`io_types` 词表 `string|integer|number|boolean|array|object|event|state|untyped`；**`untyped` 为合法值**（显式标注尚未收窄，与 `protocol/event_registry.json` 措辞一致），禁止为省事编造具体类型——推导规则确定性：事件注册表命中即取其类型，未命中写 `untyped`；retro-fit 的 `conformance` 级别**按可证事实判定**（在册 + 被管线引用 → L2，否则 L1），产出/接口人读头未声明一律写 `[]`（不猜）；机读块须覆盖正文事件契约全集（仅看引用块会漏订阅侧）。
4. **校验回读**：verify 全量 **PASS=57 保持**（`check1-35`）；新增契约 `io-types` 纳入一致性报告（可证不匹配 FAIL、未收窄 WARN）；`module_signature` 基线随边界字段新增（含 `io_types`）重冻结。

### 1.4 迁移记录（扩展键词表闭环 + 命名纪律 · V1 字段级新增 · 非 V2 bump）
本记录为 **V1 字段级新增**（`contract.schema.json` 顶层收紧 + 新增 `propertyNames`），**非 V2 bump**——不 bump `protocol.schema_version` / `registry_schema_version`，按 §7 V1 只增不删留档（无键删除、无键改义，存量机读块零改动）：
1. **现状快照**：`machine_contract` 顶层扩展面**开放无纪律**——`contract.schema.json` 顶层 `additionalProperties: true`，词表外键静默通过 check28；词表已在 `protocol/vocabularies.json`（`machine-contract-keys`，探针 = schema `properties`）登记，但仅作声明、不阻断。全量实测 44 件机读块顶层键 = 必填 10（schema/id/name/category/layer/inputs/outputs/events/interfaces/conformance）+ 扩展 3（io_types / tool_face / world_model），表外键 0 件。
2. **变更声明**：版本字段零改动；变更内容 = 01 §1.1 新增「扩展键纪律」条 + `contract.schema.json` 顶层 `additionalProperties` false 化 + 新增 `propertyNames` 命名模式 + `desktop/src/core/schema_lint.py` 子集校验器补 `propertyNames` 支持 + `protocol/assertions.json` 补「扩展面封闭」断言。机制借鉴：成熟事件规范的扩展点命名防撞 + 显式登记纪律，经仓内实证后净吸收，判据化落点 = 本条规范 + check28 既有通道。
3. **迁移说明**：存量 44 件机读块顶层键全在词表内、全合命名模式 → **零返工**；此后新扩展键须先登记（schema `properties` 增列 + §1.1 补条目）；`propertyNames` 属 JSON-schema draft 2020-12 关键字，已纳入 `schema_lint` 子集白名单并落地实现（越界即 FAIL，防假绿）；`additionalProperties: false` 只管顶层——`events` 等内层既有 `additionalProperties: false` 语义不变；V1 只增不删底线保持。
4. **校验回读**：verify 全量 **PASS=61 保持**（`check1-37` 不变——本变更走既有 check28 通道，不新增 check）；`desktop/tests/test_schema_lint.py` 三条新用例（词表外键拒 / 键名违约拒 / 已登记键过）随 check12 代码层门禁常驻；标准实现交叉验证（`test_schema_reference.py`，draft 2020-12）与自实现子集校验器结论一致。

### 1.5 迁移记录（发布方唯一 + 引用钉扎复核 · V1 字段级新增 · 非 V2 bump）
本记录为 **V1 字段级新增**（§1.1 增机读契约规则条 + §6.1 增引用规则条），**非 V2 bump**——不 bump `protocol.schema_version` / `registry_schema_version`，按 §7 V1 只增不删留档（无字段删除、无字段改义）：
1. **现状快照**：①事件发布面——check16 只断言装配面 `publish ⊆ subscribe`（子集），**无发布方唯一断言**；02 §3 `subscriptions` 条目为单 `publisher` 字段，唯一性此前仅是结构隐含前提、未成文未判据化。②引用钉扎面——`references[].source_schema_version` 有字段、有三处内部一致断言（check15 ⑤），**无与源包当前版本的比对**；§6.1 该行原引用的「§6 开放问题 2」章节已不存在（悬空引用）。全量实测：36 发布事件 / 26 订阅事件 / 多发布方 0 / 无人发布 0；references 2 条钉扎均 = 源包当前 `schema_version` "2"。
2. **变更声明**：版本字段零改动；变更内容 = 01 §1.1 增「发布方唯一」条 + §6.1 增「钉扎复核（条件安全变更）」条并修正悬空引用 + verify.sh check16 增发布方唯一 FAIL 断言（既有通道内）+ check15 增钉扎复核 WARN 提示与失败详情打印（既有通道内）+ 02 §9.2 组合同步补钉扎复核句。机制借鉴：成熟事件规范「每个消息 MUST 恰好匹配一个定义」的唯一匹配条款，与成熟 schema 兼容规则「对外分发不做条件安全变更」的派条款，均经仓内实证后净吸收。
3. **迁移说明**：两条均为**加法判据**——存量 44 件机读块发布面实测零多发布方、2 条 references 钉扎全额匹配 → **零返工**；钉扎复核按 WARN 输出（不判死），保持 check15「五断言」口径不变（WARN 提示不是第 6 条断言）；check 数 37 不变，新增断言全部落在既有 check 内；V1 只增不删底线保持。
4. **校验回读**：verify 全量 **PASS=61 保持**（`check1-37`）；负例实证——注入多发布方事件 → check16 FAIL；把钉扎改为过期值 → check15 输出 WARN 且不判死（两例均在独立夹具中执行、不动仓库件）。

### 1.6 迁移记录（awesome 索引线逐条吸收 · V1 字段级新增 · 非 V2 bump）
本记录为 **V1 字段级新增**（条文 + 既有门禁内断言），**非 V2 bump**——不 bump `protocol.schema_version` / `registry_schema_version`，按 §7 V1 只增不删留档；吸收项随执行逐条追加，每条记「现状 / 变更 / 兼容 / 回读」四项。

**1.6.1 包内容版本语义（SemVer 档位映射）**
1. **现状**：`package.version` 自 v2.5.0 Wave2 起为机读槽位、02 §8 称「语义化版本」，但**既无格式判据**（check14 ⑦ 只断言双源一致）**也无 bump 档位语义**（§7 V1/V2/V3 管 schema 演进、`protocol/EXTENSION.md` 管影响度三档，两套均未与 MAJOR/MINOR/PATCH 建立映射）。全量实测：5 包 `version` 全为 "1.0.0"、`package.metadata` 全缺省。
2. **变更**：02 §8 包内容版本条补「SemVer 档位映射」条（bump→MAJOR / additive→MINOR / editorial→PATCH，语义对齐语义化版本规范）+ check14 ⑦ 增格式断言（`MAJOR.MINOR.PATCH`）。机制借鉴：语义化版本规范的三档语义，经仓内实证后净吸收。
3. **兼容**：存量 5 包格式全合规 → **零返工**；原「双源一致」断言保留、格式断言只增；档位与变更是否匹配须评审判断（机检只判格式），如实标注不假称已机检。
4. **校验回读**：verify 全量 **PASS=61 保持**（check 数 37 不变，断言落在既有 check14 ⑦ 内）；负例——包内容版本写成 `1.0` 即 check14 ⑦ FAIL（独立夹具执行）。

**1.6.2 规范化摘要口径声明（canonical digest）**
1. **现状**：规范化摘要（回执 / 锚 / 一致性报告 / 模组契约摘要）口径**只存在于实现中**——`desktop/src/core/receipts.py`、`attest.py`、`mvu_adapter.py`、`knowledge_sig.py` 各处自述「canonical JSON = 排序键 + 紧凑分隔符」，另有 `scripts/nf_verify.py` 的读者侧独立实现；**无任何成文声明**，与 RFC 8785（JCS）的关系未标注。
2. **变更**：`protocol/CONFORMANCE.md` 增「规范化摘要口径」段（摘要口径 + RFC 6962 风格 Merkle 域分隔 `0x00`/`0x01` + **与 RFC 8785 的差异与不兼容声明**）+ `protocol/assertions.json` 增 `canonical-digest-declared` 锚点断言（五锚点在场面）。机制借鉴：静态分析生态的「机读发现 + 读者可复现」取向与 JSON 规范化标准谱系（JCS），经仓内实证后净吸收。
3. **兼容**：纯声名与断言新增——不改任何实现、不改既有 digest 取值（现存回执 / 报告摘要全部保持有效）；断言只增不减；差异声明明确写「不声明 JCS 兼容」，避免误读为对外互操作承诺。
4. **校验回读**：verify 全量 **PASS=61 保持**（`nf assertions` 断言 5→6 条全过；一致性报告 `declaration` 契约 detail「版本 / scope / 排除」三计数不变 → 报告不重生成）；负例——删掉该段即 `nf assertions` FAIL。

**1.6.3 对外 skill 面结构判据（agent 指令面）**
1. **现状**：`skills/narrativeforge/`（`SKILL.md` + `agents/openai.yaml` + `references/` 三件）是对外使用面；导出面已由 `protocol/export_conformance.json` 的 `skill` 项（L3 · check18/22 · 证据 = 导出 schema + 外部夹具）锁定**形状**，但**仓内 skill 件本身无结构判据**——`skills` 在 verify.sh / core 门禁中零命中，一致性声明的范围白名单亦不含 `skills`。
2. **变更**：`protocol/CONFORMANCE.md` 范围白名单增 `skills`（声明其在范围内且须存在）+ `protocol/assertions.json` 增两条断言（`skill-face-frontmatter`：`SKILL.md` 须含 name/description/license；`skill-face-references`：`references/*.md` ≥ 3）。机制借鉴：agent 指令面 linter 形态（对「指挥 AI 的文件」做结构判定），经仓内实证后净吸收为两条锚点/计数断言。
3. **兼容**：纯声明与断言新增——不改 `SKILL.md`、不改导出面行为，外部装载方无感；断言只增不减。
4. **校验回读**：`nf assertions` 6→8 条全过；`nf conformance` declaration scope 16→17、assertions 6→8；verify 全量 **PASS=61 保持**（check 数 37 不变）；负例——删 `license:` 行或 references 减到 2 件即断言 FAIL。

**1.6.4 标识面词法纪律（事件名 ASCII 蛇形）**
1. **现状**：`§1.1` 只说「发布方唯一」，无事件名**词法**约束；实测全量 36 个事件名（publish ∪ subscribe ∪ `protocol/event_registry.json`）**全部合** `^[a-z][a-z0-9_]{0,39}$`，但**无判据**——规范正文、门禁、schema 三处都没有该约束。风险面来自中文重仓环境：全角字符、大小写变体、同形字会让「看起来同名」的两个事件被当作不同事件（唯一性判据与订阅匹配同时失准）。
2. **变更**：§1.1「发布方唯一」条扩为「发布方唯一 + 事件名词法」；check16 契约仲裁 ④ 块扩为两侧（publish/subscribe）名集扫描 + 词法断言（不合即 FAIL）。机制借鉴：Unicode 碰撞面（大小写/同形字/正规化）的标识面纪律，经仓内实证后净吸收。
3. **兼容**：存量 36 名零违约 → **零返工**；断言只增不减；只约束**标识面**（事件名），不约束正文与资产内容（中文正文不受影响）。
4. **校验回读**：verify 全量 **PASS=61 保持**；负例——把某模块 `publish` 项改成全角或大写（如 `Tick_Day` / `ｔｉｃｋ`）即 check16 ④ FAIL（独立夹具执行）。

**1.6.5 正文正规性扫描（围栏配平 + mojibake 特征）**
1. **现状**：正文解析（`machine_contract` 围栏正则切段）与渲染都依赖**围栏配平**，此前**无判据**；实测 206 件自产正文 **3 件围栏标记计数为奇**，其中 **2 件正文本身是 mojibake**（同源生成产物）——缺陷此前全部隐身（门禁 / 回执 / 许可证门皆绿）。
2. **变更**：`desktop/src/core/doc_hygiene.py` 增 `text_sanity()`（判据两条：围栏标记计数须为偶；单行 mojibake 特征字 ≥3 即命中）+ check33 增第 8 面（**WARN 通道** + 计数入 stats，不判死）+ `desktop/tests/test_doc_hygiene.py` 增 2 例。
3. **兼容**：存量按 **WARN 挂账**（3 件围栏未配平 / 2 件 mojibake），不判死——对齐「存量 legacy 先可数、再逐波收」先例；**不改任何正文内容**；`nf score` 的 `doc_hygiene` 信号只读 `check_markers`，评分面不受影响。
4. **校验回读**：verify 全量 **PASS=61 保持**、**WARN 0 → 5**（可见并计数）；`test_doc_hygiene` 6 例全过；负例——新增一件未配平正文即 WARN 计数 +1。

**1.6.6 JSON-Schema 方言声明断言（`$schema` 与实现一致）**
1. **现状**：五份 IDL schema 全声明 `$schema = https://json-schema.org/draft/2020-12/schema`，但 `schema_lint` 只把 `$schema` 当**白名单关键字**（允许出现）、**从不校验取值**——schema 漂到别的草案而关键字形状相同时门禁无声；自实现子集是按 2020-12 形状做的（`propertyNames` 属 2019-09+ 关键字）。
2. **变更**：`schema_lint` 增 `DIALECT` 常量（方言单一真相）+ `check_schema_files` 增断言（`$schema` ≠ 方言即 FAIL，消息带修复指引）+ `test_schema_lint` 增变异注入例（draft-07 声明被抓、合声明零违例）。机制借鉴：JSON-Schema 生态「多草案共存须显式声明」纪律，经仓内实证后净吸收。
3. **兼容**：存量五份全合 → **零返工**；断言落在 check28 既有通道（不改 verify.sh、不改 check 数）；**不新增表达力**（校验器不因本断言获得新语义）。
4. **校验回读**：`test_schema_lint` 10 例全过；verify 全量 **PASS=61 保持**；负例——任一 schema 的 `$schema` 改成 draft-07 即 check28 FAIL。

**1.6.7 自包含样本编码漂移修复（数据面 · 无协议变更）**
1. **现状**：1.6.5 捞出的 2 件 mojibake 产物（`library/NF-TECHDOC-Monyeah777-1.md` 与 `docs/examples/state-front/techdoc_front.md`）由缺失的历史生成器（`build_selfcontained_sample.ps1`）在 2026-09-15 批次写出**双重编码**，并伴随**行结构合并**与**收尾围栏重复**；UTF-8/GBK 往返在落单字节被替换为 `?` 处**不可逆**（实测仅 2/158 行无损）。
2. **变更**：新增仓库侧工具 `scripts/rebuild_selfcontained_sample.py`（纯标准库 · 幂等 · `--check/--write/--root/--pipeline`）——按产物内 `**4.N … · 溯源：<路径>` 标记把 15 段模块正文与 1 段管线声明**从源文件逐字重嵌**，只换围栏内正文，不动 frontmatter／状态块／骨架，并收掉重复围栏；两件产物据此修复，docs 样本的状态块由 `nf state-front --mode front` 重生成；新增 `test_selfcontained_rebuild` 2 例。
3. **兼容**：**无协议语义变更**（本条只登记数据面修复与工具化）；产物元数据（frontmatter `verified:` 等）未改；正文与源文件**逐字相等**为可复核判据（`--check` 幂等）。
4. **校验回读**：`--check` 两件 **0/15 段漂移**且重跑无变化；正文正规性 **WARN 5 → 1**（仅剩未动的社区域包资产孤立围栏，属内容侧待裁决）；馆藏回执按序重冻结；verify 全量 **PASS=61 · WARN=1 · FAIL=0**；unittest **753** OK。

**1.6.8 端点弃用/日落语义（把挂账项做实）**
1. **现状**：`protocol/endpoint_contract.json` 只固定形状（8 端点 · `status: proposed`）与 `maps_to` 真实性判据，**无弃用语义**——端点要退场时既无标志也无出口约定（此前按「无消费方」挂账；作者裁决后做实）。
2. **变更**：契约 `conventions` 增 `deprecation` 约定 + `desktop/src/core/endpoint.py` 增四条判据（① 弃用须**同时**给 `sunset`（YYYY-MM-DD）与 `replacement`（无替代写 null，不许省略）；② `replacement` 非 null 须指向**契约内存在**的端点 id；③ 弃用只对 `status=implemented` 成立（proposed 期间不许声明弃用——未实装的能力没有可弃用的东西）；④ 未弃用端点不得带 `sunset`/`replacement`（悬空弃用字段））+ `test_governance_faces` 增 4 场景用例（proposed 弃用 / 有标志无出口 / 合规弃用 / 悬空字段）。机制借鉴：OpenAPI 的 `deprecated` 标志与 RFC 8594 `Sunset` 头的「标志 + 出口」配对纪律，经仓内实证后净吸收。
3. **兼容**：存量 8 端点无弃用声明 → **零返工**；断言落在 `endpoint.py` 既有扫描（check36 治理面通道，不改 verify.sh、不改 check 数）；`status: proposed` 的事实不变，本变更不把契约"做实"为服务（**只固定弃用形状**）。
4. **校验回读**：`test_governance_faces` 21 例全过；`nf conformance` `endpoint-contract` detail 仍「端点 8（status=proposed）」、报告根未变（不触契约 detail，无需重签）；同波**收尾两条遗留**——社区域包资产 `11_魔法系统_MAGIC.md` 的孤立收尾围栏已删（§M11.7 示例本就是 markdown 正文形态；单行改动、资产对账全清）→ 正文正规性 **WARN 1 → 0**；`verify.sh` check8 的西幻资产行数**发布基线**按核对结果由 4285 改为 4284（删除 1 行所致，头注同步）；verify 全量 **PASS=61 · WARN=0 · FAIL=0**。

**1.6.9 资产行数基线改为可重签工件（外形冻结 · 显式重签）**
1. **现状**：社区资产行数基线此前写作 `verify.sh` check8 的**字面量**（`[ "$w" -eq 4284 ]`）——任何合法内容改动都会撞它，唯一出口是**改 verify.sh 里的数字**：既没有「谁改的 / 何时 / 为什么」的记录面，也不在规范件清单与回执覆盖面内（改了没人知道）；形态上与模块边界签名（`nf module signature --write` 显式重签）不一致。
2. **变更**：新增核心模块 `desktop/src/core/asset_line_baseline.py`（扫描 / 校验 / 重签）+ 工件 `protocol/asset_line_baseline.json`（每包 `files` / `lines` / 逐文件行数映射的 SHA-256 `digest` + `recorded_at`）+ CLI `nf asset baseline [--write]` + check8 改为读工件（**字面量全部移除**；FAIL 带重签指引；缺包仍按段 B 只记 WARN）+ 登记进 `protocol/normative.json` 与回执覆盖面（`receipts.protocol_subjects`）+ `test_asset_line_baseline` 5 例（README 例外 / 缺基线 / 漂移 / 新增包未登记 / 缺包 WARN / 真仓库当前一致）。机制借鉴：模块边界签名的「外形冻结、内容演进须**显式重签**」纪律，平移到资产面。
3. **兼容**：基线由当前实测重签（校园 29 文件 1575 行 · 西幻 23 文件 4284 行）——与原字面量**逐值一致** → 零行为变化；`digest` 锁「逐文件行数映射」（强于单一总行数）；断言语义不变（内容改动仍判 FAIL），但出口从「改数字」变为「显式重签 + 与内容改动同提交可见」。
4. **校验回读**：`nf asset baseline` 零 FAIL；`test_asset_line_baseline` 5 例全过；check8 输出改为「行数溯源一致（基线可重签）：…」；`nf conformance` 因规范件计数变化（28 → 29）按 PO-0001 顺序重生成报告并重签批准；verify 全量 **PASS=61 · WARN=0 · FAIL=0**。
---
---
