---
decision: 38 方案按「承诺交付」范围执行：接线+Release+冒烟全绿；交互大重构剥离为有用户证据即立项的独立候选项；端壳治理改「冻结-接线-冻结」脉冲节奏
date: 2026-09-06
decider: 用户裁决（2026-09-06 F2 三问）
context: 38 方案 Stage 0 G3
related: [L3_FROZEN.md, ROADMAP §7.6, 23 方案]
---
# 钢人论证工作单

> **问题**：F2 三问裁决：端壳大规模重构（38 方案）的方向输入——终点形态/平台功能差/端壳治理后续

## 1. 问题重述

38 方案 = L3 解冻一次性包络：把 v2.0.0-v2.5.0 冻结期新能力（5 格式导出/双向读入/质量报告/引用反查/市场深化/变体组合/多出口渲染/全链管道/MCP serve）在 exe 与 APK 上接线。前置三问决定方向：① 终点形态——NF 的消费端最终是什么；② 平台功能差——桌面 exe 与 Android APK 的能力取舍边界；③ 端壳治理后续——解冻后端壳是否恢复常态化跟进。

用户裁决（2026-09-06）的框架修正：「双线」不是策略选择，是架构事实——NF 是共享内核（协议/内容库/生成器）+ 多出口（CCV3/SKILL/MCP/rules……），GUI 与 CLI/MCP 只是同一真身的两个出入口，且两个出口都已存在（工具线铺了六个版本，端壳冻结但源码在 git 历史）。问题从「选哪条线」变为「v2.6 承诺什么、端壳价值如何验证」。

## 2. 支持侧最强论据

- 契约要求：23 方案明示'基础层收敛后端壳最后一公里一次性包络'——冻结期已积 5 版本能力，接线账稳定成型
- 双线已是架构事实（共享内核+多出口），v2.6 只需把已冻结出口接回——不发明新架构，只兑现已承诺交付
- L3_FROZEN.md 已备好接回路径（5ae202b^ checkout + sync 重建 + v* tag workflow 触发），基建成本低
- 桌面内置 MCP serve 合体（GUI 编辑内容即时可被 agent 读）是低成本 POC——验证「端壳对工具线有无独特价值」，结果而非口号决定后续投入

## 3. 反对侧最强论据

- GUI/APK 无门禁保护（历史短板）——接线产出质量无自动保障，回归风险高
- 交互大重构（工作台/图谱/拖拽）范围大、无用户证据——预绑 v2.6 会膨胀版本且验证对象错位
- 端壳若继续「一次性产出后迅速过时」，接线价值递减——需治理修订（脉冲节奏）兜底，否则本次接线是重复劳动

## 4. 核心变量

端壳治理节奏（三问③）——「冻结-接线-冻结」脉冲节奏下，端壳永远落后基础层一个接线波而非无限远，每次接线有明确账目；此变量决定 v2.6 是「承诺交付（接线+Release+冒烟）」而非「交互大重构」。

## 5. 判断与理由

**判断**（三问裁决）：
- 三问① 终点形态：双线是架构事实非策略选择；v2.6 = 承诺交付（接线 + Release + 冒烟全绿，一次兑现 23 方案）；交互大重构不预绑 v2.6，作为「有用户证据即立项」的独立候选项；桌面内置 MCP serve 合体（GUI 编辑内容即时可被 agent 读）作为 v2.6 接线期 POC——验证「端壳对工具线有没有独特价值」。
- 三问② 平台功能差：APK 与桌面能力对齐（用户选：工作量更大）。⚠ 待澄清：与三问①「APK 降浏览级」表述的张力（见文末）。
- 三问③ 端壳治理：不恢复常态化，改为「冻结-接线-冻结」脉冲节奏——基础层主循环不变（高频迭代纯 L0-L2 verify 门禁）；端壳默认冻结（沿用 23 方案纪律）；解冻靠明确触发条件（未接线新能力 ≥6 项 或 用户/消费方驱动的必须项）；接线波后回冻结；质量分层永久保留（L3 不参与 verify，端侧质量靠 workflow 构建+冒烟断言）；下一次接线点预设示例 v2.8 或 3.0（按积压触发，不预设死期）。

**理由**：
1. 架构事实论：双出口已存在，v2.6 无新架构决策要做——只有交付范围决策
2. 验证驱动论：端壳价值应由「MCP serve POC 是否被 agent 消费」实证，不由「双线」口号先行
3. 治理红利论：脉冲节奏保留 23 方案全部成本逻辑，同时消灭「一次性产出后迅速过时」死角

## 6. 回退路径

重构失控 → 退回'薄壳接线 + 冒烟全绿'最低交付（v2.6 底线不变）；交互大重构本就不在 v2.6，天然可裁；脉冲触发条件若过严/过松，在下次接线波收口时校准（接线账计数为机读基础）。

## 需落地的治理修订（用户裁决明确列出）

1. **L3_FROZEN.md 契约升级**：从「冻结 → 最后一次性产出」改为「冻结 → 脉冲式接线波（触发条件 + 接线账 + 质量分层永久保留）」，记录本裁决
2. **ROADMAP 治理指针增补**：端壳接线波触发条件入 §8（可被 check 监督的陈述性规则）
3. **接线账清单物化**：每次基础层发布时在 CHANGELOG 标注「端壳待接线项」计数（积压量单一事实源）

## ⚠ 待澄清张力

三问① 用户原文：「APK 降浏览级」；三问② 用户选项：「APK 与桌面能力对齐（工作量更大）」。两处对 v2.6 波 APK 范围的表述不一致——需用户确认：v2.6 接线波内 APK 是「浏览级起步」还是「对齐桌面（大工作量）」？这直接影响 38 Stage 3 工程量与接线账切分。
