---
mode: full
target: 38 方案 Stage 1
verdict: <三态之一：通过 / 需补充以下信息 / 需重做>
date: 2026-09-06
auditor: 天枢（基于项目现状取证）
related: [v2.6 接线波]
---

# M_AUDIT 协议设计审计

> **被审计对象（target）**：38 方案 Stage 1
> **审计问题**：38 Stage 1 是否立即启动（checkout 5ae202b^ 恢复 L3 冻结快照）？

> 引导（人-AI 对话脚手架，Q1/Q2 内置）见文末；完成后回填各节。
## 1. 问题重述

38 方案 Stage 1 = 从 git 历史恢复 L3 冻结快照（git checkout 5ae202b^ -- android/ + desktop/src/ui + desktop/main.py + desktop/src/__main__.py + desktop/packaging + L3 scripts），跑冻结前冒烟（smoke_gui/zone_g/bench）建立基线，然后进入 Stage 2 GUI 接线。待决：G3（F2 裁决 + 治理修订）已完成、G1（CI 线上修复）与 G2（Release 恢复）未完成——Stage 1 是否应先于 G1/G2 启动（本地恢复 + 冒烟不依赖 CI/Release），还是等 G1/G2 全绿再启动。
> Step1 重写问题：若重述后问题变了，说明初始问题质量低（先修问题再审方案）。

## 2. 支持侧最强论据

- 本地恢复不依赖 CI：checkout 快照 + 本地冒烟是纯本地操作，与 G1（CI workflow 修复）G2（Release 资产）正交——先恢复可提前暴露冻结代码与 v2.5 基础层的兼容问题
- 冻结源码在 git 历史 5ae202b^，checkout 是低成本可逆操作（不触碰当前工作区已提交内容，恢复文件进工作区可随时还原）
- 冒烟基线（smoke_gui/zone_g/bench）是 Stage 2-4 的质量参照系——早建立早发现冻结代码与 9 项新能力的接线冲突，降低 Stage 2 风险
> 禁'如果…也许…'弱表述、禁'虽然但是'软化表述。

## 3. 反对侧最强论据

- 接线波纪律（38 F2 裁决 + ROADMAP §8 指针 6）要求触发条件达成后执行完整接线波——G1（CI 红）未修意味着接线波产出无 CI 闸门保障，冒烟基线可能建立在不可信基础上
- 恢复 L3 文件会让工作区携带大量冻结代码（android/ + desktop/src/ui 等），与并发会话（steelman/mcp_runtime 持续提交中）共享工作区时增加文件冲突面
- v2.6 是承诺交付（接线 + Release + 冒烟全绿）——Release 产出依赖 G2 workflow，G1/G2 不绿则承诺交付不完整，先启动 Stage 1 可能造成『部分接线』半成品态
> 反对侧论据不得软化为'只是有点担心'。

## 4. 核心变量

G1（CI workflow 红）是否已修复——CI 绿则接线波有自动闸门，Stage 1-4 可放心推进；CI 仍红则先修 CI 再启动 Stage 1（接线波纪律：产出质量靠 workflow 保障）（Step3：变了结论就该翻转的变量，非影响因素清单；列不出 = 判断未收敛）

## 5. 低置信度清单

**Q1「你最没有把握的事情是什么？」——反幻觉**
低置信度清单（3-7 项，少于 3 = 敷衍，多于 7 = 未收敛）：
- CI workflow 红的根因未知（本地无法复现，gh CLI 不可用）——影响：无法判断是代码问题还是 workflow 配置问题
- 冻结快照（5ae202b^ 时代）与 v2.5.0 基础层（core 已迭代 5 版本）的兼容性未知——影响：冒烟可能大面积失败
- G2 Release 恢复范围未定（历史 tag 重跑 vs v2.6 起恢复）——影响：Stage 4 收口依赖此决策

## 6. 遗漏清单

**Q2「我最大的遗漏是什么？我没有意识到什么？」——反思维惯性**
遗漏清单（3-7 项，每项标注若不处理会怎样）：
- 没考虑并发会话正在改 desktop/src/core 同区域文件（steelman/audit 刚提交）——恢复冻结快照若含 core 旧版会与已提交新版冲突
- 没考虑 android build 依赖（buildozer/KivyMD 版本）在冻结期后是否仍可用——若依赖过期，APK 重建会失败
- 没考虑『恢复哪些文件』的范围决策——L3_FROZEN 移出清单 5ae202b 是否完整覆盖所有需恢复文件（如 scripts/smoke_*.py 是否需同步）

## 7. 评估结论

**评估结论（三态）**：需补充以下信息

**一句话理由**：G1（CI 红根因）与 G2（Release 范围）未决是启动 Stage 1 的前置信息缺口——本地恢复本身可先行（低成本可逆），但完整接线波启动须 G1 绿。

## 8. 行动建议与缺陷条目

**下一步行动**：① 先做 G1 取证（用户提供 gh auth 或贴 CI 日志定位 workflow 红因）；② 本地 checkout 5ae202b^ 到临时分支验证冻结快照可冒烟（不动主工作区，避免并发冲突）；③ G1 绿后正式启动 Stage 1 进主工作区。

**回退路径**：本地验证分支若发现冻结代码与 v2.5 core 大面积冲突 → 丢弃验证分支，回到『先 G1/G2 再 Stage 1』顺序，把冲突清单作为接线账输入。

**本轮缺陷条目（= 下轮迭代输入）**：
- （缺陷：G1 线上状态不可本地核验——需用户补 gh auth 或日志）
- （缺陷：G2 Release 恢复范围待用户定——历史 tag 重跑 vs v2.6 起）


## 引导模板（填充后删除本段）

--- Prompt A（路线决策）---
两三条路线各列最强论据，再比较核心变量。
--- Prompt B（需求收敛）---
重述真实问题（剥离方案），支持=为什么现在做，反对=为什么可缓。
--- Prompt C（设计评审）---
支持=该设计不可替代处，反对=耦合/维护/边界风险。
--- 盲区双问（blindspot/full 内置）---
Q1 你最没有把握的事情是什么？（低置信度，3-7 项）
Q2 我最大的遗漏是什么？（未意识到的假设/场景，3-7 项）