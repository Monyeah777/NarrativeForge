# 16 · v1.4.0 质量治理闭环：自定义质检门 + check17 入段 C（2.0 E0-③）

> **需求提炼（用户原话）**：「质量治理闭环（可信任度层）解决如何保证导出的产物质量可验证——不是让输出更好看，而是让每次导出都经过一套可验证的质检规则」；「用户可通过界面定义或选择质检规则，系统在每次导出前自动执行并给通过/警告/失败结果」；「验收：每个 2.0 版本发布前，都能用同一个组合包通过质检并导出，在目标生态实际运行验证」。
> **非目标**：GUI 规则编辑器（自定义规则集 UI 编辑随 v2.0 导出层收口）；v2.0 导出适配器（本版质检门消费 IR，先不接导出）。

## 0. 定位

v1.2 IR + v1.3 检索之上：给"装配产物"加**可验证的质检规则执行**（PASS/WARN/FAIL 三态）——质检在 IR 层跑（不解析 MD），为 v2.0 导出前自动质检铺引擎。check17 正式入 verify 段 C（2.0 基线 §6 承诺：v2.0.0 起导出产物过 check17 方收口）。

## 1. 问题全景

- **A1 质检散在静态 check**：verify check1-16 查文档/代码一致性；装配产物本身（IR/MD）无"可运行规则 → 三态结果"的执行机制。
- **A2 导出前无强制门**：v2.0 若导出不带质检，坏产物（缺核心/资产引用悬空）会进生态——可信任度不成立（基线 §2 E0-③）。
- **A3 规则不可组合/选择**：无规则集概念，无法"选一套规则跑这次导出"。

## 2. 改进收口

- **B1 质检门引擎（A1/A3）**：新增 `desktop/src/core/quality_gate.py`：

```python
@dataclass
class Issue: level: str   # pass / warn / fail
             message: str

@dataclass
class GateResult: n_pass: int; n_warn: int; n_fail: int; issues: list[Issue]
                  def ok(self) -> bool   # fail == 0

def default_rules() -> list[Callable[[IRDocument], list[Issue]]]
def run_gate(ir: IRDocument, rules=None) -> GateResult   # rules 缺省 = default_rules

# 预设规则（default_rules，规则语义对齐现有 validator/order_modules 缺漏提示）：
#  fail 规则：
#   R1 空装配：ir.layers 全空且 extra 空 → fail（无可产出内容）
#   R2 核心锚点缺失：P00/P80 层无模块（数据基座/输出呈现，registry 锚点语义）→ fail
#  warn 规则：
#   W1 资产引用悬空：ir.asset_missing 非空 → warn（列出缺失键）
#   W2 层外模块：ir.extra_modules 非空 → warn（模块层不在管线层序内）
```

  消费 IR（v1.2 结构），不解析 MD——质检与导出共享同一 IR 真源。

- **B2 check17 入段 C（A2）**：verify.sh 段 C 追加 check17「质量治理门禁」——跑 `test_quality_gate` unittest（合法装配 IR → GateResult.ok()=True；缺核心/空装配 → fail 计数>0；悬空资产 → warn），任一 FAIL = 协议事故。PASS 计数 22→随新增 check 增（历版门禁扩容同例：check14/15/16 各 +）。

- **B3 消费位（A2 半程）**：`zone_d.do_generate` 生成后追加 run_gate 结果展示（三态行 + 明细到 warn_view，装配即可见质检）；"导出前强制"完整闭环随 v2.0 适配器（本版引擎就绪 + 生成后可见）。

## 3. 任务分解

- **T1 quality_gate.py + 单测（无前置）**：Issue/GateResult/run_gate/default_rules 四规则。单测：空装配 fail / 缺核心 fail / 资产悬空 warn / 合法装配 ok / ok()=fail==0。
- **T2 zone_d 生成后展示（依赖 T1）**：do_generate 生成预览后跑 run_gate(ir)，warn_view 增"—— 质量门 ——"三态汇总 + 明细。
- **T3 verify check17（依赖 T1）**：verify.sh 段 C 加 check17（unittest test_quality_gate），注释对齐 check14-16 风格；verify PASS 全绿。

## 4. 提交规划

- C0 = T1（quality_gate.py + test_quality_gate.py）
- C1 = T2 + T3（zone_d 展示 + check17 + 回归）
- 每批 verify 全绿方可推（阶段工作流：先推→方案→执行→完成推）。

## 5. 验证清单

- `python -m unittest discover -s tests`（90 + 新增全绿）
- `bash verify.sh` PASS 全绿（含新 check17）
- e2e headless PASSED
- zone_d 人工：生成装配 → warn_view 见"质量门"三态（Windows GUI）

## 6. 瑶光反证

- **断言**：v1 无三态质检门——证据：v1 validator.py 仅 82 行（validate_module + check_assembly 核心锚点），无 run_gate/规则集（本会话 grep 实证）。
- **断言**：IR 有 asset_missing/layers/extra_modules 字段供规则消费——证据：本会话 v1.2 实现 ir.py dataclass（asset_missing/list、layers、extra_modules）。
- **断言**：verify.sh 段 C 现有 check12-16 结构（check17 追加点）——证据：本会话已读 verify 主执行体（段 C 逐 check 调用）。
- **待验证假设**：zone_d.do_generate 现在产出后写 warn_view（装配检查+生成器提示）——T2 前读 v1 zone_d 确认追加点（v1.0 可能改过）。

## 7. 范围外（留 v2.0）

- GUI 自定义规则编辑器 / 规则集持久化选择 / 导出前强制门（适配器内执行）——引擎本版就绪，编辑与强制随导出层收口。
