# NarrativeForge · v0.8.0 自定义模块组合方案（落盘执行版）
> **版本定位**：v0.8.0「自定义模块组合——在 v0.7 开放协议注册之上进一步对外开放模块组合，第三方题材模块可在 P00 骨架上自由组合装配为新的社区管线」（方案甲：放开受控跨包引用）
> **基线**：v0.7.0 已发布（tag v0.7.0=`bade8b6` 收口 commit）+ 工作区干净（HEAD=`bade8b6`，与 origin/main 完全同步）
> **文档状态**：本文件为 v0.8.0 方案落盘真相源（I5）；取证时间 2026-09-04，基线 `bade8b6`
> **编号声明**：方案按「问题 A → 改进 B → 任务 T」三层自洽编号。硬锚点与 ROADMAP §1 行 v0.8.0（「对外开放模块组合，第三方题材模块可组合装配」）及 §5 一句话目标对应：A1 模块组合无机读通道、A2 组合装配合法性无门禁、A3 管线派生纯人读无组合清单；B1–B3 一一对应；T1 组合引用 Schema 化（protocol.yaml v1→v2，无前置）→ T2 组合登记机制 + 两包 retro-fit + registry protocols[] 投影（依赖 T1 Schema 定义）→ T3 verify.sh check15 组合门禁 + v2.5（依赖 T1 校验规则与 T2 落盘素材）。「协议开放三部曲」中 v0.7 完成「开放自定义协议（包级注册）」，v0.8 承接开放「模块级组合复用」，v0.9/v1.0 的 Android 同步门禁与生态收口本版不做。
---
## 0. 定位与边界
- **做什么**（方案甲，三做）：全部改动落在 **protocol.yaml Schema 组合引用机读化（01 §6.1 v1→v2 + references 字段）/ 组合登记与契约校验规则（02 §8.4 + registry.json protocols[] references 投影）/ 组合门禁 check15（verify.sh v2.5）** 三类。R1 语义受控演进：社区包由「禁跨包依赖（cross_package 恒空）」升级为「禁跨包依赖 + references 受控跨包模块引用白名单（依赖闭包必须闭合到官方核心层）」——**references 为唯一跨包引用通道**，cross_package 保留恒空（deprecated 兼容字段）。既有两包 retro-fit schema_version "1"→"2"（references: []，向后兼容演示）。不新增题材内容、不改官方核心 13 件职责语义、不动社区两包既有模块/资产内容。
- **不做什么**：不复制/移动任何模块与资产文件（引用保持单一真相源，被引模块仍归源包，防双源漂移）；不做 Runtime 组合调度引擎改造（registry_loader 保持只读官方核心投影，不消费 references 做装配驱动——引擎级消费归后续版本）；不开放跨包资产内容复制（资产仍随源包，组合包经 references.asset_readonly 声明源包资产只读授权，运行时寻址实现留开放问题 1）；不做完整题材级可玩组合包战例（本期战例 = 两包 retro-fit + 文档级组合声明示例，战例深度留开放问题 3）；APK 闪退修复与 Android↔真源同步差异入 CI（= v0.9.0）；端到端测试入 CI（范围外）。
- **护航不变式**：I1 三正交分离 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一；平台规则 R1（社区包依赖边界）/ R2（类别独占）/ R3（装配契约）。**本版受动声明**：R1 由「社区包禁互引」演进为「禁互引 + references 受控引用（依赖闭包闭合官方核心）」——check14 ④ 断言同步升级为「cross_package 恒空 且 references 合规」；v1 声明（无 references）零改动向后兼容不破。I5 由 v0.7.0「文档层 + 机读投影 + 协议声明三校验唯一」扩展为「文档层 + 机读投影 + 协议声明 + 组合登记四校验唯一」——02 §8.4 组合登记 ↔ protocol.yaml references ↔ registry protocols[] references 三者一致，由 check15 ⑤ 断言。registry_schema_version 保持 "2" 不动（protocols[] 加 references 为字段级新增，走 V1 只增不删）；protocol.schema_version "1"→"2" 为声明 Schema 结构性变更（V3 版本字段独立，不触发 registry bump）。其余不变式不受动。
- **改动范围**：01_核心协议.md（§6.1 Schema v1→v2：字段表补 references + 模板骨架升 v2；§7 迁移记录 v1→v2 四步入档）、02_联动注册表.md（§8.4 组合管线登记条款 + 文档级组合声明示例；§9.1 字段映射补 references 行；§9.2 组合同步条）、community/校园情感领域包/、community/西幻生存领域包/（protocol.yaml schema_version→"2" + references: []；README 双源声明版本行同步）、desktop/src/core/registry.json（protocols[] 两包补 references/schema_version "2"）、verify.sh（check15 + v2.5）、ROADMAP（§1 行 v0.8.0 状态切主线 + §5 范围细化落盘）、README（协议链追加 11 + 版本行切 v0.8.0）、CHANGELOG（[Unreleased] 回填，v0.7.0 收口批次已切至 v0.8.0）。
---
## 1. 问题全景（A1–A3，实测取证）
### A1 模块组合无机读通道（ROADMAP §5「第三方题材模块可组合装配」无人读以外落地形态）——protocol.yaml 必填 12 字段中 `dependencies.cross_package` 被字段表与模板注释钉死「禁跨包依赖，必须为空」（01 §6.1：必填字段表 cross_package 行「空数组 []（禁跨包依赖）」；模板骨架 `cross_package: [] # 禁跨包依赖，必须为空`；check14 ④ 断言 cross_package 为空），`modules[]` 只允许「包内自带（M91-M99 预留段）+ 重号限定引用官方核心 13 件（如 `情感:M22` = 引用官方事件总线）」两类。想组合校园 M55 匿名情书 × 西幻 M17 生产制造为跨题材管线（ROADMAP「对外开放模块组合」的直接语义），协议层**无引用声明字段、无通道**——第三方只能在「复制模块文件进自家包」与「放弃组合」间二选一，前者破坏单一真相源（I5）。
### A2 组合装配合法性无门禁——verify.sh 段 B（check7–11：两包结构/行数溯源/引用可寻址/EXT 闭合/三方对账）与段 C（check12–14：代码层/协议一致性/社区登记）只验「**包内**」合法性；组合产物须回答的四问——①引用目标是否在册可寻址（02 §8/registry protocols[] 能否解析）②组合依赖图是否闭合到官方核心（沿 references 展开有无环/悬空）③挂载层同层 default 槽位是否冲突（两模块争同一层 default）④装配相邻模块契约是否断裂（01 §1 events：前一模块 publish ⊆ 后一模块 subscribe）——**均无任何断言**。「组合合法与否」全靠人眼，与 A1 叠加即第三方复制 P00 派生新管线后无闸可跑、出错无门禁拦截。
### A3 管线派生纯人读、组合无清单实体——P00 运行规则（03_管线库/P00_通用文档生成管线.md）「新领域接入三步：①复制本文件派生新管线（改 id/name/层名/default/allowed/flow/tags）②新模块按 01 §2 声明进 04_模块库或复用通用类 ③在 02_联动注册表登记挂载表与 execution_order → Agent 即按注册表调度，引擎代码不改」；07 §5「新领域包/新管线：以 P00 通用骨架（03_管线库/P00_通用文档生成管线.md）为模板复制派生」——组合产物的 structure/flow/挂载槽位/引用来源散落于人读 md，**无组合清单（compose manifest）机读实体**，第三方「组合装配」无模板可抄、无流程可循、无校验器可跑（与 v0.7.0 之前「登记规则纯人读」同构的空白）。
---
## 2. 改进收口（B1–B3，与 A1–A3 对应）
### B1 组合引用 Schema 化（A1/B1）：01 §6.1 protocol.yaml Schema **v1→v2**（V2 bump，01 §7 迁移四步）
- 新增 `package.references`：跨包模块白名单引用数组，元素 = `{source_package 源包 id（须与 02 §8 在册一致）、module_id 被引模块限定 id（如 校园:M55）、source_schema_version 源包协议版本钉扎（源包 bump 后引用方须复核，见 §6 开放问题 2）、asset_readonly 源包资产只读授权 bool}`。可选字段，**缺省 [] = 纯官方依赖包，v1 声明零改动向后兼容**（check14 ④ 升级为「cross_package 恒空 且 references 合规」，v1 文件不破）。
- `dependencies.cross_package` 语义标注：**deprecated 兼容字段（恒空 []），references 为受控跨包引用唯一通道**——R1 由「禁跨包依赖」演进为「禁跨包依赖 + references 受控引用（依赖闭包闭合官方核心）」。
- 必填字段表补 references 三行（字段路径/含义/对齐规则）；模板骨架升 v2（含 references 示例注释 + cross_package deprecated 标注）；01 §7 迁移记录 v1→v2 四步入档（schema_version 独立演进 V3 说明：不触发 registry_schema_version bump）。
### B2 组合登记与契约校验规则（A2/B2）：02 §8 新增 §8.4 组合管线登记 + 投影扩展
- **组合包定义**：自带管线 Pxx + 自有模块（新增编号落 M91-M99）+ references 组合清单（module_id_range + references + mount_layers 即组合清单机读实体，不另设独立 compose 文件防清单漂移）。
- **登记判定四规则**：①引用目标在册可寻址（02 §8.1/8.2 或 registry protocols[] 可解析）②依赖闭包闭合官方核心（references 沿源包 dependencies.core_modules 展开成图：无环、无悬空、叶节点 ⊆ 官方核心 13 件）③挂载层同层 default 槽位唯一（组合包 mount_layers 与源包叠加，同层不争 default；available 槽位并存允许）④契约提示（相邻装配模块 publish⊆subscribe 断裂 WARN 不 FAIL——模块头契约机读化未完备前不强校验，收紧留 §6 开放问题 4）。
- registry.json protocols[] 投影扩展 references/asset_readonly（registry_schema_version 保持 "2"，字段级新增走 V1 只增不删）；§9.1 字段映射补行；§9.2 纪律补「组合同步」条（组合登记 ↔ protocol.yaml ↔ protocols[] 三处一致，check15 ⑤）。
### B3 check15 组合门禁 + v2.5（A3/B3）：verify.sh v2.5 新增 check15（入段 C 无条件执行，check14 之后）
五断言：①references 引用目标在册可寻址 ②依赖闭包闭合官方核心（图遍历：无环/无悬空/叶节点 ⊆ 13 件）③挂载层同层 default 冲突检测 ④schema_version v2 兼容（v1 文件可读、references 缺省等价向后兼容）⑤双源一致（protocol.yaml references ↔ registry protocols[] references 逐条一致）。PASS 19→20。
---
## 3. 任务分解（T1–T3，依赖序）
- **T1 组合引用 Schema 规范落盘（A1/B1，v0.8.0 核心命题）**：01 §6.1 必填字段表补 references 三行 + 模板骨架升 v2（references 示例 + cross_package deprecated 标注）+ 01 §7 迁移记录 v1→v2 四步。无前置。
- **T2 组合登记机制 + 两包 retro-fit + registry protocols[] references 投影（A2/B2）**：02 §8.4 组合管线登记条款（四规则）+ §8.4 文档级组合声明示例 + §9.1/§9.2 扩展；校园/西幻两包 protocol.yaml schema_version "1"→"2" + references: []（向后兼容演示）+ 两包 README 双源版本行；registry.json protocols[] 两包补 references/schema_version "2"。依赖 T1 Schema 定义。
- **T3 verify.sh check15 组合门禁 + v2.5（A3/B3）**：check15() 函数（五断言）+ 段 C echo/调用 + banner v2.4→v2.5 + L4/L10 配套注释同步；全量 verify PASS 19→20 全绿。依赖 T1 校验规则与 T2 落盘素材。
---
## 4. 任务明细
### T1 组合引用 Schema 规范落盘（A1/B1，v0.8.0 核心命题）
- 01 §6.1：必填字段表新增三行——`package.references`（跨包模块白名单引用，可选缺省 []，对齐 R1 references 规则）、`references[].source_package`（源包 id，须 02 §8 在册）、`references[].module_id`（被引模块限定 id，须源包 modules[] 在列）；`references[].source_schema_version`（源包协议版本钉扎）；`references[].asset_readonly`（资产只读授权 bool）。
- 01 §6.1 模板骨架升 v2：`schema_version: "2"` + `references: []` 示例注释（含一条组合引用示例：`- {source_package: 校园情感领域包, module_id: 校园:M55, source_schema_version: "2", asset_readonly: true}` 标注「组合示例非完整可玩包」）；cross_package 注释改「deprecated 兼容字段（恒空），references 为唯一跨包引用通道」。
- 01 §7 迁移记录 `### 1.1 迁移记录（protocol.schema_version v1 → v2，组合引用 references 引入）`：四步（现状快照 v1 = 12 必填字段无 references、消费方 check14 ①-⑥；bump 声明 "1"→"2" 依据 = references 结构性新增字段触发 V2、仅 protocol.yaml 独立演进 V3 不触 registry；迁移说明 v1 文件可读、references 缺省 = 纯官方依赖包与 v1 等价；校验回读 check15 ④ + check14 ④ 升级）。
### T2 组合登记机制 + 两包 retro-fit + registry protocols[] 投影（A2/B2）
- 02 §8.4「组合管线登记（v0.8.0 引入：模块组合）」：组合包定义 + 登记判定四规则（在册可寻址/依赖闭包闭合/同层 default 唯一/契约 WARN 提示）+ 与 §8.1/8.2 整包登记的关系（组合包同样须三要件：protocol.yaml 在场 + 本表在册 + protocols[] 投影；references 为 protocol.yaml 内部字段）。
- 02 §8.4 文档级组合声明示例：以 YAML 骨架演示「跨题材组合声明」——引用校园 `校园:M55 匿名情书` + 西幻 `西幻:M17 生产制造`（原模块不改号，全限定 id 引用）+ 官方核心 13 件子集，自带管线 P04 示例名 + 挂载层叠加声明，注释标注「声明示例，验证 Schema/门禁可接受；非完整可玩包，完整战例留 v0.8.1（§6 开放问题 3）」。
- 02 §9.1 字段映射表补行：`§8.4 组合登记 | 组合包 references 组合清单 | protocols[].references[]：source_package / module_id / source_schema_version / asset_readonly`。
- 02 §9.2 纪律补条：**组合同步（v0.8.0 起）**——组合登记 / protocol.yaml references / registry protocols[] references 三处一致，check15 ⑤ 断言。
- 校园/西幻两包 protocol.yaml：`schema_version: "1"`→`"2"`（注释改「组合引用 Schema 版本，v2 起支持 references；bump 走 01 §7」）+ `references: []` 行（注释「v0.8.0 retro-fit：无跨包引用，v2 向后兼容演示」）+ `cross_package` 注释改 deprecated 标注；两包 README 双源声明版本行同步（protocol.yaml schema_version "2"）。
- registry.json protocols[]：两包条目补 `references: []` + `schema_version: "2"`（字段级新增，registry_schema_version 保持 "2"）。
### T3 verify.sh check15 组合门禁 + v2.5（A3/B3）
- check15() 函数（L 追加，五断言）：①遍历每包 references，source_package 在 02 §8.1/8.2 或 registry protocols[] 可解析、module_id 在源包 module_ids 在列（缺则 miss++）②依赖闭包：以 references 起点沿源包 dependencies.core_modules（递归 references）BFS/DFS 展开，检测环与悬空，叶节点 ⊆ 官方核心 13 件（OFFICIAL13）③挂载层冲突：组合包 mount_layers 各层 default 与源包同层 default 取交集非空则冲突（校园/西幻两包 references 空 → 本项天然 PASS）④schema_version 兼容：v1（无 references 字段）与 v2 文件均可读，references 缺省等价 ⑤双源一致：protocol.yaml references ↔ registry protocols[] references 逐条比对（source_package/module_id/source_schema_version/asset_readonly）。
- v2.5 四处同步：L4 配套注释（补 11 v0.8.0 check15 引用）、L10 段 C 描述（check12/check13/check14 → 补 check15）、L483 banner（v2.4→v2.5）、段 C echo + check15 调用（check14 后追加）。
- 全量验证：PASS 19→20，WARN 0 FAIL 0 全绿。
---
## 5. 提交规划
| 序 | 提交内容 | 前缀 | 关联 |
| --- | --- | --- | --- |
| C0 | 11 方案落盘 + ROADMAP §1 行 v0.8.0 状态切「🔵 当前主线」/§5 范围细化落盘 + README 协议链追加 11/版本行切 v0.8.0 + CHANGELOG [Unreleased] 回填 | docs(roadmap) | 本文件 |
| C1 | T1 01 §6.1 Schema v1→v2（references 字段 + 模板升 v2）+ §7 迁移记录 | docs(protocol) | A1/B1 |
| C2 | T2 02 §8.4/§9 升级 + 两包 protocol.yaml retro-fit + README 双源同步 + registry.json protocols[] | docs(protocol) | A2/B2 |
| C3 | T3 verify.sh check15 + v2.5 四处同步 | feat(verify) | A3/B3 |
| C4 | 全量 verify 绿（PASS=20）+ tag v0.8.0 收口 + push | chore(release) | 收口 |
每任务独立 commit；C1–C3 期间工作区只允许该任务相关文件变更，避免交叉。C4 push 前须向用户索取新 PAT 并 revoke 旧 token（见 §6 开放问题 6）。
---
## 6. 风险与开放问题
1. **组合包资产只读通道运行时**：声明层已授权（references.asset_readonly），但 desktop Runtime asset_get 跨包只读寻址实现不属本版——本版交付协议层（Schema/登记/门禁）；组合包被引擎装载消费需运行时支持，引擎级组合调度留后续版本（同 10 §6 开放问题 5 纪律：不顺手扩展 loader 功能，防范围蔓延）。
2. **源包升级兼容治理**：references.source_schema_version 钉扎源包协议版本；源包后续 bump（V2 结构性变更）后引用方组合包须复核契约（check15 ② 闭包重跑 + 人读复核）；自动联动治理（引用方强制复核门禁）留 v1.0.0 生态收口。
3. **战例深度**：本期战例 = 两包 retro-fit（references: [] 向后兼容演示）+ 02 §8.4 文档级组合声明示例（声明骨架验证 Schema/门禁，非完整可玩包）；完整题材级可玩组合包（如「校园 × 西幻 轻混」实包）是否 v0.8.1 补，视示例战例验收反馈定。
4. **契约闭合校验强度**：本期相邻装配 publish⊆subscribe 断裂仅 WARN（模块头 inputs/outputs/events 机读化未完备，强校验会误伤合法弱耦合装配）；待模块头契约机读化（后续版本）后收紧为 FAIL，check15 ④ 升级路径预留。
5. **组合管线 execution_order 机读化**：组合包自带管线 flow 仍为人读声明，登记后 Agent 按注册表调度（I5：引擎代码不改）；flow → registry.json 机读 execution_order 投影是否纳入 modules 层消费，留后续版本评估，本版不立项。
6. **推送安全（操作层）**：C4 push 前须向用户索取新 PAT 并 revoke 旧 token（沿用历版流程）；C0–C3 本地 commit 不受影响。
7. **check15 环境依赖**：references 解析沿用 check14 的 python3（PyYAML）依赖；缺失时按 check13/14 同款 WARN 降级处理，不因环境缺依赖误报 FAIL。
8. **v0.8.0 交付锚点核验**：ROADMAP §1 行 v0.8.0 状态切「🔵 当前主线」+ 方案真相源落 11 文档 + verify.sh v2.5（check1–15）全绿 + 两包 protocol.yaml schema_version "2" 在场 + registry protocols[] references 同步——五项齐备方可进入 v0.9.0 立项。
