# 20 · v2.0.x-E3 组合运行时调度引擎（模块跨包引用的运行时消费）

> **需求提炼**：E3 承接 v0.8/v1.0 references 受控跨包组合，2.0 增量 = 组合装配的**运行时调度引擎**（v0.8 外推"引擎级组合调度"遗留项）——让 references 声明运行时真正生效：引用模块正文入装配/导出（当前只 asset_get 资产寻址，模块正文未装配）。批准方案全文 .rivet/plans/v2-0-x-e3-组合运行时调度引擎-references-跨包引用的运行时消费.md。
> **非目标**：不做跨包内容复制入包（只读引用 I5）；不改 check15 静态校验；不做包间互引扩展。

## 核心

`composer.py`：`resolve_combination(store, pipeline)` 按 registry protocols references（实证轻混引用校园 M55/西幻 M17）闭包解析 own(M91/M92) + reference(source modules parse 装载) + core 完整装配集；`build_assembly` 在 render_ir 前合并 → 跨包组合运行时生效。render_ir 默认含引用 → e2e/桌面/导出一次吃到。

## 文件
- 新增 `desktop/src/core/composer.py` + `desktop/tests/test_composer.py`
- 修改 `desktop/src/core/generator.py`（装配前 build_assembly）+ `scripts/e2e_desktop_headless.py`（[8] 断言含 M55/M17）
- 本文件 20（方案真相源）

## 验证
- test_composer：轻混 P04 解析 own+ref(M55校园/M17西幻)+core；引用缺失 warn；build_assembly 合并
- e2e [8]：导出 world 含 M55/M17（跨包运行时生效端到端）
- 全量单测 + verify PASS 保持
