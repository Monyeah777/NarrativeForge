# 21 · v2.0.x-E4 模块市场雏形（zone_g 升级检索驱动一站式视图，2.0 E4 目标落地）

> **需求提炼**：「E4 模块市场雏形：zone_g 升级为检索驱动一站式视图——消费 retriever.search 四类（module/asset_pack/pipeline/protocol），含"从搜索结果加入装配"」。检索范围语义经用户确认 = **本地资源**（已安装模块/本地资产/缓存管线/在册协议），即 search() 现状，不扩 community 仓库盘点、不做在线拉取。批准方案全文见 .rivet/plans/21-v2-0-x-e4-模块市场雏形-zone-g-升级检索驱动一站式视图.md。
> **非目标**：不做社区仓库盘点检索；不做 GitHub 在线拉取/更新（zone_g head 注释"阶段②"维持占位）；不改 01/02 协议层/registry.json/retriever.search 核心逻辑；不新增主窗口 tab；不删现有导入/E2 向导功能。

## 核心

`zone_g_community.py` 垂直分区改造（不新增主窗口 tab）：顶部检索区（kind 下拉：全部/模块/资产包/管线/协议 + 关键词 + 搜索）→ 中部结果 QTableWidget（类型|引用|名称|信息，行选中底部动作按钮按 kind 分流：module「加入装配」/ pipeline「设为当前管线」/ asset_pack「选用资产包」/ protocol「查看详情」；module 双击弹既有详情对话框；已装配 module 行信息列标「✓ 已装配」）→ 底部工具区收纳现有 ①目录导入 ②单文件导入 ③创建自定义协议（E2）。消费 `retriever.search()`（E0-② 已就绪、当前无 UI 消费方）。

`main_window.py` 增两协调方法：`add_module_to_assembly(full_id)`——**追加**进 app.selected（区别于 apply_preset_state 的重填语义）+ zone_c/zone_b/zone_d refresh + 状态栏；`set_current_pipeline(pid)`——切管线复用子序列。

## 文件

- 修改 `desktop/src/ui/zone_g_community.py`（主：检索+结果表+装配动作+工具区收纳）
- 修改 `desktop/src/ui/main_window.py`（add_module_to_assembly + set_current_pipeline 协调方法）
- 新增 `scripts/smoke_zone_g_market.py`（offscreen GUI 冒烟：检索命中/加入装配 → selected 断言 + zone_c 勾选同步）
- 本文件 21（方案真相源）+ README 协议链补 19/20/21（顺带收口 E2/E3 遗漏，治理指针 2）

## 验证

- `cd desktop && python -m unittest discover -s tests` 全绿（test_retriever 5 用例保持 = 消费源回归锚点）
- `python scripts/smoke_zone_g_market.py`（offscreen）：M00 命中 module 行 / 空 query 四类合计 >0 / 加入装配 → selected 含 full_id
- `bash verify.sh` PASS=24 保持（无 verify 改动）+ `python scripts/e2e_desktop_headless.py` PASSED（core 链路不回归）
- README 协议链 L32 含 19/20/21（grep 复核）

## 分波

单 GUI feature + 协调方法 + 冒烟脚本——单波实现 + 提交（阶段工作流：先推→方案审批→实现→完成推）；GUI 真机人工验证按用户惯例延后（最后集中修小问题范畴）。
