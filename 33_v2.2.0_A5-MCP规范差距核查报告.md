# 33 · v2.2.0-A5 MCP 导出物 vs MCP 规范（2025-11-25）差距核查报告

> **性质**：一次性实证核查（33 方案 Wave3 A5）——文档落盘不建机制。核查对象 = 27 方案 A2 的 mcp 导出物（`desktop/src/core/mcp_adapter.py` → mcp.json），对照基准 = Model Context Protocol 规范 2025-11-25 版（`modelcontextprotocol/specification` `schema/2025-11-25/schema.ts` 权威 schema）。
> **实证方式**：真实 techdoc IR（P90 + M90，与 27 方案验收样例同构）经 `export(ir, 'mcp', dest_dir)` 产出 mcp.json，逐字段对照 schema.ts interface 定义。规范原文经 modelcontextprotocol.io 2025-11-25 spec 页 + GitHub schema.ts 实抓（2026-09 本会话取证）。

## 核查对象：NF mcp.json 产物（实测）

```json
{ "mcp": {
    "name": "P90-mcp",
    "version": "0.1.0",
    "capabilities": { "resources": {} },
    "resources": [
      { "uri": "nf://P90/P10/协议骨架-M90",
        "name": "技术文档类:M90 · 协议骨架",
        "mimeType": "text/markdown",
        "text": "协议必含：前置声明/字段表/示例三段。" }
    ]
} }
```

顶层单键 `mcp`；内部 `name`/`version`/`capabilities.resources`/`resources[]`（每模块一条 Resource，正文内嵌于 `text`）。

## 规范事实（schema.ts 2025-11-25，权威定义）

| 规范要素 | 定义（file:line 实证） | 形态 |
|---|---|---|
| MCP 本体 | `JSONRPCMessage` 信封（schema.ts 头） | **运行时 JSON-RPC 会话协议**（经 transport 交换消息），规范不定义任何「server 定义 JSON 文件」格式 |
| ServerCapabilities | L386 `resources?: {subscribe?: bool; listChanged?: bool}` | initialize 响应中声明的能力对象；resources 键在场即声明支持 |
| Resource（list 返回） | L802 `uri`(必) + BaseMetadata `name`(必)/`title?` + `description?`/`mimeType?`/`annotations?`/`size?`/`_meta?` | **纯元数据，无 text 字段** |
| ListResourcesResult | L664 `resources: Resource[]` | 列目录（元数据），不含正文 |
| ReadResourceResult | L723 `contents: (Text\|Blob)ResourceContents[]` | 按 uri 读正文；`TextResourceContents`(L895) = `uri` + `mimeType?` + `text` |
| Implementation | L550 `name`(必)/`version`(必)/`description?`/`websiteUrl?`/`icons?` | initialize 的 serverInfo（握手元数据） |

## 差距清单（对照裁决）

| # | 维度 | 2025-11-25 规范 | NF mcp.json | 判定 |
|---|---|---|---|---|
| G1 | 产物形态 | 运行时 JSON-RPC 会话协议；无静态定义文件概念 | 静态定义快照 JSON 文件 | **根本差距**——mcp.json 不是 wire 协议消息，不能被 MCP client 直接加载为 server。但 27 方案非目标已明示「产 server 定义 JSON 文件，非跑 server」→ 形态选择，非缺陷。 |
| G2 | Resource.text | Resource 是元数据，**无 text**；正文属 `resources/read` 响应的 `TextResourceContents.text` | text 内嵌 Resource | **字段级差距**——把「list 元数据 + read 正文」两段式压成一段静态快照。schema 语义上 text 放错了对象。 |
| G3 | capabilities.resources | `resources?: {subscribe?, listChanged?}`；`{}` = 声明 resources 支持、无订阅/变更通知 | `{}` | ✅ 合法（空对象 = present 即声明，两个可选布尔都缺省）。 |
| G4 | server 元数据（name/version） | 属 initialize 握手 `serverInfo: Implementation` | 顶层 `mcp{name, version}` | 键/值语义一致，归属层不同（文件自述 vs 握手消息）。静态文件无握手，故无规范对应锚点——非违规。 |
| G5 | 顶层容器 | 无文件概念 | 包一层 `mcp{}` | 自定义容器，规范无对应物——非违规（文件格式本身不在规范内）。 |
| G6 | mimeType | `text/markdown` 任意合法串 | ✓ | 无差距。 |
| G7 | uri scheme | `nf://…`（RFC 3986 任意 scheme，server 自行解释） | ✓ | 无差距。 |
| G8 | tools/prompts | 可选能力；无结构化工具参数源时不声明 | 不声明 | ✅ 诚实映射（27 方案裁决：硬造 Tool 会伪造 inputSchema）。 |

## 结论

1. **形态定位**：NF mcp.json = 「MCP 资源清单快照 / server 自述文件」——开发、归档、文档用途，不是可运行的 MCP server，也从未声称是（27 方案非目标）。与 2025-11-25 规范的差距是**形态级（G1）+ 字段级（G2）**，其余字段合法。
2. **最大语义偏差 = G2**：若未来以 2025-11-25 为合规目标，`text` 不应在 Resource 上——规范中正文必须经 `resources/read`（按 uri）返回 `contents[].text`，`resources/list` 只给元数据。静态快照把正文烘焙进 list 条目，是把两步协议压平为一步。
3. **转运行时所需**（C1 MCP Server 立项时对照此清单）：JSON-RPC transport（stdio/HTTP）+ initialize 握手（serverInfo=name/version、capabilities.resources）+ `resources/list`（返回**去 text** 的 Resource 元数据）+ `resources/read`（按 uri 返回 text 正文）。
4. **check19 边界声明**：`export_schema.check_mcp_json` 校验的是 **NF 自定义快照 shape**（mcp.name/capabilities.resources/resources[] 在场），**不构成 MCP wire 合规校验**——它是 NF 产物自身的形状门禁，非 MCP 规范符合性门禁。两者不可混淆。
5. **C/D 族联动**：本报告为 ROADMAP C1（MCP Server，绑三问①终点形态）立项时提供差距基线——C1 的 scope = 把快照「烧成」运行时（G1/G2/G4 落地），G3/G6-G8 已合法无需返工。

## 证据索引

- mcp.json 真实产物：本会话 `export(ir, 'mcp')` 实测（P90+M90 techdoc，uri `nf://P90/P10/协议骨架-M90`）。
- 规范权威：`https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.ts`（本会话实抓 L386/L528/L550/L664/L723/L802/L895 实证）。
- 适配器源码：`desktop/src/core/mcp_adapter.py` L45-62（`_build_mcp_json`）+ 27 方案（诚实映射裁决）。
