# 30 · v2.1.0-B3-B 协议登记最后一公里：nf register 本地登记助手

> **需求提炼**：「查看roadmap继续执行」「挨个执行呗」——ROADMAP §7.5 B3 项标注「B（nf register 自动登记 CLI）遗留最后一公里：registry 投影现为『人工合并 projection 产物』半自动态（02 §9.2 登记自动化留后续）」。批准方案全文见 .rivet/plans/30-v2-1-0-b3-b-协议登记最后一公里-nf-register-本地登记助手.md。
> **非目标**：PR 机器人/自动登记流水线（02 §9.2「留后续」边界原样保留）；不改 02 协议 Schema/三要件语义；不改 registry_loader 只读消费；不自动改 02 文档（02 在册是前置条件，人先登记 02）。

## 核心

把 B3 自举闭环最后一段接通：`nf register` 本地登记助手把 29 方案 `protocol_projection` 产物**校验后合并**入 registry protocols[]，替代人工手抄投影。仍**人工触发**，登记内容源头仍是 02 文档 + protocol.yaml。

1. **02 §9.2 豁免子句**（文档真相源修订）：明确「在包已 §8 在册 + protocol.yaml 合规前提下，nf register --apply 同步派生投影 = 02 发起登记的机读落地，非独立内容变更」，与 PR 机器人/自动流水线划界——「直接改 json 不经 02 = 违约」句不适用于此受控路径。
2. **`desktop/src/core/registry_sync.py`**（纯函数层）：
   - `check_registerable(pkg_dir, doc)`：三要件校验——① protocol.yaml 在场可解析 + schema_version ∈ {1,2}；② 包在 02 §8 登记段（### 8. → ## 9. 区间）正文在册（领域包标题在册 8.1/8.2、通用/组合包段内登记实况 8.3/8.4——对齐 verify check15 ⑤ 可寻址语义）；③ registry 现状由调用方传入。返回 issues 列表（空=通过）。
   - `merge_protocols(reg, entries)`：id 命中更新（**保原键序**，避免 json.dump 重写污染既有条目键序）/ 缺失追加 / 多余不删（只增不删 V1 底线）/ 幂等。
3. **`scripts/nf.py` register 子命令**（IO 层）：`--check`（缺省，只读校验+diff 预览）/ `--apply`（校验过 → 合并写 registry protocols[] → 提示跑 verify 自证）。**幂等 = 无实质变化不写盘**（键序无关 canon 比较）。02 未在册 → exit 2。

## 验证

- registry_sync 单测 8 例（RED→GREEN）：02 未在册拒绝 / schema 非法拒绝 / 轻混在册通过 / projection==registry 锚点 / merge 更新/追加/不删/幂等。
- 集成自证（w4）：registry 漂移 M65→M66 → `--check` 检出且不写盘 → `--apply` 修复（尾项回 M65）→ `bash verify.sh` check14 ⑦ PASS=24。
- 全量回归：verify PASS=24 + 单测 177 绿（169 基线 + 8 新增）。
- e2e 不跑（CLI 辅助，不改 runtime 装配路径）。

## 文件

- 修订 `02_联动注册表.md`（§9.2 豁免子句）+ `scripts/nf.py`（register）
- 新增 `desktop/src/core/registry_sync.py` + `desktop/tests/test_registry_sync.py`
- 本真相源 + README 协议链补 30 + CHANGELOG [Unreleased] + ROADMAP §7.5 B3 项 B 段切 ✅

## 遗留

B3 自举闭环至此三段全通：向导生成 protocol.yaml → check14 门禁（C，29 方案元素级+glob）→ nf register 机读落地（本方案）。02 §9.2 的「PR 机器人 / 自动登记流水线」平台化边界原样保留。
