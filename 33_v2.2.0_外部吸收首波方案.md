# 33 · v2.2.0 外部吸收首波：verify 验证纵深（A1-A5）+ 方案模板纪律（B1-B2）

> **需求提炼**（用户裁决）：「接下来是 v2.2.0 以及以下需要做的 NarrativeForge 外部吸收清单总结（两批 28 项筛选后）…A 族：verify 验证纵深族…B 族：方案/流程模板族」→ 裁决「A 族 5 项 + B 族 2 项全做（v2.2.0 首波 = verify 深化 + 方案模板纪律；C/D 族记入 ROADMAP 条件池）」。批准方案全文见 .rivet/plans/33-v2-2-0-外部吸收首波-verify-验证纵深-a1-a5-方案模板纪律-b1-b2.md。
> **非目标**：C 族 7 项（MCP Server/安全层/Spec Registry/工作流/双模式/M3E/Agent 检索——绑三问①形态决策，记 ROADMAP §7.6 条件池）；D 族 5 项（AI 内容生成——触发条件冻结，同上）；已有等价物（文档先于代码由 verify 门禁承载、渐进式加载已由 Discovery/Activation 实现）。

## 核心

外部两批 28 项筛选收敛到同一方向：**verify 从「协议一致性」（check1-18）加深到「产物与语义完整性」**。四波落地：

1. **A1 导出产物 schema 合规**（Wave1，cc3106e）：新增 `desktop/src/core/export_schema.py`——5 导出格式（ccv3/skill/agents/claude/mcp）各建逐键 shape 校验函数（非完整 JSON Schema 引擎）+ fixture 对照；verify 新 check19（`test_export_schema`，合法产物 PASS、篡改 FAIL）。
2. **A3 文档完整性门禁**（Wave2，14e47dc）：verify 新 check20——模块文档硬性必填清单分层校验（官方 13 件强校验 machine_contract/元数据/职责章节；社区模块机读完备者同结构强校验，未完备存量 WARN 统计不阻断，对齐 check16 过渡策略）。
3. **A2 引用反查**（Wave2，c13fb95）：`retriever.referenced_by(module_id, path)` 反向引用查询（遍历 registry protocols[].references，支持裸号 M55 / 限定 id 情感:M55 双向命中）+ `nf who-refers <module_id>` CLI（--registry 可指路径）。
4. **A4 变更影响面**（Wave3，d0dc61b）：新增 `desktop/src/core/impact_check.py`——registry 引用图闭合门禁（直接以 registry.json 为起点，纯 JSON 无 PyYAML/community 目录依赖：① references.source_package 在册 ② module_id 裸号归一在源包在列 ③ 同包无裸号重复）+ `removal_impact` 变更前置影响面查询；verify 新 check21（补 check15 ① 的降级盲区——check15 依赖 protocol.yaml + 目录在场，registry 自洽性缺独立门禁）。
5. **A5 MCP 规范实证核查**（Wave3，181de51）：一次性差距报告落盘 `33_v2.2.0_A5-MCP规范差距核查报告.md`——mcp.json（27 方案产物）对照 MCP 规范 2025-11-25 schema.ts 实证：G1 形态级差距（静态快照 vs JSON-RPC 会话协议，非缺陷=27 非目标明示）+ G2 字段级（Resource.text 属 read 响应非 list 元数据）+ G3-G8 合法无返工；check19 边界声明（shape 门禁 ≠ wire 合规）；为 C1 MCP Server 立项提供差距基线。
6. **B1/B2 方案模板纪律**（Wave4，e68718b）：CONTRIBUTING 新增 §4.5——NN_ 方案模板必填两段：B1 五问自检（谁消费/何时验证被消费/与非目标边界/与现存协议关系/失败定义）+ B2 消费方声明（功能→谁消费→怎么验证被消费，空消费方不得入方案），防 C-a 无消费方能力重演。
7. **C/D 族**（af3d191 预落）：ROADMAP §7.6 C 族条件池 C1-C7（绑三问①+F2）+ D 族 D1-D5（触发条件冻结）——本版只落盘不实现。

## 验证

- verify.sh v2.9→v2.10（check19/20/21 入段 C）：PASS=29 全绿（check12 单测 199 OK 含 A2+6、check19 export_schema、check20 文档完整性、check21 impact_check 真源自洽 smoke）——基线 PASS=24 → 29。
- A2 CLI 冒烟：`nf who-refers M55` → 校园西幻轻混组合包 → 校园情感领域包.M55 只读；M91 → 无引用（registry 实测）。
- A4：impact_check 15 单测（隔离构造悬空/重复检出 + 真 registry smoke 空 issues）；check21 接入后 verify PASS 27→29。
- A5：真实 techdoc IR 导出 mcp.json 逐字段对照 schema.ts（L386/L528/L550/L664/L723/L802/L895 实证）。
- 全量：`bash verify.sh` PASS=29 全绿 + `python -m unittest discover -s desktop/tests` 199 OK。

## 文件

- 新增 `desktop/src/core/export_schema.py` + `desktop/tests/test_export_schema.py`（A1/check19）
- 修改 `desktop/src/core/retriever.py` + `scripts/nf.py` + `desktop/tests/test_retriever.py`（A2 +6 用例）
- 新增 `desktop/src/core/impact_check.py` + `desktop/tests/test_impact_check.py`（A4，15 用例）
- 修改 `verify.sh`（v2.9→v2.10：check19/check20/check21 + 头部/banner 配套）
- 新增 `33_v2.2.0_A5-MCP规范差距核查报告.md`（A5 一次性核查）
- 修改 `CONTRIBUTING.md`（§4.5 B1/B2 方案模板纪律）
- 修改 `ROADMAP.md`（§7.6 条件池，af3d191）+ README 协议链补 33 + CHANGELOG [Unreleased]

## 遗留

- verify 其余纵深（check22+ 候选）：协议文档示例/边界必填、registry 变更前置机读化（当前 impact_check 为静态门禁 + 库函数，nf 前置命令未建——removal_impact 已有库层）。
- A5 转 C1（MCP Server 运行时）时按差距报告 G1/G2/G4 落地；A2 消费方除 who-refers 外，GUI 市场视图检索联动（zone_g 检索已消费 retriever.search，referenced_by 未接 GUI）待 E4 深化。
- C/D 族记 ROADMAP §7.6 条件池，触发条件见该节。
