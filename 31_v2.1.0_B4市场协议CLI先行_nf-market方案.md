# 31 · v2.1.0-B4 市场协议 CLI 先行：nf market 依赖/冲突查询

> **需求提炼**：「B4 改为 CLI/库先行：nf.py 加 market 子命令（依赖解析/冲突仲裁做成 CLI 查询，消费 catalog——同 B2/B3 的 CLI 先行形态；不做 GUI）」。ROADMAP §7.5 B4「市场协议本体化」经用户裁决收敛为 CLI/库先行。批准方案全文见 .rivet/plans/31-v2-1-0-b4-市场协议-cli-先行-nf-market-依赖-冲突查询.md。
> **非目标**：GUI（zone_g 冻结移出）；包版本槽位（协议层 V2 另立项）；上架规则/市场目录视图（GUI 解冻后以本库接壳）；不改 verify.sh 门禁判据。

## 核心

把 verify check15 ②③ 判据（依赖闭包 + 挂载冲突）从 bash heredoc 提为**可 import 纯函数库**——登记前可查（门禁前移），GUI 解冻后即其库层。

1. `desktop/src/core/market_analyzer.py`：
   - `dependencies(pkg_id, prots, data)`：references → 源包 core_modules 闭包；源包不可读 / 叶越界官方 13 / 源包嵌套 references → issues。
   - `conflicts(pkg_id, prots, data)`：挂载层键归一后 default 交集非空即冲突。
2. `scripts/nf.py market <包目录>`：登记状态（复用 registry_sync 校验）+ 依赖闭包 + 冲突预检（信息查询，冲突不阻断 exit）。

## 过程发现（瑶光反证，verify 死检查缺陷）

verify.sh check15 ② 源包嵌套检查取 `deps.get('references')`（verify.sh L626 `deps = pkg2.get('dependencies', {})` 后取 references）——但 protocol.yaml 的 references 在 **package 层**（与 dependencies 平级，见 community/校园西幻轻混组合包/protocol.yaml L14 dependencies vs L18 references）→ **恒取不到，死检查**。market_analyzer 按 package 层真实层级修正；verify.sh 死检查记 ROADMAP 待修项（本方案不动门禁，避免打破 PASS=24 基线）。

## 验证

- market_analyzer 单测 7 例（RED→GREEN）：轻混依赖闭包={校园,西幻}零 issue / 校园空闭包 / 源包含 references 报嵌套 / 叶越界官方 13 报错 / 轻混与源包零挂载冲突 / 构造交集报冲突 / OFFICIAL13==13 件。
- CLI 集成：`nf market 轻混` → 登记在册 + 依赖={校园情感领域包,西幻生存领域包} + 无冲突；`nf market 校园` → 无跨包引用 + 无冲突。
- 全量回归：verify PASS=24 + 单测 184 绿（177+7）。e2e 不跑（不改 runtime 装配路径）。

## 文件

- 新增 `desktop/src/core/market_analyzer.py` + `desktop/tests/test_market_analyzer.py`
- 修改 `scripts/nf.py`（market 子命令）
- 本真相源 + README 协议链补 31 + CHANGELOG [Unreleased] + ROADMAP §7.5 B4 切 ✅（CLI 先行）+ verify 死检查记 ROADMAP 待修

## 遗留

- verify.sh check15 ② 源包嵌套 references 死检查（dependencies 层取恒空）——待修项（改 verify 会动 PASS 基线，独立小方案处理）。
- 包版本槽位（协议层 V2）+ 上架规则/市场目录视图——GUI 解冻后以 market_analyzer 为库层立项。
- B2 原文「组合运行时变体/条件组合 + 冲突仲裁报告」——独立 B2 深化，未做。
