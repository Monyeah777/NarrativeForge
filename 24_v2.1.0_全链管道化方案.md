# 24 · v2.1.0 全链管道化立项（B2：retrieve→compose→gate→export 单命令，L2 首个里程碑）

> **需求提炼**：「E4/E5 已摸到门——检索驱动、装载/安装都有了，只差把整链串成一条命令」；ROADMAP §7.5 锚定「B2 全链管道化 = 首个里程碑（CLI/库先行形态的地基）——建议 v2.1.0 首立项」。批准方案全文见 .rivet/plans/24-v2-1-0-全链管道化立项-b2-retrieve-compose-gate-export-单命令-cli-库先行形态.md。
> **非目标**：不做 GUI/APK（L3 冻结，L3_FROZEN.md）；不做新导出格式（A1 AGENTS/MCP 另行立项，exporter._REGISTRY 可插）；不改 E1-E5 模块行为语义；不做变体/条件组合（B2 上半段留后续）；不做协议向导登记一站式（B3）。

## 核心

新增 `desktop/src/core/pipeline.py`：`pipe(store, pipeline, selected, *, include_references=True, fmt='ccv3', dest_dir=None, fail_on_gate=True) -> PipeResult`——selected(full_id 列表，本地缺失项跳过入 warnings) → `composer.build_assembly`（E3 references 并入）→ `generator.render_ir`（装配→IR）→ `quality_gate.run_gate`（三态）→ gate.ok 且未禁阻断才 `exporter.export`（fmt 注册表透传；skill 拒 narrative 的矩阵纪律 warnings 原样带出）。可信任度不变量：fail 时 `ok=False`；`fail_on_gate=False` 强制导出诊断产物但 ok 仍 False。

新增 `scripts/nf.py` CLI（纯 stdlib argparse）：`python scripts/nf.py run --pipeline <P04.md> --modules 通用类:M00,轻混类:M91,轻混类:M92,通用类:M80 [--seed] [--fmt ccv3|skill] [--dest] [--force-export]`——打印 GateResult 摘要 + 产物路径；FAIL 且非 force → exit 1（CLI 层门禁镜像 verify 铁律）。

## 文件

- 新增 `desktop/src/core/pipeline.py` + `desktop/tests/test_pipeline.py`（5 用例：全链 ccv3 / skill 拒 narrative / 缺模块 warn / 空装配 fail 阻断 / force 导出）+ `scripts/nf.py`
- 本文件 24（方案真相源）+ README 协议链 + CHANGELOG [Unreleased] 补条（release 搁置不 tag）

## 验证

- `python -m unittest discover -s tests` 全绿（135→140：test_pipeline 5）
- `python scripts/nf.py run --pipeline community/校园西幻轻混组合包/pipelines/P04_轻混装配流管线.md --modules 通用类:M00,轻混类:M91,轻混类:M92,通用类:M80 --seed --fmt ccv3 --dest <tmp>` → 质量门 PASS + chara.json/world.json 在场（真实组合包全链实证）
- 空/缺失模块 → FAIL 阻断 exit 1；skill+narrative → 拒出 warnings（矩阵反例）
- `bash verify.sh` PASS=24 保持 + e2e PASSED（对照组，零回归）

## 分波

W1 core（pipeline + test，f25ea79）→ W2 CLI（nf.py，c59b11f）+ 文档收口。端壳不涉（L3 冻结）。全部验证后随 23 方案提交链推送（需用户确认 push）。
