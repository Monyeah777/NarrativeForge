# 15 · v1.3.0 Agentic 检索：四类对象可访问性层（2.0 E0-②）

> **需求提炼（用户原话）**：「Agentic检索（可访问性层）解决如何让导出产物可被检索和复用」；「2.0 依赖链：IR → 检索 → 质检 → 导出——没有 IR 检索没有统一结构可查；检索是模块市场/协议 GUI 化的基础，必须在它们之前完成」；「模块库/资产库/管线/协议四类检索入口」。
> **非目标**：不做向量库（规模几十~几百对象，结构化 grep 正确取舍，对齐 Agent Skills 生态 Discovery 元数据模式）；不做模块市场/协议 GUI 生成器（v2.0）；不改 IR（v1.2 已定）。

## 0. 定位

v1.2 IR 让"装配结果"有统一结构；本版让**已有资源（模块/资产/管线/协议）可被检索与复用**——装配动作基于已有资源、不从零开始（2.0 模块市场雏形与协议 GUI 化的前置）。

## 1. 问题全景

- **A1 模块/资产/管线/协议各自散在 Store / 缓存 / registry，无统一检索入口**——装配前"有哪些现成模块/资产可复用"靠人工翻。
- **A2 GUI 无检索消费**：模块库表格只能看全量，不能按关键词/类型快速定位复用它。
- **A3 检索结果形态不一**：无统一元数据卡片（Discovery 层）——消费方（装配/市场/质检引用）无从接入。

## 2. 改进收口

- **B1 统一检索接口（A1/A3）**：新增 `desktop/src/core/retriever.py`：

```python
@dataclass
class Hit:                                  # 元数据卡片（Discovery 层，轻量）
    kind: str        # module / asset_pack / pipeline / protocol
    ref: str         # full_id / pack_name / pipeline_id / protocol_id
    name: str
    tags: list       # 描述/标签（供 UI 展示与二次过滤）
    layer: str       # 仅 module（挂载层）
    content_snippet: str

def search(store, kind: str | None, query: str, *, limit=30) -> list[Hit]
# kind=None 跨四类；query 在 ref/name/tags/描述 上做大小写不敏感子串匹配
```

  四类源：module → `Store.list_modules()`；asset_pack → `Store.list_asset_packs()`（tags=键数）；pipeline → `store.load_cache("pipelines")`（无则空）；protocol → registry.json `protocols[]` + community `protocol.yaml` 投影（core_only/community 社区包名）。
  不命中任何源 → 空表。**结构化 grep、不上向量库**（对象量级小，全表线性扫描够快；每 Hit 只带元数据不带正文 → Discovery 轻量，全文按需在装配/GUI 选择时加载）。

- **B2 GUI 装配界面消费（A2）**：②模块库（zone_b）表格顶加检索输入框——输入即过滤当前模块表（ref/name/layer/描述命中显示，与全量浏览、勾选装配同一表格同一状态，零新增状态）。模块检索是最直接装配入口；资产/管线/协议检索入口随 v2.0 市场雏形 GUI 复用同一 search()。
- **B3 契约测试（A3 防回归）**：search 对真实 community 源（校园/西幻包 protocol.yaml + registry protocols[]）与临时 Store 命中断言。

## 3. 任务分解

- **T1 retriever.py + search()（无前置）**：四类源归一 + 子串匹配 + Hit 元数据卡片。单测钉：按 kind 过滤 / query 命中 ref-name-tags / 无结果空表 / protocol 源来自 registry+community。
- **T2 zone_b 检索框接入（依赖 T1）**：② 模块库表头加 QLineEdit，textChanged → 过滤表格（复用现有行重建/显隐逻辑），勾选/装配状态不受影响。
- **T3 回归验证（依赖 T2）**：全量单测 + e2e（官方13件装载后 search('module','M00') 命中）+ verify PASS=22。

## 4. 提交规划

- C0 = T1（retriever.py + 单测）
- C1 = T2 + T3（zone_b 接入 + 回归，含 e2e 检索断言扩展）
- 每批 verify 全绿方可推（阶段工作流：先推→方案→执行→完成推）。

## 5. 验证清单

- `python -m unittest discover -s tests`（82 + 新增全绿）
- `bash verify.sh` PASS=22
- e2e headless PASSED（检索断言并入）
- zone_b 检索框人工：输入关键词 → 表格过滤、勾选不丢（Windows GUI）

## 6. 瑶光反证

- **断言**：Store 无统一检索，模块经 `list_modules()` 全量——证据：v1 storage.py（本会话已读，module_dirs/list_modules API，无 search）。
- **断言**：管线在桌面侧经 cache 'pipelines' 缓存存在——证据：v1 main_window._reload_pipelines_impl save_cache/load_cache（同源 v0.10 已读 L118-129）。
- **待验证假设**：zone_b 表格结构支持行过滤显隐（v1 zone_b 行/列布局需读实——T2 前读 zone_b._build_ui/refresh 确认，不复现不改）。
- **待验证假设**：e2e 断言可扩展检索命中——e2e 装载官方13件后 Store 有模块（E2E PASSED 已证 15 件），search 命中可断言。

## 7. 范围外（留 v2.0）

- 协议/资产/管线的 GUI 检索视图（市场雏形一站式检索/装载/组合，E4）——search() 已就绪，UI 消费随 v2.0。
