# NarrativeForge · v0.6.0 协议中转站方案（落盘执行版）
> **版本定位**：v0.6.0「协议中转站——协议体系从人读规范文档升级为机读可驱动的通用协议层」
> **基线**：v0.5.0 已发布（tag v0.5.0=eac0fbd）+ v0.6.0 治理补漏前置批次（main=`5b63649`，工作区干净）
> **文档状态**：本文件为 v0.6.0 方案落盘真相源（I5）；取证时间 2026-09-04，基线 `5b63649`
> **编号声明**：方案按「问题 A → 改进 B → 任务 T」三层自洽编号。硬锚点与 ROADMAP §3 四做对应：A1 协议统一入口概念空白（缺口⑧）、A2 迁移机制 V1–V3 无落地（缺口⑥）、A3 sync 无 --check（缺口⑦）；B1–B3 一一对应；T1 协议统一入口 → T2 迁移机制落地（依赖 T1.1 的 bump 实战素材）→ T3 sync --check（独立并行）。四做之四「v0.5.0 治理缺口补漏」（ROADMAP/CHANGELOG/CONTRIBUTING/.github 模板/verify.sh check12）已随前置批次 commit `5b63649` 闭环且门禁全绿（PASS=14/WARN=0/FAIL=0），本文档不再展开任务化。
---
## 0. 定位与边界
- **做什么**：全部改动落在 **协议层机读化（统一入口）/ 迁移机制落地 / 同步工具链 --check** 三类。不新增题材内容、不改官方核心 13 件模块的职责语义、不动资产源文内容。
- **不做什么**：对外开放协议注册（= v0.7.0）；第三方模块组合装配（= v0.8.0）；Android 功能扩展与 APK 闪退修复（= v0.9.0）；端到端测试入 CI（范围外）。sync --check 仅做模式本体，接入 CI 归 v0.9.0。
- **护航不变式**：I1 三正交分离 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一；两级平台规则 R1（社区包禁互引）/ R2（类别独占）/ R3（装配契约）。**本版受动声明**：I5 由「文档层唯一」扩展为「文档层 + 机读投影双校验唯一」——02 文档仍为注册表真相源，registry.json 为其机读投影，新增门禁断言二者一致（见 B1/T1.1/check13）；其余不变式不受动。
- **改动范围**：01_核心协议.md（§7 迁移实操增补）、02_联动注册表.md（§9 机读投影 + registry_schema_version bump 记录）、desktop/src/core/（registry.json + registry_loader.py + 消费收敛点）、desktop/tests/（新增用例）、scripts/sync_android.sh（--check）、verify.sh（check13）、README（协议链追加 09）、CHANGELOG（[Unreleased] 回填）。
---
## 1. 问题全景（A1–A3，实测取证）
### A1 协议统一入口概念空白（缺口⑧）——双端各以文件系统约定消费协议层，注册表无机读驱动
- **证据**：
  - 代码层零注册表概念：`grep -rnE 'registry|register|schema_version|Registry' desktop/src/core/*.py desktop/src/ui/*.py` → **零命中**（空输出）。desktop 全代码不消费 02 注册表。
  - desktop 消费 03_管线库 = 用户手动选目录：`zone_c_pipeline.py:159`「选择管线库目录（03_管线库）」→ `discover_pipelines(path)` 扫 `*.md`；`pipeline_loader.py`（233 行）自带 YAML 子集回退解析器（无 PyYAML 环境时用内置缩进树解析固定子集），按文件系统约定推导管线/层/模块引用。
  - android 消费 = sync 拷贝 + 硬编码布局：`bootstrap.py:3-6`「seed 目录布局（由 scripts/sync_android.sh 从仓库根同步，.gitignore 生成物）：android/app/seed/03_管线库/*.md、04_模块库/<分类>/*.md、05_资产库/<包>/*.md」——双端各写各的路径约定与解析。
  - 注册表本身是人读文档：`02_联动注册表.md` 头部 registry_schema_version:"1"，§2 模块表 / §3 订阅 / §5 挂载点以 Markdown 表格与 YAML 代码块呈现；`06_Agent执行协议.md §2.2`「读注册表（02_联动注册表.md）」同样是人读指引。
- **根因**：v0.5.0 前协议层是纯文档体系，desktop/android 作为**文档消费者**各自实现读取；02 注册表从设计起未承诺机读契约。
- **影响**：同一协议体系三份并行消费实现（desktop loader / android bootstrap / 文档叙述）；schema 演进无代码级校验落点；v0.7.0「开放自定义协议注册」将面对每个第三方自研解析器、注册表登记无法被平台校验的困境——统一入口缺失是「协议开放三部曲」的地基断层。
### A2 迁移机制 V1–V3 只有规则无落地（缺口⑥）——结构性变更 bump 无模板/示例/校验
- **证据**：
  - `01_核心协议.md §7`：V1 只增不删 / V2 结构性变更 bump 并注明迁移说明 / V3 版本字段独立——**三行规则**，无实操指引。
  - `02_联动注册表.md` 头部：「读取旧版本注册表遵循 01 协议 §7 版本与兼容规则：只增不删、结构性变更 bump 并注明迁移」——宣言式，无模板可循、无战例可抄。
  - 全仓无任何迁移记录/示例文档；desktop/src 代码层 schema_version 零命中（见 A1）——版本字段只活在文档头部，代码不消费、门禁不校验。
- **根因**：v0.5.0 T2.3 从零引入版本字段时全部实体「首引即 v1，无历史文件需迁移」，迁移机制只写了规则没写实操。
- **影响**：v0.6.0 机读化一旦对注册表结构做演进（新增机器消费字段/重排）即触发 V2 bump 首次实战——但当前无模板、无示例、无校验，bump 全凭自觉；v0.7/v0.8 第三方协议与模块入场后迁移失控风险放大。
### A3 sync 无 --check、Android 一致性靠人工（缺口⑦）——双端真源漂移无自动闸
- **证据**：
  - `scripts/sync_android.sh`（1326B）全文：core 段 `rm -rf "$DST_CORE" + cp -r`、seed 段 `rm -rf "$DST_SEED" + cp -r`——**纯覆盖拷贝**，无差异比较模式、无 --check/--diff 参数、无「有差异」退出码语义。
  - android/app/{core,seed} 为 .gitignore 生成物不入库：本次实测 `diff -q desktop/src/core/{generator,models,parser,pipeline_loader,preset_manager,storage,validator}.py android/app/core/` 七文件全 OK——一致性是**当前时刻人工/构建保证**，非机制保证。
  - .github 两 workflow（build-android.yml/build-desktop.yml）只构建不测试、不跑一致性校验。
- **根因**：v0.2.0 起 android 以「同步拷贝」复用 desktop core，当时无差异校验需求；一致性长期依赖「记得跑 sync」。
- **影响**：desktop core 改动后漏 sync → Android APK 静默携带旧 core——v0.9.0 APK 闪退类问题的直接土壤；无 --check 则「是否已同步」无法在提交前自证。
---
## 2. 改进收口（B1–B3，与 A1–A3 对应）
| 编号 | 收口主题 | 对应问题 | 一句话收口 |
| --- | --- | --- | --- |
| B1 | 协议统一入口（协议中转站） | A1 | 02 注册表机读投影 registry.json + desktop Runtime 装载器 registry_loader.py，双端模块/挂载信息统一经机读 Schema 驱动，I5 扩展为文档 + 投影双校验唯一 |
| B2 | 迁移机制落地 | A2 | 01 §7 增迁移实操模板 + registry_schema_version 1→2 V2 bump 首例实战 + verify.sh check13 迁移完整性校验 |
| B3 | sync --check 模式 | A3 | sync_android.sh 增加 --check 只读差异校验分支（有差异 exit 1），双端一致性提交前可自证 |
---
## 3. 任务分解（T1–T3，依赖序）
```
T1 协议统一入口 ──→ T2 迁移机制落地
  （A1/B1，含机读      （A2/B2：T2.2 依赖 T1.1
     Schema bump 决策）   的 1→2 bump 实战素材）

T3 sync --check（A3/B3）——独立，与 T1/T2 全程并行
```
依赖：T1 无前置；T2 的 bump 首例依赖 T1.1（registry.json 结构演进触发 1→2）；T3 无前置。执行序建议 T1.1 →（T1.2 ∥ T2.1/T2.2）→ T1.3 → T2.3；T3 任意时刻可并行。
---
## 4. 任务明细
### T1 协议统一入口（A1/B1，v0.6.0 核心命题）
- **T1.1 registry.json 机读 Schema 落盘**
  - 动作 1：`02_联动注册表.md` 新增 §9「机读投影 registry.json」——声明 registry.json 为 §2 模块表 / §3 订阅 / §5 挂载点的机读投影；单一事实源仍为 02 文档（I5），定义文件路径、字段映射、同步纪律（改 02 必同步 json，反之亦然，由 check13 断言）。
  - 动作 2：落盘 `desktop/src/core/registry.json`——含 registry_schema_version（**bump 至 "2"**：机读化引入 loader 消费字段属结构性演进，触发 V2 bump 首次实战）、官方核心 13 件模块表（id/name/category/source/mount_point，与 02 §2 逐条一致，重号模块用限定 ID 如 通用:M10/事件:M22）、挂载点 P00–P80 定义（含 P40/P60 空层 optional 标记）、订阅关系（02 §3 核心内部固定订阅机读化）。
  - 动作 3：02 头部 registry_schema_version:"1"→"2"，附迁移记录（按 T2.1 模板）。
  - 验收：registry.json 在场且 version="2"；check13 PASS（json 模块表与 02 §2 逐条一致）。
- **T1.2 Runtime 装载器 registry_loader.py（desktop）**
  - 动作 1：新建 `desktop/src/core/registry_loader.py`：load_registry()→Registry（模块表/挂载点/订阅），API：list_core_modules / get_module(full_id) / layer_mounts(layer_id) / validate_assembly(module_ids)（对齐 02 §2 限定 ID 规则与 R3 装配契约）。
  - 动作 2：收敛点改造——zone_b_validate / zone_c_pipeline / zone_d_generate / zone_e_assets 中自行扫描模块、硬编码挂载处改经 registry_loader（先列收敛点清单再动，防范围蔓延）；03 管线文件读取仍走 pipeline_loader 不变（registry 管模块与挂载、pipeline_loader 管管线结构，两 loader 职责正交）。
  - 动作 3：`desktop/tests/test_registry_loader.py` 新增用例（json 解析/挂载点/装配校验/与 02 抽样一致性，≥10 用例）。
  - 验收：unittest 全绿（原 40 + 新增）；check12 自动收集新用例 PASS；desktop GUI 冒烟（zone_b/c/d 装配路径可用）。
- **T1.3 android 侧装载接入**
  - 动作 1：registry_loader.py 随 sync core 段（整目录拷贝）自动到达 android/app/core；bootstrap.py 模块登记改经 registry_loader（替代硬编码扫 seed 目录推导模块清单）；seed_version 幂等标记逻辑（force=False 跳过已导入、保护用户数据）不得破坏。
  - 动作 2：selftest_android.py 回归（先 sync 前置）覆盖 registry 装载路径。
  - 验收：android selftest 全绿；sync 后 android/app/core/registry.json 在场且与 desktop 一致（T3 --check 同时覆盖）。
### T2 迁移机制落地（A2/B2）
- **T2.1 迁移实操模板**：`01_核心协议.md §7` 增补「迁移实操四步」——①现状快照（记录被改实体当前版本与结构）→ ②bump 声明（按 V2 规则 bump 版本字段）→ ③迁移说明（逐字段记录变更/理由/兼容性影响）→ ④校验回读（verify.sh check 断言新旧版本可读）。附模板骨架代码块。
- **T2.2 V2 bump 首例实操**：registry_schema_version 1→2 全程按 T2.1 四步走通（02 header + §9 + registry.json），迁移说明即本版本首例战例。
- **T2.3 校验**：verify.sh 新增 check13（协议版本一致性 + 迁移完整性：bump 实体必有迁移记录；registry.json 与 02 的 version 字段一致），无条件执行入段 C。
- 验收：01 §7 模板与示例在场；02/registry.json 完成 1→2 bump 且迁移记录完整；check13 PASS。
### T3 sync_android.sh --check（A3/B3）
- 动作 1：sync_android.sh 增 --check 分支——不加参数行为不变（rm -rf + cp 覆盖）；加 --check 为只读差异模式：core 段 `diff -rq desktop/src/core android/app/core`、seed 段逐目录 find + diff；输出差异清单（新增/缺失/内容差异分列），有差异 exit 1、无差异 exit 0。
- 动作 2：边界处理——android/app/{core,seed} 缺失（新 clone 未 sync）时 --check 明确提示「生成物缺失，先跑 bash scripts/sync_android.sh」并 exit 1，不误报 diff。
- 动作 3：双态自测文档化——已 sync 工作树 --check exit 0；touch desktop/src/core 某文件后再跑 exit 1 且清单含该文件；还原后复绿。
- 验收：当前工作树（已 sync）--check exit 0；脏态模拟 exit 1 且清单正确；--check 全程只读无写操作。
- 注：--check 接入 CI 归 v0.9.0（本版交付模式本体）。
---
## 5. 提交规划
| 序 | 提交内容 | 前缀 | 关联 |
| --- | --- | --- | --- |
| C0 | 09 方案落盘 + README 协议链追加 09 + CHANGELOG [Unreleased] 回填 | docs: | 本文件 |
| C1 | T1.1 registry.json + 02 §9 + bump 1→2（含 T2.2 迁移记录） | feat: | A1/A2 |
| C2 | T1.2/T1.3 Runtime 装载器 + 收敛点改造 + unittest + android 接入 | feat: | A1 |
| C3 | T2.1/T2.3 01 §7 迁移模板 + verify.sh check13 | docs:/chore: | A2 |
| C4 | T3 sync_android.sh --check + 双态自测 | feat: | A3 |
| C5 | 全量 verify 绿 + tag 收口 + push | chore: | 收口 |
每任务独立 commit；C1–C4 期间工作区只允许该任务相关文件变更，避免交叉。C5 push 前须换新 PAT（旧 token 已暴露，见 §6 开放问题 7）。
---
## 6. 风险与开放问题
1. **registry.json 与 02 文档双源一致性（I5 受动）**：registry.json 为 02 的机读投影，check13 断言一致；直接改 json 不经 02 = 违约，CONTRIBUTING 三改动域细则需在协议层条目同步补「机读投影同步纪律」。
2. **bump 1→2 下游影响面**：registry_schema_version 被 02/07/06 及方案文档引用处需全仓 grep 检索同步；迁移记录若写错位置将导致 check13 误报——以 check13 为唯一仲裁，人工 grep 仅兜底。
3. **Runtime 装载器收敛范围蔓延**：zone_b/c/d/e 收敛点先列清单评审；03 管线读取保持 pipeline_loader 职责正交；禁止顺手重构无关代码（本版只做入口收敛）。
4. **android bootstrap 改动风险**：seed_version 幂等标记是 APK 用户数据保护关键（force=False 跳过已导入），接入 registry_loader 时不得破坏；android 侧真机冒烟属 v0.9 范围，本版以 selftest_android.py 为验收上限。
5. **sync --check 与生成物缺失态**：新 clone/android 目录重建时 --check 必须先提示 sync，勿让缺失态污染 diff 语义。
6. **APK 闪退问题边界**：属 v0.9.0（Android 同步门禁 + APK 闪退修复），本版不处理；若收口回归中 sync --check 暴露 core 漂移，记录为 v0.9 前置证据。
7. **推送安全（操作层）**：历史 PAT token 已在早期终端明文出现，C5 push 前须向用户索取新 token 并 revoke 旧 token；C0–C4 本地 commit 不受影响。
8. **端到端断言缺口（桌面）**：不在本版（ROADMAP §3 四不做），留待另行立项。
