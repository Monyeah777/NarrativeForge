# 25 · v2.1.0-A1 AGENTS/CLAUDE 适配器 + SKILL 边界裁决规则机制化（A 线第三格）

> **需求提炼**：「AGENTS vs SKILL 边界裁决——不是再写一个适配器，而是写一条语义分类规则进方案文档……两判据：复用性（脱离仓库仍有意义→SKILL / 绑仓库→AGENTS）+ 装载（常驻→AGENTS / 按需→SKILL）」。批准方案全文见 .rivet/plans/25-v2-1-0-a1-agents-claude-适配器-skill-边界裁决规则机制化-a-线第三格.md。
> **非目标**：不做 narrative→AGENTS（矩阵纪律拒出同 skill）；不改 SKILL（已交付）；不做 MCP（A2）；不改 IR 结构（语义信号走 meta/title 启发，宁薄勿厚）。

## 核心

裁决规则机制化为 `desktop/src/core/semantics.py`（**唯一真源**）：`classify_doc_semantics(ir)` → project_rules/skill——meta['doc_semantics'] 显式声明 > title/模块名项目约定词启发 > 缺省回退 skill。三处共用：AGENTS 出口适配器、protocol_wizard.self_check 内置校验、pipeline/nf CLI。

`desktop/src/core/agent_rules_adapter.py`：`export_agents`（techdoc+project_rules → AGENTS.md，章节按 IR 层/模块，正文=模块内容，格式锚点对齐仓库根 AGENTS.md）+ `export_claude`（CLAUDE.md 同内容变体）；注册 exporter._REGISTRY['agents']/['claude']。非 project_rules（narrative/能力语义 techdoc）→ 拒出 warnings 0 文件（矩阵纪律，同 skill 拒 narrative）。

## 文件

- 新增 `desktop/src/core/semantics.py` + `agent_rules_adapter.py` + `tests/test_semantics.py`（5 用例）+ `tests/test_agent_rules_adapter.py`（4 用例）
- 修改 `desktop/src/core/exporter.py`（注册 agents/claude）+ `protocol_wizard.py`（self_check 校验 doc_semantics 值域）+ `scripts/nf.py`（--fmt 扩 agents/claude）
- 本文件 25（方案真相源）+ README 协议链 + CHANGELOG [Unreleased]

## 验证

- `python -m unittest discover -s tests` 全绿（140→149：semantics 5 + agents 4）
- 实测：project_rules IR → export agents 产 AGENTS.md（含 verify 正文，首章 # Agent Operating Rules）/ claude 产 CLAUDE.md；narrative/能力语义 → 拒出 0 文件
- `bash verify.sh` PASS=24 + e2e PASSED（零回归）

## 分波

W1 core（semantics + adapter + exporter 注册 + 测试，74db425）→ W2 集成（self_check + nf.py --fmt，98ed908）+ 文档收口。GUI 不涉（L2 能力，nf/库出口）。
