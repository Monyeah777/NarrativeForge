# 41 波C W2 · C3/C4 实测记录（2026-09-08 · 本工作台实测）

> 对应 `41_v2.8.0_波C质量编译深化规划.md` C3/C4 验收；载体 = `desktop/src/core/semantic_conflict.py`（check26）+ `market_analyzer.related_of` / `nf related`。

## C3 语义矛盾扫描：扫出并修复 3 例真实矛盾

规则：R1 techdoc 链（M90/M97/M98）machine_contract 订阅事件无发布方断链；R2 头挂载点 vs contract layer 漂移；R3 头类别 vs contract category 漂移。

扫描实证（全库 44 模块 · techdoc 链 3 · 发布事件方 13）扫出 **3 例真实事件契约断链**并修复（均为「订阅无发布方」的语义级矛盾，check15/21 结构门禁不拦）：

| 矛盾 | 修复 |
|---|---|
| M90 订阅 `intent_received` 无发布方 | 官方 M50（主循环/调度器）machine_contract `publish` 补 `intent_received`（P90 装载输入闭环） |
| M98 订阅 `doc_delta_committed` 无发布方 | 官方 M90 machine_contract `publish` 补 `doc_delta_committed`（与其职责文本自洽） |
| M97 订阅 `term_conflict_detected` 无发布方 | 社区 M97 machine_contract `publish` 补 `term_conflict_detected`（自检检测即广播） |

修复后复扫：**零 issue**（挂载点/类别漂移规则亦全库零命中）。单测 `test_semantic_conflict` 3 例（断链检出 / 补发布方后清除 / 漂移检出）。

## C4 图书馆 See-Also：`nf related` 实测

命令：`python scripts/nf.py related 技术文档域包` / `python scripts/nf.py related M90` / `python scripts/nf.py related 官方核心基础包`

- `related 技术文档域包` → 关联条目：官方核心；相关模块互见：`M00、M90`（techdoc 依赖官方核心链）。
- `related M90` → 反向引用方：技术文档域包；相关模块互见：`M00、M97、M98`（官方核心 ↔ techdoc 互见）。
- 人读化基于 registry protocols[] + 模块级 machine_contract inputs 依赖图，references/who-refers 机读基底未改。

## 门禁与回归

- `bash verify.sh`：verify.sh v2.14→v2.15，check1-26 **PASS=39 / WARN=0 / FAIL=0**（check26 新增 2 PASS，37→39）。
- 单测：desktop/tests 全量 discover **396 OK**（+test_semantic_conflict 3 + test_related_of 3）。
