# 41 波C W5 实测记录（2026-09-08 · 本工作台实测）

> 对应 41 规划 D 组 D3（lint）与 D4（日志级别体系）。

## D3 ruff 语法级静态检查

- 交付：`ruff.toml`（target py311；`[lint] select = E9/F63/F7/F82`，排除 docs/community/.rivet/fixtures）+ `.github/workflows/lint.yml` 独立 job（ubuntu + setup-python + pip install ruff → `ruff check desktop/src scripts`；不碰 verify.sh 零依赖红线）。
- 实测：本机 ruff 0.16.6 跑 `ruff check desktop/src scripts --select E9,F63,F7,F82` **全绿**；顺带修复 1 例真问题——`market_analyzer.py` 用了未导入的 `Optional`（F821，已补 typing import）。
- 选择依据：语法级 + 未定义名保守集，避免全库风格规则把 CI 拉到与内容波无关的噪音。

## D4 日志级别体系

- 交付：`desktop/src/core/plog.py`——`get_logger(name)` 惰性初始化 logging（默认 INFO，handler 绑 **stderr**），诊断绝不污染 stdout（MCP stdio 协议面只写 JSON-RPC）；`mcp_runtime.main` 接入启动/退出日志。
- 设计边界：CLI 用户面 print（结果/产物路径）为输出协议保留（不翻 logging），既有 stdout 契约不变——D4 落在「诊断走 logging、级别可调」。

## 回归

- `python -m py_compile` 通过；ruff 全绿；单测 406 保持 OK；`bash verify.sh` check1-26 PASS=39 不破（W5 为配置 + 工具层新增，未动协议/门禁/模块头）。
