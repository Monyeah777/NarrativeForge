# 41 波C C7 实测记录（2026-09-08 · 本机 stdio 冒烟）

> 对应 41 规划 C7（agent 工具面扩展：MCP tools/prompts 只读）；载体 = `desktop/src/core/mcp_runtime.py`。

## 交付

- **tools（4 个只读检索工具，inputSchema 真实存在）**：`library_search`（仓库侧知识库检索，docs + community README + 编号方案文档）/ `registry_query`（registry 模块+协议子串查询）/ `pipeline_ls`（03 + community pipelines 清单）/ `spec_ls`（registry protocols 协议包清单）。
- **prompts（1 条装载引导）**：`assemble_guide`（取件顺序 07→02→01→06 + 包 README 装载 + 自检纪律 + verify）。
- **握手**：initialize capabilities 现含 `resources`/`tools`/`prompts`（全只读）。
- 数据源 = 仓库只读扫描（白名单目录：03_管线库 / 04_模块库 / community/* / docs / registry.json）；**无写路径工具**，未知工具 → `-32602`，未知方法 → `-32601`（C2 只读纪律保持）。

## 实测（真实 stdio 会话 · 换行分隔 JSON-RPC）

用 `python -m core.mcp_runtime <快照>` 起服务、按标准协议逐行请求，冒烟结果：

- `initialize` → capabilities = `prompts / resources / tools`；
- `tools/list` → 4 工具全在；
- `tools/call pipeline_ls` → 返回 8 条管线；
- `prompts/get assemble_guide` → 引导文本含「装配师」；
- 进程 exit 0。

## 验收口径

- 「标准 MCP 客户端可调 ≥2 tools + 1 prompt」的**协议面**已由本机 stdio 冒烟验证（≥3 tools + 1 prompt 可调）；
- **外部标准 MCP 客户端（E3：Claude Desktop 类）实测仍待回填**——回填前不宣称「转正」，见 docs/mcp.md E3 占位。

## 回归

- desktop/tests 全量 discover 401→**406 OK**（test_mcp_runtime 12→17：+initialize caps + tools/list + prompts/list + registry_query + 未知工具拒绝 + prompts/get）。
- `bash verify.sh` check1-26 PASS=39 复核不破（C7 为纯代码层新增，未动协议/门禁）。
