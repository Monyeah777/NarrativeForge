# 41 波C W3 实测记录（2026-09-08 · 本工作台实测）

> 对应 41 规划 C6（部分）/C8/C9 + D 组 D2/D5/D6；全部为可本地闭环的文档/测试交付，verify 不破（PASS 39 复核）。

## C6 需求收敛衔接（部分）

- 交付：`docs/需求收敛模板.md`——8 字段澄清稿模板（一句话需求 / 背景痛点 / 消费方 / 范围边界 / 可测验收 / 风险未知 / 涉及文件 / 素材引用）+ 自动流转链（澄清稿 → `nf design steelman init` → `nf design audit init` → 草案留档）。
- 首例挂账：NF-FIELD-001（Y5 回填）回执原件不在仓库，素材到后按模板走通全链并沉淀 D7 fixtures。

## C8 MCP transport 扩展评估（结论：暂不做，回冻结）

- 交付：`docs/41_波C_C8_transport评估.md`——stdio 现状 + streamable HTTP/SSE 差距评估（服务形态/安全面/能力收益/运维）+ 试点结论。
- 依据：无真实远程消费方（E3 未回填）+ 网络暴露安全面 + stdio 已覆盖本机 agent 场景；回冻结条件 = E3 回填且出现真实远程装载需求。

## C9 回合装载指针 v0

- 交付：`docs/41_波C_C9_回合装载指针_v0.md`——指针格式（常驻协议要点/热区本层契约与场景资产/冷区键引用现取）+ P03 西幻第 5 回合样例。
- 纪律核验：01/02 契约字段零改动；样例可复现（随包文档在册即可逐项核对键）；verify 不破。

## D 组（本轮部分）

- D2 CLI 层自动化测试：`desktop/tests/test_nf_cli.py` 5 例（子命令面暴露 sig/diff/explain/related/pipeline/module/demo + sig --verify / explain 25 / market --list / related 技术文档域包 smoke），全过。
- D5 迁移指南：`docs/迁移指南-基于nf-sig-diff.md`（nf sig/diff/explain 的迁移核查入口，链接 check13 与 CHANGELOG 归档）。
- D6 Windows 说明：README 核心逻辑层补「Windows 用 Git Bash / WSL 跑 `bash`」。

## 门禁与回归

- 单测：desktop/tests 全量 discover 396→**401 OK**（test_nf_cli +5）。
- `bash verify.sh`：check1-26 **PASS=39 / WARN=0 / FAIL=0** 复核不破（纯文档 + 测试新增，无协议/门禁/模块头改动）。
