# 41 波C C1/C2 实测记录（2026-09-08 · 本工作台实测）

> 对应 41 波C 质量编译深化 C1/C2 验收（规划已按作者裁决内部消化）；实施载体 = `desktop/src/core/knowledge_sig.py` + `nf sig/diff` + verify.sh check25（v2.14）。

## C1 `nf sig`：试点 3 版本 + 全量 01-36 可复现

试点版本（§4 执行默认值 01 最早 / 24 中间 / 36 最新）签名摘要（digest 前 12 位，可复现）：

| 文档 | kind | doc_id | title | refs |
|---|---|---|---|---|
| 01_核心协议.md | doc | 01 | 核心协议 | 19 |
| 24_v2.1.0_全链管道化方案.md | doc | 24 | 24 · v2.1.0 全链管道化立项（B2：retrieve→compose→gate→export 单命令，L2 首个里程碑） | 5 |
| 36_v2.5.0_基础层深化续方案.md | doc | 36 | 36 · v2.5.0 基础层深化续：CCV3 资产条目 id 修复 + 34 方案文档偏差收口 | 0 |

全量扫描：根目录 01-36 编号方案文档 **35 份**，两遍生成逐字节一致（`nf sig --verify`：文档 35 · 可复现 1）。签名字段 = kind/path/doc_id/title/version/self_id/layer/category/headings/schema_names/refs/meta_line_count/line_count/body_hash。

## C2 `nf diff`：01 vs 36 实测差异清单

命令：`python scripts/nf.py diff 01_核心协议.md 36_v2.5.0_基础层深化续方案.md`

- 判定：**破坏（文档编号变更）**（01 → 36）。
- 代表性字段差异：doc_id `01→36`；version `` → `v2.5.0`；heading_count 14→5；line_count 274→27；body_hash 全变。
- 代表性引用/章节差异：01 特有标题（模块协议/管线规范/资产库接口/联动注册表/五条核心不变式等）在 36 移除；36 为单点修复方案文档（章节集小）。

规则（自含文档化）：doc_id 变更 = 破坏；存在引用/章节移除 = 需评审；仅版本演进 = 兼容。

## 门禁与回归

- `bash verify.sh`：verify.sh v2.13→v2.14，check1-25 **PASS=37 / WARN=0 / FAIL=0**（check25 新增 2 PASS，35→37）。
- 单测：desktop/tests 全量 discover **390 OK**（含 test_knowledge_sig 5 例）。
- `nf explain check25` / `nf explain all` 冒烟通过。
