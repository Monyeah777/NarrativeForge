# 42 M1 · P03 西幻生存流 · 内部执行演练集（第 1 份）
> ⛔ 操作指令：阅读即执行——本文含可直接执行的命令/步骤/判定，勿当资料阅读。
> 最后更新：2026-09-08

> 对应 M1 验收「P03 管线 1 份演练集可复现执行」；样本载体 = `desktop/tests/fixtures/execution/p03_drill_cases.json`。

## 一、可复现执行

```bash
PYTHONPATH=desktop/src python -m core.execution_drill desktop/tests/fixtures/execution/p03_drill_cases.json
```

结果（2026-09-08 实测）：8 例全过——失范 4/4 捕获（browse_repeat / fabricated_id / no_citation / semantic_misalignment），guard 3/3 无误报。

## 二、演练案例清单

| # | 样本 | 类型 | 期望 |
|---|---|---|---|
| 1 | P03-pos-advance | 正例：引用 06 §3 + 真实编号 + 职责自洽 + 推进信号 | 无捕获 |
| 2 | P03-dev-browse-repeat | 失范：浏览-复述（原文复制、无推进） | browse_repeat |
| 3 | P03-pos-browse-progress | 变异对照：复制 + 推进信号 | 无捕获 |
| 4 | P03-dev-fabricated-id | 失范：编造编号 M99（非 P03 装配集） | fabricated_id |
| 5 | P03-dev-no-citation | 失范：决策句无引用（凭印象） | no_citation |
| 6 | P03-pos-cited-decision | 变异对照：决策句带 06 §3 引用 | 无捕获 |
| 7 | P03-dev-semantic-misalign | 失范：M00 被描述为时间推进（职责属 生存:M10） | semantic_misalignment |
| 8 | P03-pos-semantic-aligned | 变异对照：M00 数据槽记录（职责自洽） | 无捕获 |

## 三、五维自评（42 §4.2）

- ① 静态可核验：+0（未新增 check）；演练经 unittest discover 常驻（静态覆盖）。
- ② 动态可执行：**本维主推进**——把「浏览-复述/编号语义错位/无引用/编造编号」四类失范变为可机检硬断言（最大鸿沟的第一步内部自测化）。
- ③ 架构纯度：+0（纯新增库/夹具，未改协议层）。
- ④ 资产密度：fixtures 反例样本 = 新的测试资产（来源标注 inferred；field001 预留）。
- ⑤ 文档可执行性：本演练集 = 指令类文档（可复现命令在首节），非资料。

## 四、边界与遗留

- 样本当前为推断构造（据已观察失范类别），**NF-FIELD-001 回执原件入仓后按 field001 增补/替换**——届时把 8 条失范明细逐条固化为硬断言并更新捕获率基线。
- **M2 已衔接（2026-09-08）**：本集断言分级已与协议判级器（06 §11，必须/应/可）对齐；带状态头执行 = 每回合前置 `【回合状态头 v1】`（`desktop/src/core/round_header.py`），「引用后执行」（L1）即 `no_citation` 硬断言所在；中断重入一致性由 `test_round_header.replay_identity` 常驻断言。
- **带状态头执行验收口径（2026-09-08 audit 注）**：状态头 = 执行上下文，**不进入被检文本**（防引用遮蔽）；捕获按 agent 输出独立判定。硬断言 ↔ 判级映射常量 `execution_drill.RULE_LAW`：no_citation↔L1（必须）/ fabricated_id↔L2（禁止）/ browse_repeat↔L3（禁止）/ semantic_misalignment↔L4（必须）——单测 `test_state_header_acceptance_rule_law_mapping` 常驻断言无越级（软级/自由级不上硬断言）。
