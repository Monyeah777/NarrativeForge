# 42 M5 · 工程质量面补齐（收口记录 · 2026-09-08）

> 42 最后里程碑 M5（5.1-5.6）。5.3 已于 M3 check27 R4 收编；本轮落 5.1/5.2/5.4/5.5/5.6。

## 交付明细

- **5.1 覆盖率量化**：`coverage` CI 独立 job（`.github/workflows/coverage.yml`）——desktop/src/core **≥80%**；本地实测 **85%**（4065 stmt / 605 miss）；本地摘要入口 `scripts/coverage_summary.sh`（不碰 verify 零依赖红线）。
- **5.2 版本↔方案↔能力映射矩阵**：`VERSION-MATRIX.md`（机器可读四列）+ `test_version_matrix`（行格式/方案文件存在性常驻校验）。
- **5.3 错误信息审计**：已在 M3 check27 R4（raise 消息修复指引 ast 审计，零缺失）。
- **5.4 本地一键聚合**：`scripts/quality_all.sh` = verify + e2e（headless core 直驱）；bench 属 L3 冻结移出清单（L3_FROZEN），壳波接回时随接线账恢复——见设计边界。
- **5.5 设计自查清单**：`docs/42_M5_设计自查清单.md`（SOLID/YAGNI/DRY/单一真相源 NF 化六问）。
- **5.6 Release checklist + golden master**：`docs/42_M5_Release-checklist.md` + `scripts/release_freeze.sh <tag>`（tag 点 sha256.manifest + nf-sig 指纹双存）。
- **三闸门本地一键（42 audit 行动建议④落地）**：`scripts/gates_all.sh` = ruff lint（E9/F63/F7/F82）+ coverage（core ≥80%）+ verify（check1-27 PASS=41）；lint/coverage 本地缺失时提示并由 CI 对应 job 兜底。

## 五维总评（42 收口）

- ① 静态可核验：check1-27 PASS=41 全绿 + 覆盖率 CI（core 85%）+ doc_hygiene/version-matrix 常驻；
- ② 动态可执行：M1 演练捕获器 + M2 回合状态头/判级器（失范可机检、状态可见化）；
- ③ 架构纯度：M3 check27 制度化（收口必跑）；
- ④ 资产密度：M4 评估在档（无应淘汰低密度键）；
- ⑤ 文档可执行性：M4 指令/资料分类 + 内容边界 + Release checklist 就绪。

## 遗留

- 覆盖率门槛 CI 待首次 push 后线上核验；VERSION-MATRIX 需随每次发布/方案落库同步（常驻测试兜底）。
- bench 本地入口待壳波接回（L3 冻结契约）；外部实证（E3/B1/NF-FIELD-001）与 tag 决策仍挂账，与 42 纯内部收口解耦。
