# 42 M1 · P06 技术文档题材装配流 · 内部执行演练集（第 2 份）

> M1「给每条管线写演练集」多管线扩展第 2 份；样本载体 = `desktop/tests/fixtures/execution/p06_drill_cases.json`（techdoc 链：M90/M97/M98 + 官方 M00/M80）。

## 一、可复现执行

```bash
PYTHONPATH=desktop/src python -m core.execution_drill desktop/tests/fixtures/execution/p06_drill_cases.json
```

结果（2026-09-08 实测）：8 例全过——失范 4/4 捕获（browse_repeat / fabricated_id / no_citation / semantic_misalignment），guard 3/3 无误报。

## 二、案例清单

| # | 样本 | 类型 |
|---|---|---|
| 1 | P06-pos-advance | 正例（引用 + 职责自洽 + 推进） |
| 2 | P06-dev-browse-repeat | 失范：浏览-复述 |
| 3 | P06-pos-browse-progress | 变异对照（复制 + 推进信号） |
| 4 | P06-dev-fabricated-id | 失范：编造编号 M99 |
| 5 | P06-dev-no-citation | 失范：决策句无引用 |
| 6 | P06-pos-cited-decision | 变异对照（带 § 引用） |
| 7 | P06-dev-semantic-misalign | 失范：M98 被描述为维护术语表（职责属 M97） |
| 8 | P06-pos-semantic-aligned | 变异对照（职责自洽） |

## 三、口径与纪律

- 语义词区分「模块功能词」与「模块显示名」（显示名作标签不作判据，防错位检测失效）——见 fixtures `semantics` 注释。
- 测试 `test_execution_drill` 现参数化覆盖 P03 + P06 两套（捕获无漏报 / guard 无误报 / 捕获率 ≥2/3 逐管线）。
- 样本为推断构造（inferred），NF-FIELD-001 回执原件到位后统一 field001 增补。
