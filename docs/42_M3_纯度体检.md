# 42 M3 · 架构纯度体检（check27 制度化）
> ⛔ 操作指令：阅读即执行——本文含可直接执行的命令/步骤/判定，勿当资料阅读。
> 最后更新：2026-09-08

> 对应 42 质量纵深工程 M3（规划已内部消化）；载体 = `desktop/src/core/purity_scan.py`（verify check27，v2.16）。

## 一、四规则（首批文档级 grep 断言族）

| 规则 | 检查 | 作用域 |
|---|---|---|
| R1 端壳/APK 残留 | android / APK / src/ui / Kivy 词 | 01/02 协议真相源（裁决 #16 移除后残留） |
| R2 私货/可变物 | 绝对盘符路径 / /tmp|/Users|/home / TODO-FIXME-XXX-TBD | 01/02/06/07 协议层文档 |
| R3 重复标题 | 同文档重复章节标题 | 01/02/06/07 |
| R4 错误信息即微型文档 | core 全部 raise 消息须含修复/出处指引（ast 审计） | desktop/src/core/*.py |

## 二、首轮实扫结果（2026-09-08）

- **R1 修复**：02 联动注册表清除 2 处旧 android 表述（投影声明 / §9.1 现状快照）。
- **R4 修复**：9 处 raise 消息补修复指引（asset_ledger×2 / exporter / import_adapter×3 / parser×2 / storage），错误信息审计零缺失。
- R2 / R3 首轮零命中。

## 三、check27 变异自检（check 的 check）

`test_purity_scan` 5 例：对四规则各注入典型违规样本（变异注入），断言可被捕获 + 干净树零误报。

## 四、门禁与回归

- verify.sh v2.15→v2.16，check1-27 入库，PASS 39→41；单测 412→417 全绿。
- 体检结果随波收口入 audit（docs_audit 格式已有）；连续 2 波零命中即常态水位确认。

## 五、五维自评

① 静态可核验：**主推进**（纯度为机检规则，R1-R4 无「人肉抽查区」）；② 动态可执行 +0；③ 架构纯度：**主推进**（制度化扫描，收口前置）；④ 资产密度 +0；⑤ 文档可执行性：本档 = 指令类（收口必跑），非资料。

## 六、R5 / R6 与自洽面（2026-09-21 补记）

后续两规则已入同一通道（`check27`，不新增 check 序号）：

| 规则 | 检查 | 作用域 |
|---|---|---|
| R5 import 面越界 | 第三方 import 须登记（`HARD_ALLOW` / `SOFT_IMPORTS` + 软导入守卫）；残留走 `IMPORT_RESIDUE` WARN | `desktop/src/core/*.py` + `scripts/*.py` |
| R6 危险 sink 面 | `eval` / `exec` / `os.system` / `subprocess(shell=True)` / `pickle` / `marshal` / `yaml.load`；放行须在 `SINK_ALLOW` 写理由 | 同上（AST 级） |

**2026-09-21 增补（外部标准吸收）**：R6 的**登记表自身**也进了判据——每个 sink 类目须带
**CWE 对齐**（`CWE-78` shell 注入 / `CWE-95` 动态执行 / `CWE-470` 动态导入 / `CWE-502` 不安全反序列化），
且 `SINK_ALLOW` 的每个放行键必须指向**已登记 sink**（放行不得凭空出现）。理由：缺陷类型编码是
跨工具对账的公共键——外部扫描器报 `CWE-502` 时，NF 侧须能一眼对上自己的哪条约定。
