# 42 M2 · 回合状态头 v1 + 判级器 + 中断重入（协议可执行性改造）
> ⛔ 操作指令：阅读即执行——本文含可直接执行的命令/步骤/判定，勿当资料阅读。
> 最后更新：2026-09-08

> 对应 `42_顶尖质量纵深工程规划.md` M2；载体 = `desktop/src/core/round_header.py` + 06 §11 + 本档（与 C9 回合装载指针合并为运行态实例）。

## 一、判级器（RFC2119/8174 NF 化 · 落档 06 §11.1）

| 强度词 | 级别 | 违反判定 |
|---|---|---|
| 必须 / 禁止 / 不得 | 硬约束 | 结构事故：回滚 + 记失范；演练 = 硬断言 FAIL |
| 应 | 默认行为 | WARN + 可说明性豁免；演练 = 软断言 WARN |
| 可 | 自由区 | 无判定；自由区不得上硬断言 |

判级语义与 verify 判定 / M1 演练断言分级一一对应；未标强度词条款按「可」处理（防把资料当指令）。

## 二、回合状态头格式（= C9 指针运行态实例）

```text
【回合状态头 v1】管线：<管线路径>｜已执行段：<已跑段>｜本回合：<编号>｜本回合铁律：<L1-L5>（判级器见 06 §11）
```

- 铁律集（`round_header.LAWS`）：L1 引用后执行（必须）/ L2 禁止编造编号 / L3 禁止浏览-复述 / L4 职责自洽（必须）/ L5 可说明性豁免（应）。
- C9 静态装配单 → 本 v1 = 运行时注入格式 + 恢复语义（中断重入底座）。

## 三、P03 第 5 回合状态头实例（可复现）

```text
【回合状态头 v1】管线：community/西幻生存领域包/pipelines/P03_西幻生存流管线.md｜已执行段：P10 装载、P20 状态装载、P30 事件生产｜本回合：5｜本回合铁律：L1、L2、L3、L4、L5（判级器见 06 §11）
```

生成/断言：`python -m unittest desktop.tests.test_round_header`（幂等 + 中断重入切片一致）。

## 四、引用后执行 + 中断重入验收

- 引用后执行：关键决策先引用再输出；无引用 = 硬断言 `no_citation`（execution_drill R3，P03 演练集已含）。
- 中断重入断言 PASS：`replay_identity(interrupt_at=2)`——重入后序列与连续执行同一切片逐字节一致；同输入重跑幂等。
- P03 演练集断言分级与判级器对齐：硬断言（四规则）≤ 必须/禁止语义，自由区未硬编码。

## 五、五维自评

- ① 静态可核验：+0 check（演练经 unittest 常驻）；② 动态可执行：**主推进**——每回合协议约束可见化 + 引用后执行 + 重入底座；③ 架构纯度：协议正文仅 06 增 §11 判级器/状态头（01 Schema 零改动）；④ 资产密度：+0；⑤ 文档可执行性：判级器 = 指令类（操作即判定），非资料。

## 六、遗留

- 判级器段落档 06（执行层）；后续如需 01 正文强度词普查归 M4（文档可执行性）+ M3 check27 收编。
