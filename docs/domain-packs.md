# AI 品类域包（domain packs）
> ⛔ 操作指令：阅读即执行——本文含可直接执行的命令与判定，勿当资料阅读。
> 最后更新：2026-09-24

## 是什么

NF 的「域包」= 一个内容域的**口径协议 + 可机验产出 + 装载管线**。本工程按 AI 品类清单
（100 大类 / 1200 细分）逐类建包：每个包把该域的 **12 条细分**写成可判定口径，
并产出**可复算的数**与**可机验的形态面**——不是资料页。

每包固定 16 件生成物：

| 件 | 内容 | 机检档位 |
|---|---|---|
| `protocol.yaml` | 登记三要件①（机读真相） | — |
| `README.md` | 人读速览（含包名 / 管线 / 资产数，双源一致） | — |
| `modules/<码>a_*.md` `modules/<码>b_*.md` | P40 口径登记 + P60 收口派生（机读契约 + 事件载荷） | L2 |
| `pipelines/Pxx_*.md` | 派生自 P00 骨架的九层装配流 | 管线 dry-run 零 hard |
| `assets/CONCEPT_GRAPH.md` | 12 概念前置偏序图（机读块 = 唯一真相） | 图健康六判据 |
| `assets/DOMAIN_SPEC.md` | 12 条细分口径（定义 / 判据 / 失效模式） | 双源一致 |
| `assets/STANDARDS_ANCHORS.md` | 逐条权威锚 + 可达性实测 | 锚状态非 0 |
| `assets/provenance.json` | 资产供应链台账 | 头 ↔ 台账双源一致 |
| `outputs/`（10 面） | 契约 3 / 数据 3 / 可复算 1 / 图表 1 / 图示 2 | **全部 ≥T2** |

## 机验率与「≥95%」口径

**可机验占比 = 该包 `outputs/INDEX.json` 声明的产出面里档位 ≥T2 的比例**，
门槛 **0.95**，由 `check32 domain_packs` 子扫描硬判（低于门槛即 FAIL）。
14 个域包的实测值：**产出面占比 1.0000**（10/10）；
若把 `assets/*.md` 散文面也算进分母，包级比率为 **0.7692**（10 面 / 13 面）——
两个口径都写在映射表与名录里，不混用。

其中 **T4（可复算）** 面是硬要求：域报告由 `outputs/samples/CASES.csv`（确定性合成夹具）
经 `core/domain_metrics.evaluate()` 重算，`check32` 逐字段比对——口径改了没重算就 FAIL。

## 可扩展标准对齐（概念密度 · 数据真实性）

**标准目录**：`protocol/standards_catalog.json`（**370 条** · **194 家机构** · **330 条本机可达 200**；
层分布 data 148 / iface 84 / gov 71 / eng 38 / form 29）——每条 = 一个**带扩展机制**的开放标准/规范，
登记 `ext_points`（扩展点 / 扩展注册表 / profile / 命名空间）· `layer`（data / iface / form / gov / eng）·
`depends_on`（**规范层面真实依赖**的另一目录内标准，共 206 条依赖边）与**可达性实证**
（状态 + 取样标题 + sha256 + 探针日期）。不可达 40 条**如实记**（bot 防护 403 / 反爬 202 /
付费墙 / 本机出口不通），不假装可达。外部标准只作口径与机制参照，不作品质背书。

**逐条双锚绑定**：`protocol/standards_binding.json` —— 100 包 × 12 条细分 × **2 条锚** =
**2400 条绑定**，每条给出：

- **主锚**（`standard`）：该细分依据的**域口径标准**——关键词命中 / 域码专属 / 段默认轮换 / 多样性补位；
- **辅锚**（`support_standard`）：该细分产出的**承载标准**（契约 / 数据表 / 图表 / 图示 / 溯源 / 身份 / 事件），
  优先与主锚**异层**（口径锚与承载锚分属不同层才有信息量）；
- 每个锚逐条落 `layer / body / url / reachable / probe_date`——**实测证据跟着绑定走**。

门禁（check32 `domain_packs`）七条：

1. 每条细分必须有 `standard_ref`（覆盖率 **100%**）；
2. 引用的 id 必须在标准目录在册（含辅锚）；
3. 单包至少贴 **3 条不同标准**（防「一包一标准」的伪对齐）；
4. **概念密度 = 边 / 节点 ≥ 1.25**（含主锚/辅锚标准节点、绑定边与标准依赖边）；
5. **辅锚覆盖率 = 100%**（每条细分另有产出承载标准）；
6. **标准依赖边 ≥ 1**（绑定标准的 `depends_on` 必须进入概念图）；
7. **数据真实性分 ≥ 0.95** 且绑定标准**本机实测可达率 ≥ 0.9**（口径见下）。

绑定同时进入概念图：**standards 分支**（节点 `STD-<标准 id>`，层位 P80，溯源键 `std-catalog`）
含三类边——「概念 → 主锚」「概念 → 辅锚」「标准 → 标准（依赖）」。实测（100 包全覆盖）：
节点 **21–34**、边 **38–60**、依赖边 **3–12/包**，**最小密度 1.2647（均值 1.8566）**，
较上一波（最小 1.0476）实质加深。

**数据真实性（可复算复合分）**：`data_authenticity = 在册率 × (0.5 + 0.5 × 实测可达率) × 证据齐备率`
——**乘法口径**（任一环缺即降分，不用加权和掩盖缺口）。三要素逐包复算：
① 绑定标准在目录在册率（须 1.0）；② 绑定标准的**本机实测可达**率；③ 实测记录
（探针日期 + 取样 sha256）齐备率（须 1.0）。另有细分权威锚已探率（`anchor_probed_ratio`）。
实测 100 包：在册率 1.0 · 可达率 1.0 · 证据齐备 1.0 → **真实性分全 1.0**（门槛 0.95）。
夹具仍为确定性合成并显式标注；「锚不可达」或「绑定不在册」即 FAIL。

## 命令

```
python scripts/nf.py domain specs                    # 列域规格与建包状态
python scripts/nf.py domain build --spec A09 --write # 生成/更新一个域包（幂等）
python scripts/nf.py domain verify --spec A09        # 生成物 ≡ 生成器（逐字节）+ 登记三处到位
python scripts/nf.py output verify                   # 全量产出面机检（含 T4 复算）
python scripts/nf.py output meter --write            # 机验率 / 功能面 + 重签基线
python scripts/nf.py register community/<包名> --apply # 投影 registry protocols[]（三要件②③）
```

新增一个域的完整流程（作者侧）：写域规格（12 条细分 × 定义 / 权威锚 / 可机检判据 / 失效模式）
→ 锚可达性实测 → `domain build --write` → `register --apply` → 重签基线（资产行 / golden /
ledger / advisory / 模块签名 / 回执 / conformance）→ `bash verify.sh` 全绿。

## 已建 100 包（A–F 六段 · 1200 条细分）

| 段 | 主题 | 包数 | 域码 |
|---|---|---|---|
| A | 模型与能力 | 14 | A01–A14（A08/A12 等按清单缺号） |
| B | 任务类型 | 18 | B01–B18 |
| C | 工程与运维 | 18 | C01–C18 |
| D | 行业垂类 | 20 | D01–D20 |
| E | 内容与创作 | 20 | E01–E20 |
| F | 生态与治理 | 10 | F01–F10 |

全量名录（域码 / 类别 / 管线 / 模块 / 产出面 / 机验率 / 标准绑定数 / 概念密度 /
数据真实性分）见 `protocol/domain_packs.json`；逐条绑定见 `protocol/standards_binding.json`。
包序与选题曾由**决策模型**（本地 Laya 热路径在中段候选里排序）参与裁决，全部记录在内部档案；
公开面只呈现结果（包与判据），不呈现决策过程。

## 度量族（T4 口径，全部本仓实现、零第三方依赖）

`classification`（P/R/F1/混淆矩阵/ROC-AUC）· `retrieval`（R@k / MRR / nDCG / MAP）·
`extraction`（EM + 字段级 P/R/F1）· `generation`（EM / 字符 F1 / 词集 F1）·
`regression`（MAE/RMSE/R²/MAPE）· `calibration`（ECE/Brier/可靠性桶）·
`agreement`（Cohen's kappa）· `preference`（一致率/胜率）·
`exact_judgement`（pass@1 / pass@k 无偏估计）· `latency_cost`（分位时延/成本）·
`drift`（PSI）· `contract_compliance`（合规率/缺字段分布）。
每个族的公式与边界处理写在 `core/domain_metrics.py` 的 docstring 与单测里。

## 边界（不宣称）

- **样例是合成夹具**：域报告是「口径可复算」的证明，不是真实模型/系统能力的测量结论。
- **锚只作口径参照**：外部规范/论文/仓库登记入口与可达性，不复制文本、不作质量背书。
- **覆盖 100/100 类（1200 细分）**：清单内的 100 大类已逐类建包；域包**不宣称完备**——
  细分口径的质量仍由各包判据、度量族与 check32 承担，覆盖面不等于能力面。
- **标准对齐 ≠ 遵从认证**：`standards_catalog` / `standards_binding` 只声明「该细分与之对齐、
  且该标准本机实测可达」，不声明合规、认证或采用。
- 不可达锚如实记 ✗（并给替代锚），不假装可达。
