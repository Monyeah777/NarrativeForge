# 28 · v2.1.0-A1 补遗：ProtocolForm 接通 doc_semantics 声明入口（内部语义闭环）

> **需求提炼**：「A3 暂缓 → 先补 A1 遗留：ProtocolForm 加 doc_semantics 字段接通声明入口（内部语义闭环，小改动）」。A1（25 方案）已把 AGENTS vs SKILL 裁决机制化为 semantics.py 并在 self_check 加值域校验，但向导表单/生成器从未产出该键——本补遗把声明入口接通。批准方案全文见 .rivet/plans/28-v2-1-0-a1-补遗-protocolform-接通-doc-semantics-声明入口-内部语义闭环.md。
> **非目标**：不改 semantics.py / self_check 校验逻辑（已对，接输入即可）；不改 01/02 协议层 Schema（doc_semantics 是向导可选 package 声明，check14 登记门禁不读）；A3（三框架 rules 渲染）仍暂缓（外部格式锚点不可得）。

## 核心

`desktop/src/core/protocol_wizard.py`：`ProtocolForm` 加 `doc_semantics: str = ""`（未声明 / "project_rules" / "skill"）；`build_protocol_yaml` 非空时校验值域（非法抛 ValueError 拦截坏 yaml）并把 `doc_semantics:` 键写入 package 段（pipeline 后）；空缺省不写（真实实例/登记门禁兼容）。self_check 值域校验（L114-123 已就位）由死代码变生效——语义闭环完整：向导声明 → yaml 携带 → self_check 校验 → 下游按裁决路由 AGENTS/CLAUDE 或 SKILL。

## 文件

- 修改 `desktop/src/core/protocol_wizard.py`（ProtocolForm 字段 + build_protocol_yaml 写键 + 值域拦截）
- 修改 `desktop/tests/test_protocol_wizard.py`（追加 4 用例，既有 6 零改动）
- 本文件 28（A1 补遗方案真相源）+ README 协议链 + CHANGELOG [Unreleased]

## 验证

- `python -m unittest discover -s tests` 全绿（158→162：protocol_wizard +4）
- `bash verify.sh` PASS=24 + e2e PASSED
- 声明 project_rules/skill → yaml 含 `doc_semantics:` 键 + self_check 通过；非法值 build 期 ValueError；空缺省不产键（兼容）

## 分波

单文件小改动 + 测试——单波实现 + 提交（b4994f4）。GUI 不涉（L3 冻结；build_protocol_yaml 纯 core 生成器，CLI/库侧可调）。
