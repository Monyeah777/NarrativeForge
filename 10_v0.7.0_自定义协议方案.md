# NarrativeForge · v0.7.0 自定义协议方案（落盘执行版）
> **版本定位**：v0.7.0「自定义协议——在 v0.6 统一入口之上对外开放协议注册，第三方可按协议声明注册自定义协议，进入注册表登记与门禁调度」
> **基线**：v0.6.0 已发布（tag v0.6.0=cf3e922 收口 commit）+ 工作区干净（HEAD=`cf3e922`，与 origin/main 差一个收口提交待推）
> **文档状态**：本文件为 v0.7.0 方案落盘真相源（I5）；取证时间 2026-09-04，基线 `cf3e922`
> **编号声明**：方案按「问题 A → 改进 B → 任务 T」三层自洽编号。硬锚点与 ROADMAP §1 行 12（v0.7.0「对外开放协议注册，第三方可注册自定义协议」）及 §4 一句话目标对应：A1 第三方协议注册无机读入口、A2 协议声明无机读 Schema、A3 注册无门禁校验；B1–B3 一一对应；T1 协议声明 Schema 规范（无前置）→ T2 注册流程机读化 + 两包 retro-fit + registry.json protocols[] 投影（依赖 T1 Schema 定义）→ T3 verify.sh check14 门禁 + v2.4（依赖 T1 校验规则与 T2 落盘素材）。「协议开放三部曲」中 v0.7 承接 v0.6 统一入口（registry.json/registry_loader 已交付），开放模块组合装配归 v0.8.0，本版不做。
---
## 0. 定位与边界
- **做什么**：全部改动落在 **第三方协议声明机读化（protocol.yaml Schema）/ 注册流程机读化（02 §8.3 升级 + registry.json protocols[] 投影）/ 注册门禁 check14** 三类。不新增题材内容、不改官方核心 13 件职责语义、不动社区两包既有模块/资产内容、不开放模块组合装配。
- **不做什么**：第三方模块组合装配（= v0.8.0）；registry_loader 消费 protocols[] 做装配驱动（loader 只读官方核心投影维持不变，社区装配消费归 v0.8.0）；Android 功能扩展与 APK 闪退修复（= v0.9.0）；端到端测试入 CI（范围外）。
- **护航不变式**：I1 三正交分离 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一；两级平台规则 R1（社区包禁互引）/ R2（类别独占）/ R3（装配契约）。**本版受动声明**：I5 由「文档层 + 机读投影双校验唯一」扩展为「文档层 + 机读投影 + 协议声明三校验唯一」——02 §8 社区登记表仍为社区包登记真相源，包内 protocol.yaml 为其机读协议声明、registry.json protocols[] 为其机读投影，新增门禁 check14 断言三者一致（见 B1–B3/T1–T3）；官方核心投影（registry.json modules/mount_points/subscriptions）与 check13 不受动。其余不变式不受动。
- **改动范围**：01_核心协议.md（§6 增第三方协议声明 protocol.yaml Schema 小节）、02_联动注册表.md（§8.3 登记规则升级 + §9.1 字段映射/§9.2 同步纪律扩展 protocols[]）、community/校园情感领域包/、community/西幻生存领域包/（各增 protocol.yaml）、desktop/src/core/registry.json（增 protocols[]）、verify.sh（check14 + v2.4）、ROADMAP（§1 行 12 状态切主线 + §4 范围细化落盘）、README（协议链追加 10 + 版本行切 v0.7.0）、CHANGELOG（[Unreleased] 回填，v0.6.0 收口批次已切至 v0.7.0）。
---
## 1. 问题全景（A1–A3，实测取证）
### A1 第三方协议注册无机读入口（ROADMAP §4「进入注册表登记」无人读落地以外形态）——登记规则纯人读、无 Schema、无流程
- **证据**：
  - `02_联动注册表.md §8.3`（L163-165）：「M91-M99 编号段继续留给后续第三方领域包；新包须遵循 R1/R2/R3，并在本表登记后方可被平台识别」——**纯人读宣言**，无「登记 = 提交何物/校验何物/如何入投影」的可执行定义。
  - verify.sh check7（L132-163）对社区包校验止于**数件数**：从 02 §8.1/§8.2 登记行反解「模块（N）」数字，与 modules/ 实存件数比对——只证数量一致，**不校验登记内容本身的合规性**（类别独占是否冲突、依赖边界是否越界、编号是否唯一、是否落 M91-M99 预留段）。
  - `02_联动注册表.md §9.1` 字段映射（L169-175）只覆盖官方核心 §2/§3/§5；§9 投影声明（L168）明确「M90/P90 技术文档领域实例（§7）与动态订阅不纳入投影」——社区登记（§8）整体不在机读投影范围，第三方新包登记后平台无机读侧可识别物。
- **根因**：v0.5.0 迁出题材内容建 community 时（T2 社区包装配）只承诺「人读登记 + verify 数件数」，未设计第三方登记的机读契约；v0.6.0 机读化仅覆盖官方核心。
- **影响**：第三方按 02 §8.3「登记」时无从提交机读声明、无从自证 R1/R2/R3 合规；平台侧无机读识别与门禁仲裁，登记形同「在文档里加几行字」，违背 ROADMAP §4「进入注册表登记与门禁调度」的开放语义。
### A2 协议声明无机读 Schema——社区包 R1/R2/R3 声明散落在人读 README，格式自由、位置不统一、无校验器
- **证据**：
  - 校园包 `community/校园情感领域包/README.md` L1-5：独占「情感」类（01 §6 R2 / 02 §8.1）、依赖边界（R1）只依赖官方核心层 12 件（M00/通用:M10/M23/M24/M50/M80/事件:M22/M06/M12/M13/M20/M08）、不跨包依赖——**人读自由格式 Markdown**。
  - 西幻包 `community/西幻生存领域包/README.md` L1-4：独占「生存/世界/事件」类（R2）、依赖边界同构 12 件核心、不跨包依赖校园——同为人读自由格式。
  - `desktop/src/core/registry.json` 顶层键实测：`registry_schema_version / schema_name / registry_name / truth_source / note / modules / mount_points / subscriptions` 共 8 键——**无 protocols 键**，社区包声明（类别/依赖/编号/管线/资产规模）无任何机读承载。
  - 两包完整模块清单/订阅/依赖/挂载散落在 02 §8 登记行 + 包内各模块头 events/depends 声明 + README 速览表（如校园 README「题材模块 9 / 官方核心配合件 12」）——同一事实三处人读表述，无单一机读 Schema 聚合。
- **根因**：v0.5.0 社区包诞生时 R1/R2/R3 是平台规则（01 §6），但「本包声明自己遵守了哪些规则」从未被要求以结构化形式提交——人读 README 承担了声明职能。
- **影响**：第三方新包即使想合规登记，也无模板可抄（无 Schema/无必填清单）；平台无法自动校验其类别独占（R2 冲突）、依赖边界（R1 越界）与编号占用（M91-M99 重号）；v0.8.0 开放模块组合时更将面对「无法机器判断两个包能否装配」的断层。
### A3 注册流程无门禁校验——check7–13 无任何协议声明合规断言，R1/R2/R3 校验全靠人眼
- **证据**：
  - verify.sh check7–11（段 B，L132-247）职能盘点：check7 在册数一致性（数件数）/ check8 资产行数溯源 / check9 代表键可寻址 / check10 EXT 闭合 + 红线落地 / check11 资产三方对账（reconcile_assets.sh）——**五项均不解析 R1（依赖边界）/R2（类别独占）/R3（装配契约）的声明合规**。
  - check13（L286-340）③ 模块逐条一致断言仅覆盖「02 §2 官方核心 13 件 == registry.json modules」，社区登记（§8）不在其比对域。
  - 全仓 `grep -rnE 'protocol\\.yaml|protocols\\[' 02_联动注册表.md desktop/src/core/registry.json verify.sh 01_核心协议.md` → 除本方案外零命中（无协议声明概念文件/字段/校验器）。
- **根因**：门禁体系随各版本问题域生长（check1–6 官方出厂 / check7–11 社区结构 / check12 代码层 / check13 迁移完整性），「第三方协议注册」作为 v0.7.0 命题此前未立项，故无对应门禁位。
- **影响**：新包登记合规性（R1/R2/R3/编号段/双源一致）无自动闸——任一违规（如两个第三方包撞类别、依赖越界到对方包、编号落在官方段）都能通过现有 check7–13 全绿；「门禁调度」名存实亡。
---
## 2. 改进收口（B1–B3，与 A1–A3 对应）
| 编号 | 收口主题 | 对应问题 | 一句话收口 |
| --- | --- | --- | --- |
| B1 | 协议声明 Schema 机读化 | A1 | 01 §6 新增第三方协议声明 protocol.yaml Schema 规范（必填字段 + R1/R2/R3 映射 + M91-M99 编号规则 + 模板），社区包根目录必带 protocol.yaml——「登记提交物」从人读宣言升级为机读声明 |
| B2 | 注册流程机读化 + 投影扩展 | A2 | 02 §8.3 升级「登记 = protocol.yaml 在场 + 本表在册 + registry.json protocols[] 投影」三要件；校园/西幻两包 retro-fit 落 protocol.yaml 作首例战例；registry.json 增 protocols[] 投影区并扩 §9 映射/纪律 |
| B3 | 注册门禁 check14（v2.4） | A3 | verify.sh 新增 check14 第三方协议注册机读门禁（入段 C）：Schema 合法 / R1-R3 合规 / 编号段唯一 / protocol.yaml ↔ README ↔ 02 §8 ↔ registry.json protocols[] 四方一致；版本升 v2.4 |
---
## 3. 任务分解（T1–T3，依赖序）
```
T1 协议声明 Schema 规范 ──→ T2 注册流程机读化落地 ──→ T3 check14 门禁
  （A1/B1，01 §6 定义）      （A2/B2：两包 retro-fit     （A3/B3：依赖 T1 校验规则
                              + 02 §8.3 + registry.json    与 T2 落盘素材方可断言）
                              protocols[]）
```
依赖：T1 无前置；T2 依赖 T1（protocol.yaml 须按 T1 Schema 编写，retro-fit 即 Schema 首例实战）；T3 依赖 T1（校验规则）+ T2（两包 protocol.yaml 与 registry.json protocols[] 在场）。执行序 T1 → T2 → T3；每任务独立 commit（§5）。
---
## 4. 任务明细
### T1 协议声明 Schema 规范落盘（A1/B1，v0.7.0 核心命题）
- **动作 1**：`01_核心协议.md` §6 平台规则后新增「6.x 第三方协议声明（protocol.yaml）」小节——定义协议声明 Schema：
  - 必填字段：`protocol.schema_version`（声明 Schema 版本，首引 "1"，独立演进遵循 §7 V1/V2/V3）/ `package.id`（包目录名）/ `package.name`（显示名）/ `package.pipeline`（自带管线，R3）/ `package.module_id_range`（模块编号清单，含重号限定 ID，如 情感:M22）/ `package.categories`（独占类别声明，R2）/ `package.dependencies.core_only: true` + `core_modules`（依赖边界，R1，限官方核心 13 件）+ `cross_package: []`（禁跨包）/ `package.modules`（模块清单）/ `package.assets`（资产数 + README 引用）/ `package.mount_layers`（挂载层，R3，对应 02 §5 空层）。
  - R1/R2/R3 映射表：R2 类别独占 → `categories`（平台校验包间不冲突）；R1 依赖边界 → `dependencies`（core_only + core_modules ⊆ 官方核心 13 件 + cross_package 空）；R3 装配契约 → `pipeline` + `mount_layers`。
  - 编号规则：第三方新包模块编号落 **M91-M99 预留段**（02 §8.3），不得占用官方 13 件与既有社区包编号；check14 断言唯一。
  - 双源一致要求：protocol.yaml 为机读真相，README 人读速览不得与之冲突（关键字段由 check14 ⑥ 断言）。
- **动作 2**：提供 protocol.yaml 模板骨架（Markdown 代码块，抄用置于社区包根目录），含注释逐字段说明。
- 验收：01 §6 新小节在场；字段定义覆盖 R1/R2/R3 + 编号规则；模板骨架可抄用。
### T2 注册流程机读化 + 两包 retro-fit + registry.json protocols[] 投影（A2/B2）
- **动作 1**：`02_联动注册表.md` §8.3 升级——「登记 = ① 包根目录 protocol.yaml 在场（Schema 见 01 §6.3）② 本表在册登记 ③ registry.json protocols[] 投影同步，三要件缺一不可；新包编号落 M91-M99 段，check14 断言唯一与合规」；§9.1 字段映射表增「02 §8 ↔ registry.json `protocols[]`」行；§9.2 同步纪律补「改 02 §8 必同步 registry.json protocols[]，反之亦然（check14 断言）」。
- **动作 2**：校园/西幻两包各落 `community/<包>/protocol.yaml`（从 README 人读声明转录为机读声明）——校园：`categories: [情感]`、core_modules 12 件（M00/通用:M10/M23/M24/M50/M80/事件:M22/M06/M12/M13/M20/M08）、模块 9（情感:M22/M40/M41/M43/M55/M57/M58/M59/M65）、资产 29、P02、挂载层 P40；西幻：`categories: [生存, 世界, 事件]`、core_modules 12 件（M00/通用:M10/M08/M23/M24/M50/M80/事件:M22/M06/M12/M13/M20）、模块 14（M01-M04/M07/M09/生存:M10/M11/M14-M19）、资产 24、P03、挂载层 P40/P60——作 retro-fit 首例战例。
- **动作 3**：`desktop/src/core/registry.json` 增 `protocols[]` 顶层键（schema_version 保持 "2" 不变，V1 只增不删兼容演进）——校园/西幻两包协议机读投影：`{id, name, pipeline, categories, module_ids, assets, mount_layers, schema_version}`。
- 验收：两包 protocol.yaml 在场且 yaml 可解析；registry.json protocols[] 含两包且与 02 §8 在册一致；02 §9 映射/纪律已更新。
### T3 verify.sh check14 第三方协议注册机读门禁 + v2.4（A3/B3）
- **动作 1**：verify.sh 新增 `check14()`（入段 C）——断言：① 每个 community 领域包根目录 protocol.yaml 在场（缺 = FAIL）② yaml 可解析 + 必填字段在场 ③ R2：categories 与 02 §8 在册独占类别一致、包间不冲突 ④ R1：dependencies.core_modules ⊆ 官方核心 13 件（限定 ID 合规）、cross_package 为空 ⑤ 模块编号与 02 §8 在册数一致、不落官方段（M91-M99 预留段规则）、包间不重号 ⑥ protocol.yaml 与 README 人读声明关键字段一致（独占类别/依赖边界 grep 断言）⑦ registry.json protocols[] 与 02 §8 在册一致（条目数 + id 集合全等）。
- **动作 2**：版本 v2.3→v2.4 三处同步点——L4 配套注释补「+ 10_v0.7.0_自定义协议方案（check14 协议注册门禁）」；L10 段 C 描述行补 check14；L353 主执行体 banner「v2.3」→「v2.4」+ 段 C 调用追加 `check14`（check12/check13/check14 顺序）。
- **动作 3**：yaml 解析依赖 python3（同 check13 守卫，python3 缺失时 WARN 不 FAIL）。
- 验收：verify.sh 全绿 PASS 增 1（18→19）；新包缺 protocol.yaml / 类别撞包 / 依赖越界 / 编号重号任一模拟态均 FAIL；README 与 protocol.yaml 冲突模拟态 FAIL。
---
## 5. 提交规划
| 序 | 提交内容 | 前缀 | 关联 |
| --- | --- | --- | --- |
| C0 | 10 方案落盘 + ROADMAP §1 行 12 状态切「🔵 当前主线」/§4 范围细化落盘 + README 协议链追加 10/版本行切 v0.7.0 + CHANGELOG 确认 | docs: | 本文件 |
| C1 | T1 01 §6 第三方协议声明 protocol.yaml Schema + 模板 | docs: | A1/B1 |
| C2 | T2 两包 protocol.yaml retro-fit + 02 §8.3/§9 升级 + registry.json protocols[] | feat: | A2/B2 |
| C3 | T3 verify.sh check14 + v2.4 三处同步 | chore: | A3/B3 |
| C4 | 全量 verify 绿 + tag v0.7.0 收口 + push | chore: | 收口 |
每任务独立 commit；C1–C3 期间工作区只允许该任务相关文件变更，避免交叉。C4 push 前须换新 PAT（旧 token 已暴露，见 §6 开放问题 6）；cf3e922（v0.6.0 收口）与 v0.7.0 批次可一并推或先推 v0.6.0。
---
## 6. 风险与开放问题
1. **protocol.yaml 与 README 人读声明双写一致性（I5 受动）**：protocol.yaml 为机读真相、README 为速览，双写漂移由 check14 ⑥ 断言兜底；改声明必须同批次双改，CONTRIBUTING 社区包细则需同步补「协议声明双源一致纪律」。
2. **registry.json protocols[] 与官方投影边界**：protocols[] 只登记社区包协议声明（id/类别/依赖/编号/资产/挂载层），不展开模块级装配数据（模块级消费归 v0.8.0）；registry_schema_version 保持 "2" 不动（V1 只增不删兼容演进），防 bump 蔓延。
3. **两包 retro-fit 转录错误**：从 README 人读声明转录 protocol.yaml 时逐字段对照（类别/依赖 12 件/模块数/资产数）；check14 ⑥ 人读-机读一致断言兜底，转录后 verify 全绿方可提交。
4. **第三方新包登记流程落地节奏**：M91-M99 预留段与 R1/R2/R3 校验已机读化，但「第三方包从提交到登记」的协作流程（PR/评审/发布）不属本版协议层范畴——本版交付登记要件与门禁，流程治理留待 v1.0.0 生态收口。
5. **与 v0.8.0 边界**：registry_loader 保持只读官方核心投影，不消费 protocols[] 做装配驱动——社区包装配消费（自定义模块组合）归 v0.8.0；本版禁止顺手扩展 loader 功能（防范围蔓延，同 09 §6 开放问题 3 纪律）。
6. **推送安全（操作层）**：历史 PAT token 已在早期终端明文出现，C4 push 前须向用户索取新 token 并 revoke 旧 token；C0–C3 本地 commit 不受影响。
7. **yaml 解析环境依赖**：check14 解析 protocol.yaml 依赖 python3（PyYAML 或 json 化 YAML 子集）；python3 缺失时按 check13 同款 WARN 处理，不因环境缺依赖误报 FAIL。
8. **v0.7.0 交付锚点核验**：ROADMAP §1 行 12 状态切「🔵 当前主线」+ 方案真相源落 10 文档 + verify.sh v2.4（check1–14）全绿 + 两包 protocol.yaml 在场 + registry.json protocols[] 投影同步——五项齐备方可进入 v0.8.0 立项。
