# 35 · v2.4.0 外部吸收大包：规范同步核查三连 + 实现四件（全收）

> **需求提炼**（用户链式裁决）：「v2.4 做一次大而全的外部吸收：候选池扩到最大，过滤评价后收敛出『最终版全收清单』」→ 两轮侦察 12 源 → A 级 7 项全收、B 级 5 项备选、C 级排除。批准方案全文见 .rivet/plans/35-v2-4-0-外部吸收大包-规范同步核查三连-实现四件-全收.md。
> **非目标**：B 级 5 项（quality_gate 建议 v2.5 / 渐进披露拆分 / world 结构增强 / plugin marketplace / ccv3 完整性 check）；C 级全部；端壳/GUI（L3_FROZEN）。

## 核心

两波落地：

1. **Wave1 规范同步核查三连**（672029e）：`35_v2.4.0_外部规范同步核查报告.md`——A1 SKILL 规范（agentskills.io 实抓：name 仅 a-z0-9-/≤64/匹配父目录 + description ≤1024 + license/compatibility/metadata/allowed-tools 新字段）；A2 CCV3 v3（SillyTavern 官方 TavernCardValidator.js SSH clone 实读：spec_version 须数值字符串 ∈[3.0,4.0)，发现 NF "v3" 是 bug 级差距）；A3 AGENTS.md（openai/agents.md 官方仓库：开放格式无硬字段，NF 产物合规）。清单侦察的「kwaroran CCV3 v3」仓库不存在已修正为 SillyTavern 权威源。
2. **Wave2 实现四件**：
   - **A4 导出物规范体检 check22**（fbcae71）：ccv3 spec_version "v3"→"3.0"（修复 ST 导入 fail）+ skill name 改 pipeline_id ASCII slug + license 字段 + export_schema 硬约束（spec_version 数值/skill name 规范/description ≤1024）+ verify check22（PASS 29→31）。
   - **A5 变更助手引用重链**（e1b4c9e）：impact_check.rename_module_plan + `nf rename <old> <new> [--check|--apply]`——references 批量重链闭环（--check 只列不写盘，--apply 合并写 registry）。
   - **A6 质量分级徽章**（55bbfaa）：market_analyzer.grade_of_module/grades_of_package（official/community/experimental；官方判定匹配完整 id 防 M22 重号段误判）+ nf market 徽章输出。
   - **A7 上架元数据模板**（84aaa75）：02 §8.3 登记三要件补上架元数据四元组（版本/作者/兼容/说明，V1 只增不删）+ 模板制作指令包交付自检第 4 条。

## 验证

- verify.sh v2.10→v2.11（check22 入段 C）：PASS 29→31 全绿（WARN=0 FAIL=0，多次实测）。
- 单测 258→267（A4 spec_version 断言改 +1 语义、A5 +4、A6 +5）；全量 discover 267 OK。
- CLI 冒烟：`nf rename M55 M99 --check` 列出重链清单不写盘；`nf market 技术文档域包` 输出 M97/M98 实验徽章。
- 瑶光反证：A2 spec_version RED 复现（Number("v3")=NaN → validator fail，"3.0" 通过）——修复后 GREEN。

## 文件

- 新增 `35_v2.4.0_外部规范同步核查报告.md`（Wave1）
- 修改 `desktop/src/core/ccv3_adapter.py`（spec_version）+ `skill_adapter.py`（_slug/license）+ `export_schema.py`（check22 硬约束）+ `impact_check.py`（rename_module_plan）+ `market_analyzer.py`（分级）
- 修改 `scripts/nf.py`（rename 命令 + market 徽章）+ `verify.sh`（check22 + v2.11）
- 修改 `02_联动注册表.md`（§8.3 上架元数据）+ `community/模板制作指令包.md`（交付自检）
- 修改测试：test_ccv3_adapter / test_export_schema / test_impact_check / test_market_analyzer + fixture chara.json/SKILL.md
- 本真相源 + README 协议链补 35 + CHANGELOG [Unreleased] + ROADMAP §1/§7.5 收口

## 遗留

- B 级 5 项（quality_gate 建议 v2.5 / 渐进披露拆分 / world 结构增强依赖 A2 结论 / plugin marketplace / ccv3 完整性 check）记 ROADMAP 备选。
- C 级全部排除（理由见核查报告 + ROADMAP §7.6 条件池）。
- A7 上架元数据为软性建议（缺省不阻断），机读校验待 protocol.yaml metadata 字段落地后补（GUI 解冻立项）。
