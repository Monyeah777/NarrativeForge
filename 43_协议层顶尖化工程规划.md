# 43 · 协议层顶尖化工程规划（2026-09-08 作者指示立项）

> **状态**：✅ **A1-A5 已全量交付（2026-09-08 收口）**——verify v2.20 check1-31 PASS=49 全绿；audit 见 `docs_audit-43-protocol.md`。目标 = 把协议层（01/02/06/07 + registry + machine_contract）从「机检闭环」提到「顶尖」（IDL 化 + 一致性分级 + 变更分层自动化 + 生成物 golden）。
> **方法纪律**：42 §四.5「标准→方案→计划」三段流程律 + §四.6「外部观点过仓库实证铁律」。本文档灵感来源 = GitHub 开源项目，逐条对照仓库实证：已具备不重做、不适面剔除、真空白吸收。

---

## 一、外部灵感实证（来源均经本仓库逐条对照）

| 来源 | 做法/主张 | 仓库实证 | 判定 |
|---|---|---|---|
| Fission-AI/OpenSpec | spec 目录分层 + spec lint/validate 可执行 + change 提案工作流 | 仓库已有人读方案链 01-42 + verify 机检族（check13-27）+ audit/steelman；但**协议字段结构无正式 schema 定义文件**（正文 yaml 示例 + 各 parse 自行正则/半解析） | ✅ **真空白（IDL）**：把契约字段升为正式 schema 定义 + 统一校验器 |
| github/spec-kit | SDD：spec 先行、guardrails、extension/preset | 立项/分波/判级器已在（41/42），SDD 上游衔接 C6 已落地 | 🟡 不重做；不照搬 slash-command 工作流 |
| Glyph Protocol（RFC 治理） | 实质变更（新端点/schema/conformance 变化）走 RFC；conformance level 分级 | 有迁移记录与 check13，但**无 conformance 一致性分级**（协议件属哪一级无标注） | ✅ **真空白（Conformance 分级）** |
| peacprotocol PR | `schema_set_version` 与包版本解耦 + `specs/wire/README` 扩展策略 | registry_schema_version"2" 在、迁移四步在；**无显式扩展策略文档**（什么 = 字段级新增 vs 须 bump） | ✅ **真空白（扩展策略 + 影响度分层）** |
| ai-spec-driven-development | 文档 Frontmatter + 影响度 SemVer（LOW 措辞=patch 等） | VERSION-MATRIX + CHANGELOG + nf sig/diff 在 | 🟡 半有 → 并入 A3（diff 判定加影响度级） |
| Auto Agent Protocol | spec/schemas/examples/generated artifacts 同仓、字段命名一致性 | registry 投影 + export_schema + fixtures 在 | 🟡 半有 → A4 生成物同仓 golden 化 |
| contractual-dev/contractual | 契约 lifecycle/versioning/diff 防 breaking | nf sig/diff + check25/check21 已覆盖 | ❌ 已具备，不重做 |
| JSON Schema / RFC 2119/8174 | 机器校验 + 强度词 | 判级器 06 §11 已落；schema 部分见第一行 | 部分已具备（判级），schema = IDL 吸收项 |

**净吸收 = 4 真空白**：A1 协议 IDL、A2 Conformance 分级、A3 扩展策略 + 变更影响分层自动化、A4 生成物同仓 golden。其余已具备或不适，不重复建设。

---

## 二、协议层顶尖五要素（验收表）

| 要素 | 顶尖标准 | 现状水位 | 归口 |
|---|---|---|---|
| ① IDL 单一真相 | 每个契约件有**正式 schema 定义**（字段/类型/必填/约束），正文示例可由校验器验证 | 无 schema 定义文件；01/02 正文 yaml + machine_contract 由多套 parse 半解析 | A1 |
| ② 一致性可分级 | 每个协议件标注 conformance level（可读/机读/可执行/互操作），机检分级 | 无 conformance 分级字段 | A2 |
| ③ 演进可预测 | 扩展策略显式（字段级新增 vs 结构 bump 判据）+ 变更影响度自动化（diff → editorial/additive/bump 判定） | 迁移记录在、diff 判定粗（破坏/需评审/兼容） | A3 |
| ④ 产物可复现 | schema 定义 → 生成物（registry 投影/schema 文档/examples）同仓 + golden 回归 | registry 双源在；schema 生成物与 golden 无 | A4 |
| ⑤ 顶尖闭环 | 上述全部由 verify check 常驻 + 两波零命中回归 | check13-27 覆盖结构自洽与纯度 | A1-A5 收口 |

---

## 三、里程碑

### A1 · 协议 IDL 层（schema 定义 + 校验器）
- 新建 `protocol/schema/`：`module.schema.json` / `pipeline.schema.json` / `asset.schema.json` / `contract.schema.json`（machine_contract）/ `protocol.schema.json`——零第三方 JSON-schema 实现（写最小校验子集，字段/类型/必填/枚举/值域）。
- 新建 `desktop/src/core/schema_lint.py`：对 04 模块头 machine_contract、03 管线 yaml、05 资产头、community protocol.yaml 跑统一 schema 校验（替代散落 parse 的部分盲区）。
- verify check28：schema 定义在场 + 全量件可校验（PASS 41→43，v2.17）。
- 验收：44 模块 + 8 管线 + ledger 全量过 schema；任一字段漂移即 FAIL。

### A2 · Conformance 一致性分级
- 01 加「Conformance 分级」段：L0 结构可读 / L1 机读契约（machine_contract + registry）/ L2 装配可执行（references 闭包）/ L3 外部互操作（MCP/导出 schema）。
- 每协议件头加 `conformance: Lx` 位；verify check29 校验声明 ≤ 实际可证级别（防虚标）。
- 验收：官方 13 模块 + 5 协议包 + 导出 schema 分级标注齐、零虚标。

### A3 · 扩展策略 + 变更影响分层自动化
- `protocol/EXTENSION.md`：可字段级新增（注明 → 不 bump）vs 须结构 bump（→ 迁移记录）判据表。
- `nf diff` 判定升级：verdict 增加 `影响度 = editorial（措辞）/ additive（字段级新增）/ bump（结构性）` 三档（与 VERSION-MATRIX/CHANGELOG 联动）。
- check30：bump 变更须带 02 §9.3 迁移四步（否则 FAIL）。
- 验收：01 近两年历史变更可按三档回放归类（抽 3 例实证）。

### A4 · 生成物同仓 + golden
- `protocol/generated/`：由 schema 定义生成 machine_contract 文档/校验摘要（脚本 + 单测）；示例 `examples/` 进 fixtures。
- golden：`scripts/release_freeze.sh` 已存 tag 指纹——A4 追加 schema 生成物指纹（key 变更即红）。
- 验收：生成物可复现；A1 schema 与生成物双源一致（check31）。

### A5 · 全量落地 + 收口
- 01/02/06/07/registry 全量过 A1-A4；verify PASS 41→约 45（v2.17 起逐 check 升）；连续两波零命中；audit 建档（docs_audit-43 格式沿用）。
- 收口：协议层 IDL/分级/扩展策略/生成物全部进 verify；tag 随作者拍板（不产独立 tag，随内容波收口）。

---

## 四、执行约束
1. 外部灵感只作机制借鉴，不照搬星标/生态叙事；每条落库前过仓库实证（见 §一）。
2. 零第三方依赖红线保持：schema 校验器自实现（JSON-schema 子集），不引 jsonschema。
3. 协议正文改动走既有纪律（01/02 增删改后 verify 全绿；迁移记录在场）；不 bump registry_schema_version（A1-A4 均为新增文件与字段级新增，结构性 bump 才动）。
4. 每里程碑五维自评 + 收口 audit。

## 五、与既有设施边界
- check13/25/26/27 = 现有结构自洽；A1-A4 = 在其上加 schema 定义/分级/策略/生成物，不重构现有 check。
- 42 判级器（06 §11）= 语言强度层；A2 Conformance = 协议件一致性层（互补不重叠）。
- nf sig/diff（41）= 指纹/差异底座；A3 在其判定上加影响度三档。
