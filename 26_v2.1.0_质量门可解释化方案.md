# 26 · v2.1.0-B1 质量门可解释化：可解释报告 + 自动修复建议（B 线质检深化）

> **需求提炼**：「质检升级：quality_gate 从『三态门（fail/warn/ok）』→『可解释报告 + 自动修复建议』（现在 warn 只是提示，没有 actionable 输出）」；ROADMAP §7.5 B1。批准方案全文见 .rivet/plans/26-v2-1-0-b1-质量门可解释化-可解释报告-自动修复建议-b-线质检深化.md。
> **非目标**：不改质检语义与不变量（fail 阻断 ok，test_quality_gate 既有 6 用例零改动）；不做自动改写装配（suggestion 是指引非执行，用户自主权保留）；不做 GUI 规则编辑器（L3 冻结）；不改 IR 结构。

## 核心

`desktop/src/core/quality_gate.py` 向后兼容扩展：`Issue` 加 `suggestion` 字段（缺省空串——两参构造仍成立）；四条默认规则补 actionable 建议（R1 空装配→勾选模块含 M00/M80 / R2 缺锚点→勾选 P00/P80 模块 / W1 资产悬空→装资产包或移除缺失键 / W2 层外模块→移入层位或改层序 I5）；`GateResult.report_text()` 可解释报告（fail 优先列阻断、warn 列可行动，逐条 [LEVEL] 原因→建议；ok 且无 issues → 通过说明）。`scripts/nf.py` 质量门打印含每条 suggestion。

## 文件

- 修改 `desktop/src/core/quality_gate.py`（Issue.suggestion + 四规则建议 + report_text）
- 修改 `desktop/tests/test_quality_gate.py`（追加 B1 6 用例，既有 6 零改动）
- 修改 `scripts/nf.py`（质量门打印带建议）
- 本文件 26（方案真相源）+ README 协议链 + CHANGELOG [Unreleased]

## 验证

- `python -m unittest discover -s tests` 全绿（149→155：quality_gate +6）
- `bash verify.sh` PASS=24（check17 质量门 unittest 含新用例）+ e2e PASSED
- nf 实测：空装配 → `[FAIL] 核心锚点层缺失模块：P00、P80` + `建议：勾选数据基座层(P00)与输出呈现层(P80)模块（M00 数据结构 / M80 输出生成器）——核心锚点…`（warn/fail 带 actionable 建议）

## 分波

单波实现 + 提交（core 扩展 + CLI 呈现 + 测试，9d396df）。GUI 真机不涉（L3 冻结）。
