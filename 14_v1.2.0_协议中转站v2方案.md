# 14 · v1.2.0 协议中转站 v2：产物 IR 内容归一化层（2.0 E0-①）

> **需求提炼（用户原话）**：「2.0 = 所有 AI 协议标准的上游生成器」；「协议中转站 v2（内容统一层）→ Agentic 检索 → 质量治理闭环 → 导出层」依赖链不可颠倒——**没有 IR，检索没有统一数据结构可查；没有检索，质检闭环无法引用已有规则库**。本版交付 IR 层，为 v1.3 检索 / v1.4 质检 / v2.0 CCV3 导出铺统一数据结构。
> **非目标**：不做检索（v1.3）、不做质检门（v1.4）、不做 CCV3/SKILL/AGENTS 导出适配器（v2.0）——本版只把"装配结果"变成**可被程序化消费的统一 IR**，MD 渲染降级为 IR 的默认原生适配器，行为不回归。

## 0. 定位与 v0.6 边界（防重复建设）

| | v0.6.0 协议中转站 v1 | v1.2.0 协议中转站 v2 |
|---|---|---|
| 层 | **登记/寻址层** | **装载/归一化层** |
| 管什么 | registry.json 机读投影 + loader 文件寻址 + check13 迁移 | 读入的协议/模块/资产/管线数据如何归一化为统一 IR 供下游消费 |
| 证据 | registry_loader.py / check13 | 本版新增 IR 结构 + generate 重构 |

v2 在 v1 之上**新增装载消费层**，不重复 registry 寻址、不撞名。

## 1. 问题全景

- **A1 装配结果只有 MD 一种形态**：`generator.generate_document`（generator.py:138）直接拼字符串——下游（质检/导出）只能再解析 MD 文本拿结构化数据，脆弱且格式耦合。
- **A2 四类对象无统一中间表示**：协议（registry protocols[]）/ 模块（Module）/ 资产（AssetPack.entries）/ 管线（Pipeline.layers）各自结构，检索/质检/导出每层都要重复处理异构。
- **A3 无导出契约锚点**：v2.0 CCV3 导出若等到立项才设计映射，将面对"IR 未预留 → 导出器被迫耦合 MD 文本"的返工风险（2.0 基线已识别为命门，须前置）。

## 2. 改进收口

- **B1 IR 结构定型（A2）**：新增 `desktop/src/core/ir.py`，定义装配产物的统一中间表示（宁薄勿厚——只承载够渲染外部格式的最小结构，防第五套协议）：

```python
@dataclass
class IRModule:
    full_id: str; name: str; layer: str; content: str   # content = source.md 归一后正文

@dataclass
class IRLayer:
    id: str; name: str; description: str
    modules: list[IRModule]

@dataclass
class IRDocument:                                     # 装配结果的结构化形态
    type: str            # "narrative" / "techdoc"（由管线 structure_type 决定）
    title: str
    pipeline_id: str; pipeline_name: str
    layers: list[IRLayer]                             # 层间按管线层序
    extra_modules: list[IRModule]                     # 层外模块（order_modules 队尾）
    asset_refs: dict[str, object]                     # 模块声明的资产键 → 值（缺失键记 None）
    asset_missing: list[str]
    warnings: list[str]
    meta: dict                                        # timestamp/generator
```

- **B2 装配执行输出 IR（A1）**：重构 `generator.py` 为两步——`generate_document` 内部改为 `render_ir(pipeline, modules, asset_pack, title) -> IRDocument` 产 IR，再由 `ir_to_md(ir) -> str` 序列化（IR 的默认原生适配器）。**对外签名与 MD 输出逐字节不变**（desktop 冒烟/zone_d/预设闭环零回归），仅内部换血。
- **B3 IR→CCV3 投影契约预留（A3）**：IR 结构设计时即对齐未来投影——IRModule.content 保持纯正文（可折叠入 persona/scenario/world 条目）、layer 保留（世界书可按键映射）、asset_refs 分离（资产不混入正文，供 world 素材引用）。v2.0 立项只写投影规则，不改 IR。

## 3. 任务分解

- **T1 建 ir.py + IR 单测（无前置）**：dataclass 四件 + `ir_to_md` 序列化器。单测：IR→MD 与现 generator 输出一致性（对同一输入逐字节相等）。
- **T2 render_ir 提取（依赖 T1）**：把 generate_document 的逐层/模块/资产附录逻辑改构为产 IRDocument + 序列化两段；`order_modules` 不动。
- **T3 一致性回归（依赖 T2）**：新增回归断言——真实输入（官方 P01 管线 + 校园包模块 + 资产包）下，重构前后 `generate_document` MD 输出逐字节相同（快照对比）；desktop 全量单测 + smoke 绿。
- **T4 段 C 契约入 verify（依赖 T1）**：check17 预留位（v1.4 立项前不建全门禁）——本版先以单测断言承载 IR 结构稳定（四类对象归一化样例 + 层序/资产附录不变量）。

## 4. 提交规划

- C0 = T1（ir.py + 序列化单测）
- C1 = T2 + T3（generate 重构 + 一致性回归，一次性提交防中间态）
- C2 = T4 + CHANGELOG [1.2.0] 段预置（开发中）
- 每批 verify（v2.6 PASS=22）全绿方可推送（工作流：先推→落方案→执行→完成推）。

## 5. 验证清单

- `python -m unittest discover -s tests`（71 + 新增 IR 用例全绿）
- `bash verify.sh` PASS=22 WARN=0 FAIL=0
- 一致性断言：重构前后 generate_document 对真实输入 MD 输出逐字节相等（T3 核心证据）
- desktop offscreen smoke 冒烟绿（生成预览路径不回归）

## 6. 瑶光反证

- **断言**：generate_document 现产物分层（头→层→模块块→资产附录），IR 可镜像——复现：read `desktop/src/core/generator.py` L138-173（parts 拼接顺序）✓ 本会话第一手。
- **断言**：v0.6 已交付登记寻址层（防重复建设前提）——证据：registry_loader.py registry.json + check13 迁移断言在场（历版 CHANGELOG）。
- **待验证假设**：重构前后 MD 逐字节一致可达成——T3 用真实输入跑快照断言实测；若 source.md 标题级提升等边界存在不可逆规范化，以快照差异为准修正（不硬凑）。
- **待验证假设**：desktop 71 单测当前 Windows 全绿（波 0 修复后已证）——回归基线成立。

## 7. 范围外（留 v1.3/v1.4/v2.0）

- Agentic 检索 / 质检门 / CCV3 导出适配器 / 协议模板 GUI——均不占本版。

## 8. 阶段工作流遵循

每阶段：**先推送基线 → 落本方案 → 执行 → 完成后推送**（用户既定纪律）。
