# 36 · v2.5.0 基础层深化续：CCV3 资产条目 id 修复 + 34 方案文档偏差收口

> **需求提炼**（作者裁决）：「v2.5 立项」→ 从 35 方案 B 级候选 + 34 方案 A4 遗留中剔除已落地项收敛真实开放项。调研戳破 35 清单「B1 quality_gate 建议 v2.5」失真（26 方案已落地 suggestion/report_text），收敛为一个 bug + 一处文档偏差。批准方案全文见 .rivet/plans/36-v2-5-0-基础层深化续-ccv3-资产条目-id-修复-34-方案文档偏差收口.md。
> **非目标**：B2 渐进披露（需先量化超限）、B5 ccv3 完整性（exporter 结构性保证）、34 A4 遗留三项（persona 还原/skill 资源装载/读入登记——非目标冻结无消费方）、B2 条件组合（协议 V2）、端壳/GUI。

## 核心

1. **B3 CCV3 资产条目 id 修复**（Wave1，c1d310f）：`ccv3_adapter._asset_entries` 资产条目缺 `id` 字段，而 `export_schema.WORLD_ENTRY_KEYS` 把 id 列必填——含 asset_refs 的装配导出后 check_ccv3_world 必报「缺键: id」。RED 复现（`world 条目 1 缺键: id`）→ 修复 `_asset_entries` 加 `start` 参数续号（id/insertion_order 从模块条目数起连续编号，去固定 1000 魔法数）+ `world_entries` 衔接 → GREEN（268 单测全绿）。测试盲区定位：`test_export_schema._narrative_ir` 无 asset_refs 未覆盖，补 `_narrative_ir_with_assets` 用例。
2. **34 方案文档偏差收口**（Wave2）：34 方案「非目标」L29 宣称 parse_ccv3「标记占位非真实角色定义」，源码实际仅透传 chara_meta 无标记——修订表述对齐源码实际行为。

## 验证

- RED 复现：含 asset_refs 的 IR 导出 → validate_export 报「world 条目 1 缺键: id」（实测）；修复后 test_export_schema 22 用例全绿。
- 单测 267→268（+1 B3 回归用例）；全量 discover 268 OK。
- verify PASS=31 保持（check19/20/21/22 全绿）。

## 文件

- 修改 `desktop/src/core/ccv3_adapter.py`（_asset_entries start 参数 + id + world_entries 衔接）+ `desktop/tests/test_export_schema.py`（+fixture +用例）
- 修改 `34_v2.3.0_A4双向读入方案.md`（persona 表述修订）
- 本真相源 + README 协议链补 36 + CHANGELOG [Unreleased]

## 遗留

- B2 渐进披露拆分（需先量化 SKILL.md 是否真超 500 行/5000 token，留 v2.5 二期）。
- B5 ccv3 完整性 check（exporter 结构性保证 world 同次写入，风险低，价值待议）。
- 34 A4 遗留三项（persona 语义还原/skill 资源装载/读入登记）随 F2 端壳解冻后立项。
