# 18 · v2.0.x-SKILL 插件出口方案（AGENTS/SKILL/MCP 插件序列之一）

> **需求提炼**：2.0 基线导出格式优先级表——SKILL.md（Anthropic/Claude 生态）为 v2.0.x 插件二（表序④）。本插件落地「产物×出口适配矩阵」第二行：**SKILL 只接指令/协议类装配产物**（IR.type=techdoc，如 P90 技术文档装配），叙事类产物（narrative IR）请求 SKILL → 拒绝并说明（防语义错配——SKILL 是教 agent 干活的能力包，不是叙事内容容器）。
> **格式锚点**：Agent Skills 开放标准（agentskills.io，2026 实锤：Skill = 目录 + SKILL.md，YAML frontmatter name/description 必填，渐进披露 Discovery 载元数据）。
> **非目标**：AGENTS.md / MCP 插件（同序列后续）；SKILL 生态市场分发（Plugin marketplace，外部）。

## 1. 问题

- A1 适配矩阵第二行未落地：exporter 只挂 CCV3；SKILL 出口无实现，指令/协议类装配（P90 技术文档）无外部出口。
- A2 矩阵约束无机制执行：narrative IR 若被导成 SKILL = 语义错配（2.0 基线 §2 防错配），需在适配器层拒绝。

## 2. 改进收口

- B1 SKILL 适配器（A1）：`skill_adapter.py` 注册进 exporter._REGISTRY：
  - `export(ir, 'skill', dest_dir)`：校验 `ir.type == 'techdoc'`（矩阵约束），产出 `<dest>/<skill_dir>/SKILL.md`——frontmatter（name=IR title slug / description=管线摘要）+ 正文（各叙事层模块内容按层序串成操作说明结构）。
  - narrative IR → ExportResult.warnings + **不产文件**（语义错配拒绝，明确告知用 CCV3）。
- B2 契约测试（A2）：skill 单测——techdoc IR 产 SKILL.md（frontmatter name/description 在场，agentskills 格式）；narrative IR 被拒（0 文件 + warning 说明）。

## 3. 任务分解

- T1 skill_adapter.py + test（RED→GREEN）：SKILL.md 生成（YAML frontmatter + 正文）+ 矩阵拒绝分支。
- T2 exporter 注册 + 契约断言：skill 入 _REGISTRY；e2e/verify 断言 exporter 双格式注册表完整（ccv3+skill）。

## 4. 验证

- `python -m unittest discover -s tests` 全绿（新增 skill 用例）
- `bash verify.sh` PASS=24（check18 断言扩 skill 或维持——check18 跑 ccv3 测试；skill 测试入同门或 check 扩展，择 PASS 增长一致）
- e2e 不变（CCV3 主出口验证）；skill 由单测覆盖

## 5. 瑶光反证

- 断言：exporter._REGISTRY 可扩展（插件架构底座）——证据：本会话 exporter.py `_REGISTRY = {"ccv3": _export_ccv3}` + `export()` 查表分发 ✓。
- 断言：IR.type 区分 narrative/techdoc（矩阵约束执行依据）——证据：本会话 ir.py IRDocument.type + render_ir type 判定（structure_type=='techdoc'）✓。
- 断言：SKILL 格式 = 目录 SKILL.md + name/description frontmatter——agentskills.io 200 实锤（本会话早前抓取）✓。
- 待验证：P90 技术文档装配现实路径（真实 techdoc IR 样例）——单测构造 techdoc IR 覆盖，真实 P90 装配留后续战例。

## 6. 范围外

- AGENTS.md / MCP 适配器（本序列后续插件）；SKILL 生态分发接入。
