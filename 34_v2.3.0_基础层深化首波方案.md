# 34 · v2.3.0 基础层深化首波：A3 规则出口打通 + B2 变体组合 + 文档 retro-fit

> **需求提炼**（用户链式裁决）：「推进 v2.2.0 之后的下一波工作（§7.5 遗留 / §7.6 C/D 条件池，你给方向）」→ 三方向候选（A3 多目标渲染 / B2 变体组合 / 社区元模块边界段 retro-fit）→「挨个执行」→「先存着，继续推进下一版本再一并发布」（v2.2.0 后 3 补遗本地并入 main，随 v2.3 一并发布）。批准方案全文见 .rivet/plans/34-v2-3-0-基础层深化首波-a3-规则出口打通-b2-变体组合-文档-retro-fit.md。
> **非目标**：C/D 族条件池（触发冻结，ROADMAP §7.6）；A4 双向适配（🔒 待外部样例）；C-a 质量分级（无消费方）；版本化（协议层 V2）；端壳/GUI（L3_FROZEN）。

## 核心

§7.5 三方向全做，每波独立提交：

1. **A3 规则出口打通**（Wave1，3b132bb）：实测断链根因 = `render_ir` 不写 `IR.meta['doc_semantics']` → techdoc 装配（P90）被 classify_doc_semantics 回退 skill → agents/claude 出口拒出。修复 = doc_semantics 三层透传（render_ir 加参值域校验借 semantics 常量 → pipe() 透传 → nf run `--doc-semantics {project_rules,skill}`）。**P90 战例端到端**：`nf run P90 --fmt agents --doc-semantics project_rules` 产 AGENTS.md（P90 装配经 M00/M90/M80 锚点使 gate 过）。测试 +4（透传/classify/export/invalid 忽略），RED 基线 = 无声明 classify=skill 实测。
2. **B2 变体装配**（Wave2，ff73f2e）：新增 `desktop/src/core/variants.py`——`apply_variant`（base+add−remove 幂等变换，remove 优先）+ `variant_assemblies`（全变体展开 + 跨变体模块重叠仲裁报告，声明级不阻断）。裁决：尊重 composer.py 头部「非目标：不改组合声明结构」——变体不扩展 references schema，作装配层模板选择。nf run 加 `--variant-add/--variant-remove`（经 apply_variant 变换 selected）。测试 11 用例。冒烟：P04 `--variant-remove 轻混类:M92` → ccv3 正常导出 PASSED。
3. **B3 文档 retro-fit**（Wave3，0a1f00c）：实测修正（M97/M98 已有不变式遵守段）→ 真缺边界段的 M93/M96（通用核心包）补「## 6 不变式遵守（01 §5）」段——I1-I5 基于各模块真实语义（M93 节拍→快照收敛 / M96 叙事润色：三正交分离/核心固定/通信契约/数据隔离/真相唯一），不造假。verify check20 全绿保持。

## 验证

- verify.sh PASS=29 全绿（三次实测：Wave1 后 / Wave3 后，WARN=0 FAIL=0）。
- 单测 222→237（A3 +4 pipeline、B2 +11 variants）；全量 discover 237 OK。
- CLI 端到端：P90 `--fmt agents --doc-semantics project_rules` 产 AGENTS.md（A3 GREEN，计划待验证项闭环）；P04 `--variant-remove` 变体导出 ccv3 PASSED（B2）。
- py_compile 全改动文件 OK。

## 文件

- 修改 `desktop/src/core/generator.py`（render_ir doc_semantics 参数）+ `desktop/src/core/pipeline.py`（pipe 透传）+ `scripts/nf.py`（--doc-semantics + --variant-add/remove）+ `desktop/tests/test_pipeline.py`（+4）
- 新增 `desktop/src/core/variants.py` + `desktop/tests/test_variants.py`（B2，11 用例）
- 修改 `community/通用核心基础包/modules/M93_通用状态快照.md` + `M96_通用输出润色.md`（B3 补不变式段）
- 本真相源 + README 协议链补 34 + CHANGELOG [Unreleased] + ROADMAP §7.5 A3/B2 线切 ✅

## 遗留

- B2 边界声明：条件组合运行时判断（if/分支动态路由）留协议 V2/后续——本波 = 变体模板静态选择 + 重叠报告。
- A3 消费方：GUI 向导（protocol_wizard）未接 doc_semantics 声明 UI（CLI/库层已通，GUI 待 E4/L3 解冻）。
- v2.2.0 后 3 补遗（7f73da8/0b7c4e4/931f81e）已并入 main（931f81e），随 v2.3.0 发布一并推远程（网络恢复时）。
- C/D 族记 ROADMAP §7.6 条件池，触发条件见该节。
