# STRATEGY · NF 目标与质量准绳（2026-09-08 作者批准）

> **给谁**：任何在 NF 仓库立项、规划、开工、评审的 AI 协作者与维护者——**开工前必读**。
> **地位**：方向与裁决的最高层文件。阅读顺序：`AGENTS.md`（行为纪律）→ 本文件（方向与准绳）→ `40_未来计划总纲_*.md`（路线）→ 目标工程/收口文档。任何规划与本文件冲突时，以本文件为准。
> **性质**：本文件是公开仓库**唯一的方向层文件**。计划与过程细节不发布公开仓库（见 §四）。

---

## 一、NF 要实现什么

1. **NF = 内容契约层**：定义「AI 稳定产出内容」的协议、质量与资产标准——把长内容生产从不可验证的个体经验，变成可装载、可质检、可复现的工程。
2. **定内容，不定模型**：协议与资产跨模型、跨客户端、跨平台可移植；模型无关是底线——不依附任何上游模型厂商，不站队任何连接协议（MCP/A2A 等对接一切）。
3. **域中立**：长篇叙事是官方第一个域包（当前主战场与最完整战例），但协议本体不绑定任何内容域——techdoc 等非叙事域包已为先例；任何内容域可经同一套管线、质检门与资产机制生产。
4. **双层消费**：协议对 AI 可读、可执行、可机器核验；对人亦可读、可复述、可检查。任一 AI 拿到仓库即可按协议生产或装载，不依赖特定模型能力。

## 二、验收函数 = 质量（单变量）

- 决策函数只含**质量**一个变量；影响力、使用量、star、热度类指标显式排除——不当目标、不当参考、不进入任何立项与排期论证（作者 2026-09-08 拍板，42 工程定位同源）。
- 质量 = 五维纵深（完整定义与验收见 42 收口文档）：
  1. **静态可核验**——verify 门禁全绿是底线，不是顶尖；
  2. **动态可执行**——协议被 AI 真实执行时的保真（静态绿 ≠ 可执行，这是纵深主战场）；
  3. **架构纯度**——零私货、零冗余、层级分明；
  4. **资产密度**——内容资产的语义厚度与键质量；
  5. **文档可执行性**——操作指令阅读即能执行，非参考资料。
- 执行含义：不因「没人用」降标，不因「可能有人用」赶工；公开发布 = 质量展示事件，不是增长动作。

## 三、质量法官 = 内部验证（外部只借鉴，不背书，不裁判）

**3.1 合格判据只来自内部链**：verify check 族（只增不减、基线红线）→ audit 建档 → 五维自评 → 真实战例（fixtures/演练集）。任何「达标/不达标」结论只能由这条链产生、只引用内部证据。

**3.2 外部输入只有两种合法角色**：
- **环境实测类**（出口装载/兼容性实证）：接触纪律按作者裁决执行（当前处于封闭式发育期，外部接触冻结，重启由作者裁决）。
- **机制借鉴类**（行业做法、外部 AI 分析、开源项目机制）：仅作输入源——逐条对照真实仓库实证（已具备不重做、不适面剔除、实证记录留档），净吸收后方可进入标准集。

**3.3 三条禁令**：
1. **立项理由不得引用外部**——「某外部项目有 X / 某外部 AI 说缺 X」不构成开项依据；开项理由只能来自内部差距实证（对五维/协议资产/既有 check 的审计）。
2. **质量论证不得引用外部背书**——外部项目规模（star 等）、外部评价、外部 AI 的分析结论，均不作为 NF 工程质量合格与否的论据。
3. **文档引用外部限一句式**——公开文档提及外部项目只写「机制借鉴 + 实证判定」的结论，不展开推崇性叙事、不写对照流水。

**3.4 为什么**：NF 的质量标准由五维 + verify 自足定义；外部模板按大众项目语境设计，够不到 NF 的纵深口径。以外部为法官 = 标准漂移（把已具备能力当差距重做）+ 方向分心（每波去外部进货标准）。42 工程标准集已把该吸收的外部输入吸收完毕，内部标准体系自足，无需持续外求。

## 四、仓库发布边界（计划内部消化）

- **公开仓库只承载三类**：
  1. 方向层：本文件（STRATEGY）；
  2. 结果/状态：CHANGELOG 归档、README 版本块、audit 建档、收口文档、协议与代码资产（01–07、desktop/、scripts/、community/ 等正式件）；
  3. 面向使用者的文档与入口（AGENT_START/ROUTES/assembly 指令等）。
- **计划类文档不发布公开仓库**：立项规划、草案、标准集、工作日志、实证过程等计划期产物一律内部消化（本地密级档案），完成落地后以结果形态（代码 + 收口注记 + audit）入库。
- **协作者不得自行立项并新建规划文档推公开仓库**；新工程由作者在既有通道明确指示后开工，开工依据只能是内部差距审计（§3.3）。
- 存量公开规划文档（41/42/43）已于 2026-09-09 按作者裁决全撤（计划内部消化）；版本功能史保留于 CHANGELOG，对应实测/收口/审计文档已改述。

## 五、开工前置清单

1. 读 `AGENTS.md`（高危命令闸门/安全/执行纪律）→ 读本文件 → 读 `40_未来计划总纲_*.md` → 读目标文件。
2. verify 全绿是提交前提；check 只增不减；**门禁绿 ≠ 质量宣称**——产出附五维自评（改了什么维、水位到哪、差在哪）。
3. 公开文案纪律：仓库文档只写结果/状态；不写决策过程、不引作者原话、不写内部流水账。
4. 开工依据检查：本波要补的内部差距是什么？（答不出 = 不开工）

---

> **v1 修订记录（2026-09-08）**：作者批准建立本文件——战略内核公开化（目标/验收/法官/发布边界），配套「计划内部消化」发布纪律；战术细节留本地密级档案，不随仓库分发。
