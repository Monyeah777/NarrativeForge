# STRATEGY · NarrativeForge 项目方向（公开工程层）

> **定位**：公开层的「方向 + 质量验收 + 发布边界」单一入口，与 40 总纲（执行路线）互为表里。本文件只承载可公开的工程表述；迭代过程与战术细节不留公开仓。
> **维护**：作者拍板；变更随内容波收口归档。

## 1. 目标层定位

- 做**内容生产契约层**（中游公共标准）：定义「AI 稳定产出长内容」的协议 / 质量 / 资产标准。谁定义这套标准，谁掌握内容生产的中游；上游由模型方定义能力与接口，下游经任意 AI 触达终端用户。
- **模型无关是底线**：对接任意上游模型与连接协议（MCP/A2A 等），不站队、不依附；通过任意 AI 触达下游，不直营终端。
- 护城河一句话：**定义内容而非绑定模型**——内容是跨模型 / 跨平台 / 跨时代 / 跨厂商的资产。

## 2. 质量唯一论（验收函数）

- 验收**唯一变量 = 质量**；使用量 / 影响力 / star 类指标显式排除出决策函数（不当目标、不当参考）。
- 质量五维：① 静态可核验（verify 门禁，门禁绿 = 底线不是顶尖）；② 动态可执行（协议被 AI 执行时的真实保真，以端到端真实战例证）；③ 架构纯度（协议层零私货、层级分明）；④ 资产密度（结构化内容库键质量与语义厚度）；⑤ 文档可执行性（操作指令 ≠ 参考资料，阅读即能执行）。
- 内部质量法官：verify 门禁 + 端到端真实战例 + 作者主观裁定三源合议；不因「没人用」降标，不因「可能有人用」赶工。

## 3. 发布边界：封闭式发育 + 一次性完整外部验证

- **现状 = 封闭式发育期**：内部只做不露（协议 / 代码 / 资产 / 文档持续打磨）；公开仓库照常同步（技术资产展示 + 结果/状态）。**开源文档 ≠ 产品交付**——亮相另有时机。
- **外部接触纪律**：外部实测线（E1/E2/Y5/B1 触发实测等）一律封存挂账；重启触发 = 作者判断内部质量到位，届时执行**一次性完整外部验证**（无承诺实测通过）后再谈亮相/发布。
- 默认可交付参考线：verify 全绿 + 端到端真实战例 + 一次外部无承诺实测通过（防止完美主义无限延迟）。

## 4. 公开面纪律

- 公开仓库只写**结果/状态**（CHANGELOG / README 版本块 / audit / 收口文档）+ 正式协议代码；不承载作者拍板原话、AI 协作工作流细节、个人生活信息、内部流水账。
- **计划类文档（规划/草案/标准集/工作日志）留本地工作区（.rivet，不入库、不同步）**；不再以公开规划文件方式立项。存量公开规划（41/42/43）为历史真相源，处置待作者裁决。
- **协议层零私货**：协议正文只承载可机器核验的固定结构（硬门）；可变物（风格/偏好/DNA/自定义机制）档位化、移资产层，不得写入协议正文。
