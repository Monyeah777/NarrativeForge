# lint job only (pen-test close-out F-05, 2026-09-24).
#
# Kept separate so the lint job does not install PySide6 (~150MB) just to run a
# static checker. Versions come from the PyPI JSON API as in requirements-ci.txt.
# ASCII-only on purpose: pip parses requirements files with the locale encoding.
ruff==0.16.8
