# 32 · v2.1.0-C-b techdoc 域包战例：扩展新领域三步完整示范（增强模块模式）

> **需求提炼**（用户链式裁决）：「C方向」「C-b techdoc 域包战例」「转向增强模块模式：techdoc 域包自带 M9x 新题材模块（如 M97 术语管理/M98 修订记录，落 M91-99）+ 派生管线引用 M90 底层——真正示范『扩展新领域三步』」「归类通用包路径：techdoc 域包不进 DOMAIN，categories=[技术文档] 仅与其他社区包无交集即合规」。批准方案全文见 .rivet/plans/32-v2-1-0-c-b-techdoc-域包战例-扩展新领域三步完整示范-增强模块模式.md。
> **非目标**：壳包分发（官方 P90 链——§9 明示不纳入社区投影）；动官方 M90/P90（source 保持核心(P90 实证样例)）；C 线版本化（B4 遗留）；C-a 质量分级（无消费方）。

## 核心

ROADMAP C 线「techdoc 域 = 新领域三步自然延伸」战例缺口：现有 4 包全叙事题材，无一示范从零扩展非叙事新领域。本方案建 **community/ 第 5 包「技术文档域包」**（第一个非叙事题材域包），同时 29-31 工具链（projection/register/market）首次实战消费新领域包完整链路。

1. **verify.sh check14 ③ R2 扩域**（Wave1，独立提交 25be1f3）：从 DOMAIN 两领域包扩为 ALL_PKGS 全两两——techdoc 域包自带模块落 M91-99 社区段不进 DOMAIN（DOMAIN 语义 = 自带模块落官方段），但其独占类别仍须与其他社区包无交集。现有 4 包类别两两空交集，扩域 PASS=24 不误报。
2. **技术文档域包素材**：protocol.yaml（id=技术文档域包、pipeline=P06、module_id_range=[M97,M98]、categories=[技术文档]、core_modules 13 件含 M90、references=[]）+ modules/M97 术语管理 + M98 修订记录（P40/P60 位，对齐 M90 格式 machine_contract）+ pipelines/P06_技术文档题材装配流（派生 P00 骨架 + M90 core 协作）+ README（双源一致）。
3. **登记链路（工具链实战）**：02 §8 登记段（8.3 内「技术文档题材域包登记实况」）→ `nf register --apply` 写 registry protocols[] 4→5 条（30 方案首次对新领域包实战）→ `nf market info` 输出在册/无跨包/无冲突。

## 验证

- verify.sh PASS=24 全绿（check14 ⑦ 条数断言 5==5 + 元素级逐条 5 包全过——PASS 计数是 check 行数非包数，规划「24→25」措辞误差已记录偏离）；单测 184 OK（test_registry_sync 锚点改动态 N 条 + test_protocol_projection with-open）。
- nf register --check 拒绝（② 未在册 exit 2）→ 02 登记后 --apply 成功 5 条——工具链三要件拦截与放行实证。
- nf market info 技术文档域包：登记在册 + 无跨包引用 + 挂载冲突无。
- M97/M98 编号空闲实证（M91-M96 被轻混/通用占）；P06 不撞 P00-P90；categories=[技术文档] 与官方 M90 类别在 check14 ③/④ 无交互（③ 查 ALL_PKGS 社区包间、④ 查 core_modules ⊆ OFFICIAL13）。

## 文件

- 修改 `verify.sh`（③ 扩域，25be1f3）+ `02_联动注册表.md`（§8 登记段）+ `desktop/src/core/registry.json`（protocols[] 第 5 条）
- 新增 `community/技术文档域包/`（protocol.yaml + modules/M97_术语管理.md + modules/M98_修订记录.md + pipelines/P06_*.md + README.md）
- 修改 `desktop/tests/test_registry_sync.py`（动态 N 条锚点）+ `desktop/tests/test_protocol_projection.py`（with-open）
- 本真相源 + README 协议链补 32 + CHANGELOG [Unreleased] + ROADMAP C 线切 ✅

## 遗留

- C 线其余：版本化（B4 遗留）、模块质量分级（C-a 待消费方）、题材域广度继续扩展（本包为首个非叙事战例模板）。
- M97/M98 模块正文为结构性占位（machine_contract + 职责伪代码），真实题材内容深度留后续战例迭代（同既有包模块以文档承载的定位）。
