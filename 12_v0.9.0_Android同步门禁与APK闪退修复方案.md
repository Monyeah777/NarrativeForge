# NarrativeForge · v0.9.0 Android 同步门禁 + APK 闪退修复方案（落盘执行版）
> **版本定位**：v0.9.0「Android 同步门禁 + APK 闪退修复——把 sync --check 差异校验接入 CI（缺口⑦闭环：Android↔真源一致性不再靠人工）；对 APK 闪退修复做验证归档 + 纳入 CI 回归门禁保障」
> **基线**：v0.8.0 已发布（tag v0.8.0=`eee7704` 收口 commit）+ 工作区干净（HEAD=`eee7704`，与 origin/main 完全同步）
> **文档状态**：本文件为 v0.9.0 方案落盘真相源（I5）；取证时间 2026-09-04，基线 `eee7704`
> **编号声明**：方案按「问题 A → 改进 B → 任务 T」三层自洽编号。硬锚点与 ROADMAP §1 行 v0.9.0（「APK 闪退修复 + Android↔真源同步差异入 CI」）及 §6 一句话目标对应：A1 闪退修复已含 v0.8.0 历史但无 CI 回归保障、A2 CI 同步步骤无 --check 自证闸门（缺口⑦）、A3 三件套版本状态滞后于 git 事实（v0.8.0 已发布但 ROADMAP/README/CHANGELOG 未收口）；B1–B3 一一对应；T1 UI 启动安全静态断言入冒烟（selftest_android.py，无前置）→ T2 CI 同步步骤追加 --check 自证闸门（build-android.yml，无前置，与 T1 可并行）→ T3 版本语义收口核验（依赖 T1/T2）。**关键前提（git merge-base 实证）**：APK 闪退三处修复代码（MDRadioButton→MDCheckbox / MDTopAppBar 去 subtitle / CJK 字体注册）均已含于 v0.8.0 历史 commit（`6b9445e` 2026-09-03、`e92f119` 2026-09-04），本版对闪退部分定位 = **验证修复已含历史版本 + 归档根因链 + 纳入 CI 回归门禁保障**，非新写修复代码。v1.0.0 的 desktop/android 双端生态收口本版不做。
---
## 0. 定位与边界
- **做什么**（三做）：全部改动落在 **CI 门禁与验证归档（build-android.yml 同步步骤追加 --check 自证闸门 / selftest_android.py 增 UI 启动安全静态断言 / ROADMAP §6 + README 协议链 + CHANGELOG 版本收口）** 三类。R 语义：android/app/{core,seed} 保持 .gitignore 生成物不入库（git ls-files 实证 = 0），CI 中 `scripts/sync_android.sh` 仍是其生成唯一途径；`--check` 模式本体由 v0.6.0 T3/A3 交付（只读 diff、有差异 exit 1），本版仅将其**接入 CI 步骤序列**并补 UI 修复点回归断言，不动 sync 脚本本体、不改 android 应用代码（修复代码已在 v0.8.0 历史落地）、不改协议层 01–07 与 verify.sh 门禁（check1–15 全绿铁律保持，PASS=20 不变）。
- **不做什么**：不新写 APK 闪退修复代码（修复已含 v0.8.0，见 A1 实证；重写 = 版本语义倒退）；不做端到端测试入 CI（真机/模拟器启动 UI 自动化留范围外，延续 v0.5.0–v0.8.0 四版三不做——APK 装机启动验证仍靠真机手动验收）；不动 sync_android.sh --check 本体（v0.6.0 已交付并验证，接入层零改动复用）；不动 buildozer/p4a 构建链与三补丁（p4a_disable_grp / pep517_setuptools / noisolation_wheel——v0.8.0 已验证的构建组合，本版不触碰）；不做 Android 功能扩展（= 明确不在本版，同历版三不做）；不把 android/app/{core,seed} 强制入库（保持 .gitignore 生成物地位，CI sync 为唯一生成途径的设计不变）。
- **护航不变式**：I1 三正交分离 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一——全部不受动（本版无协议层/数据层改动）；平台规则 R1–R3 不受动。**本版受动声明**：无不变式扩展；仅 CI 门禁强度提升（sync 后自证 + 冒烟断言增补）与文档状态收口。
- **改动范围**（文件清单）：`12_v0.9.0_Android同步门禁与APK闪退修复方案.md`（新建，方案真相源）、`ROADMAP.md`（§1 L14 v0.9.0 状态 🔮→🔵 当前主线 + §6 范围细化落盘）、`README.md`（L32 协议链追加 12 + v0.8.0 块引用切 ✅ 已发布 + 新增 v0.9.0 🔵 块引用）、`CHANGELOG.md`（[Unreleased] v0.8.0 段归档 [0.8.0]——治理指针 3 发布归档收口遗留 + 新建 [Unreleased] v0.9.0 段）、`scripts/selftest_android.py`（T1：增 UI 启动安全静态断言段 + 头部注释）、`.github/workflows/build-android.yml`（T2：同步步骤后追加 `sync_android.sh --check` 自证步骤 + 头部注释补 v0.9.0 接入声明）。
---
## 1. 问题全景（A1–A3，实测取证）
### A1 APK 闪退修复已含于 v0.8.0 历史但无 CI 回归保障——修复代码散落 android 提交史，冒烟门禁不覆盖 UI 层修复点，未来 KivyMD 再 bump 或修复代码回退，CI 依旧全绿
android 侧启动闪退根因共三处，修复代码**均已含于 v0.8.0 tag**（`git merge-base --is-ancestor 6b9445e v0.8.0` 与 `--is-ancestor e92f119 v0.8.0` 均 exit 0 实证）：①`6b9445e`（2026-09-03）KivyMD 1.2.0 两处 API 变更启动崩溃——screens.py `RadioRow` 旧代码 `from kivymd.uix.selectioncontrol import MDRadioButton` 抛 ImportError（1.2.0 已移除 MDRadioButton，全包零引用，selectioncontrol 仅导出 MDCheckbox/MDSwitch/Thumb），main.py `MDTopAppBar` 旧代码传 `subtitle=` kwarg 抛 TypeError（1.2.0 无 subtitle 属性，未知 kwarg 在 EventDispatcher.__init__ 即崩）；②`7fd6c0f`（2026-09-04）chore bump v0.3.2；③`e92f119`（2026-09-04）CJK 字体注册（`_register_cjk_fonts()` 用 NotoSansSC-Regular.otf 覆盖 KivyMD Roboto* 名称，build() 首行调用）。修复链时间线：`6b9445e`（09-03）→ `7fd6c0f`（09-04 bump）→ `e92f119`（09-04 字体）→ `d4b8522`（registry_loader 接入）→ … → v0.8.0（`eee7704`）。**问题**：CI 全流程（checkout → sync → `python3 scripts/selftest_android.py` 冒烟 → JDK17 → buildozer → release）中冒烟仅覆盖**控制器纯逻辑**（种子自举 → 幂等跳过 → 管线选择 → 按层自动勾选 → 装配检查 → 生成文档 → 保存 → 模块导入/勾选/删除，纯 python3 无 Kivy 依赖），**不触碰 UI 层**（screens.py/main.py 含 KivyMD import，裸 python3 无法导入执行）——三处修复点（MDRadioButton 引用 / MDTopAppBar 无 subtitle / CJK 字体注册调用）在 CI 中**无任何断言在场**：若未来 KivyMD bump 再出 API 变更类闪退、或有人回退 `6b9445e`/`e92f119`，CI 冒烟依旧全绿、构建照常出包，闪退回归**无闸可拦**。
### A2 CI 同步步骤不带 --check（缺口⑦）——core/seed 不入库故 CI sync 是生成唯一途径，Android↔真源一致性全靠人工纪律，无自动闸
android/app/{core,seed} 为 .gitignore 生成物不入库（`git ls-files android/app/core | wc -l` = 0、`git ls-files android/app/seed | wc -l` = 0 实证；android/.gitignore = `core/` + `seed/` + `.buildozer/` + `bin/` + `__pycache__/` + `*.pyc`），实存 core 10 个 + seed 20 个文件均为本地 `scripts/sync_android.sh` 生成物——**CI 中 sync 步骤是 android/app/{core,seed} 在场的唯一途径**（build-android.yml 头部注释明示「必须先在 CI 中执行 sync 从仓库根单一事实源同步，再跑逻辑冒烟自测」）。现状 .github/workflows/build-android.yml 同步步骤 = `run: bash scripts/sync_android.sh`（**不带 --check**）——语义为 rm -rf + cp 无条件覆盖：若仓库根单一事实源（desktop/src/core、03_管线库、04_模块库、05_资产库）与 android 生成物之间发生漂移（例如本地改 desktop/src/core 后未重跑 sync 便 push——CONTRIBUTING §3 步骤③纪律只靠人读；或源目录结构变化致 sync 覆盖不全），CI 会**静默用源覆盖生成物后照常构建**，漂移无任何自动拦截。sync_android.sh `--check` 只读差异模式本体（core 段 `diff -rq desktop/src/core android/app/core` 排除 __pycache__ + seed 段逐目录 diff -rq；生成物缺失明确提示先 sync 不误报；有差异 exit 1、无差异 exit 0）已由 v0.6.0 T3/A3 交付，头部注释明示「**接入 CI 归 v0.9.0**，本版交付模式本体」——模式本体就绪、接入缺位 = 缺口⑦闭环未合。
### A3 三件套版本状态滞后于 git 事实——v0.8.0 已发布（tag 在场）但 CHANGELOG 未归档、README 块引用未切、ROADMAP §6 仅一句话，v0.9.0 版本语义（「修复闪退」）与代码事实（修复已含 v0.8.0）无文档厘清
git 事实：HEAD = `eee7704`（tag v0.8.0，五引用同指），v0.8.0 已发布收口。但三件套状态全面滞后：①**CHANGELOG.md** `[Unreleased]` 仍为 v0.8.0 段（「## [Unreleased] - v0.8.0 自定义模块组合（开发中）」），治理指针 3（发布即归档 [版本号]+日期段）在 v0.8.0 收口时未执行——发布遗留缺口；②**README.md** L33 块引用 v0.8.0 仍标「🔵 当前主线·开发中」（实际已发布 ✅），L32 协议链止于 11；③**ROADMAP.md** §1 L14 v0.9.0 状态 🔮 规划、方案真相源「—」待填，§6 仅一句话（「修复 Android 端 APK 闪退问题；把 sync --check 差异校验接入 CI」）无范围边界/三不做/前置说明——且该句话承诺的「修复闪退」代码实际已在 v0.8.0 历史落地（A1 实证），**版本承诺与代码归属之间无一处文档厘清**（哪次闪退/根因/修复 commit/归属哪个版本），v0.9.0 立项前须先做版本状态收口与语义厘清，否则后续 CHANGELOG 记录将建立在错误的版本段上。
---
## 2. 改进收口（B1–B3，与 A1–A3 对应）
### B1 闪退修复验证归档 + UI 启动安全静态断言入冒烟（A1/B1）：selftest_android.py 增静态断言段，修复点回归有 CI 闸门
- **验证归档（文档层，12 方案 §1 已含实证）**：git merge-base 确认 `6b9445e`/`e92f119` 均含于 v0.8.0（exit 0）——修复代码已随 v0.8.0 发布，非 v0.9.0 新修；三处修复根因/commit/归属版本对照表（§1 A1）即归档实体，随 12 方案落盘进入真相源体系。
- **回归保障（代码层，T1）**：scripts/selftest_android.py 在控制器纯逻辑链路后追加「UI 启动安全静态断言」段——纯 python3 文件读取断言（不 import Kivy，规避裸环境无 KivyMD 依赖）：①`android/app/ui/screens.py` 不含 `from kivymd.uix.selectioncontrol import MDRadioButton`（修复点 1：ImportError 根因，含该行即 FAIL）；②`android/main.py` 的 `MDTopAppBar(` 调用不含 `subtitle=` kwarg（修复点 2：TypeError 根因）；③`android/main.py` 存在 `_register_cjk_fonts` 定义且 build() 首行调用在场（修复点 3：CJK 字体注册，定义缺失/调用不在首行即 FAIL）。三断言 FAIL 任一 = CI 冒烟步骤 exit 1 fail——闪退修复点从此有自动回归闸门，KivyMD 再 bump 或修复代码回退即被拦截。
### B2 CI 同步步骤追加 --check 自证闸门（A2/B2）：build-android.yml 同步后新增 `sync_android.sh --check` 步骤，缺口⑦闭环
- **改造要点（fresh checkout 边界已厘清）**：CI 为全新 checkout（core/seed 不入库故不在场），若把现有同步步骤直接替换为 `--check`，会命中 sync_android.sh 的「生成物缺失」边界（提示先 sync 并 exit 1）导致 CI 必然失败——**正确改造 = 保留现有 `bash scripts/sync_android.sh` 生成步骤，在其后追加一步 `bash scripts/sync_android.sh --check` 自证**：先生成（rm -rf + cp 覆盖，幂等）、后只读 diff 自证「本次构建使用的 core/seed 与仓库根单一事实源双端一致」。
- **闸门语义**：--check 有差异（含源与生成物内容差异/新增/缺失）输出差异清单并 exit 1 → CI fail；无差异 exit 0 → 继续冒烟与构建。把 CONTRIBUTING §3 步骤③的「改 desktop/src/core 后必跑 sync」人读纪律升级为 CI 可执行断言：**任何导致 Android↔真源漂移的提交（未同步、同步不全、误入库生成物等）在构建前即被自动拦截**——缺口⑦（Android↔真源一致性不再靠人工）闭环。
- sync_android.sh --check 模式本体零改动复用（v0.6.0 已交付，95 行只读 diff 已验证）。
### B3 版本状态三件套收口 + v0.9.0 语义厘清（A3/B3）：C0 立项 commit 一次性收口发布遗留并立 v0.9.0 段
- **CHANGELOG**：`[Unreleased]` v0.8.0 段归档为 `## [0.8.0] - 2026-09-04（v0.8.0 自定义模块组合）`（治理指针 3 收口 v0.8.0 发布遗留，原段内容原样迁入）；新建 `## [Unreleased] - v0.9.0 Android 同步门禁 + APK 闪退修复（开发中）` 并回填「12 方案落盘」条目（A1–A3/B1–B3/T1–T3 概要 + 修复链验证归档声明）。
- **ROADMAP**：§1 L14 v0.9.0 状态 🔮 规划 → 🔵 当前主线、方案真相源「—」→「12_v0.9.0_Android同步门禁与APK闪退修复方案.md」；§6 一句话展开为完整范围边界（✅ 三做：闪退修复验证归档 + 回归断言入冒烟 / sync --check 接入 CI / 三件套收口；❌ 三不做：端到端测试入 CI 范围外 / Android 功能扩展 / 协议层与 verify.sh 门禁改动；前置：v0.6.0 已交付 --check 模式本体、修复代码已含 v0.8.0）。
- **README**：L32 协议链追加 12；v0.8.0 块引用（L33）状态「🔵 当前主线·开发中」→「✅ 已发布 2026-09-04」；新增 v0.9.0 🔵 当前主线块引用（含范围概要 + 方案真相源 12）。
---
## 3. 任务分解（T1–T3，依赖序）
- **T1（A1/B1，无前置）UI 启动安全静态断言入冒烟**：scripts/selftest_android.py 追加静态断言段（三断言：无 MDRadioButton import / MDTopAppBar 无 subtitle kwarg / _register_cjk_fonts 定义 + build() 首行调用）+ 头部注释更新。独立可并行。
- **T2（A2/B2，无前置）CI 同步步骤追加 --check 自证闸门**：.github/workflows/build-android.yml 同步步骤后新增 `run: bash scripts/sync_android.sh --check` 步骤 + 头部注释补 v0.9.0 接入声明。独立可并行（与 T1 无文件交集）。
- **T3（A3/B3，依赖 T1/T2）版本语义收口核验 + CHANGELOG 实施条目补记**：C0 已完成三件套主体切换（B3 主体），本任务在 T1/T2 落地后补记 [Unreleased] v0.9.0 段实施条目（selftest 静态断言 / CI --check 闸门）+ 五项一致性核验（git 事实 tag v0.8.0 在场 ↔ CHANGELOG [0.8.0] 已归档 ↔ ROADMAP L14 🔵 当前主线 ↔ README 块引用 ✅ v0.8.0/🔵 v0.9.0 ↔ 12 方案在场）。依赖 T1/T2 完成。
---
## 4. 任务明细
### T1 UI 启动安全静态断言入冒烟（A1/B1）
- scripts/selftest_android.py：在现有控制器全链路自测（种子自举 → 幂等跳过 → 管线选择 → 按层自动勾选 → 装配检查 → 生成文档 → 保存 → 模块导入/勾选/删除）之后追加「UI 启动安全静态断言」段（纯 python3 文件读取，不 import KivyMD）：①读 `android/app/ui/screens.py` 全文，断言不含 `from kivymd.uix.selectioncontrol import MDRadioButton`（KivyMD 1.2.0 已移除该类，含即 ImportError 闪退根因——FAIL）；②读 `android/main.py`，断言所有 `MDTopAppBar(` 调用行不含 `subtitle=`（1.2.0 无 subtitle 属性，未知 kwarg → EventDispatcher.__init__ TypeError 闪退根因——FAIL）；③读 `android/main.py`，断言 `_register_cjk_fonts` 定义在场且 build() 函数体内首条语句为对其调用（CJK 字体注册必须在任何控件创建/首次绘制前，build() 开头第一行——缺失/移位即 FAIL）。任一段 FAIL = 冒烟 exit 1。
- 头部注释更新：标注「控制器纯逻辑自测 + UI 启动安全静态断言（v0.9.0 T1/B1：闪退修复点回归保障，对应 12 方案）」。
- 验证：`python3 scripts/selftest_android.py` 全绿（前置已 sync）；`python3 -m compileall -q scripts` 语法通过。
### T2 CI 同步步骤追加 --check 自证闸门（A2/B2）
- .github/workflows/build-android.yml：在现有「同步共享源码与种子数据」步骤（`run: bash scripts/sync_android.sh`）之后**追加**一步——`- name: 双端一致性自证闸门（sync --check，缺口⑦）` + `run: bash scripts/sync_android.sh --check`（先生成后自证：生成物缺失边界已由 sync 前置规避；有差异 exit 1 fail CI、无差异继续冒烟）。
- 头部注释同步：补「v0.9.0 起 sync 后追加 --check 自证——Android 生成物与仓库根单一事实源漂移即 fail（缺口⑦闭环）」。
- 验证：CI 全流程（push main 触发）绿——sync 生成 → --check 通过 → selftest 冒烟（含 T1 静态断言）→ JDK17/buildozer 构建 APK 成功。
### T3 版本语义收口核验 + CHANGELOG 实施条目补记（A3/B3）
- CHANGELOG.md：确认 [Unreleased] v0.9.0 段（C0 已建）在 T1/T2 落地后补记两条 Added（selftest UI 启动安全静态断言 / CI sync --check 自证闸门）。
- 五项一致性核验（全部在场方为 T3 完成）：①git 事实——tag v0.8.0 在场（`git tag -l v0.8.0`）②CHANGELOG [0.8.0] 段已归档 ③ROADMAP §1 L14 v0.9.0 = 🔵 当前主线 + 真相源 12 ④README 块引用 v0.8.0 = ✅ 已发布、v0.9.0 = 🔵 当前主线 + L32 协议链含 12 ⑤12 方案在场且 §0–§6 完整。
- 验证：verify.sh 全绿（check1–15 PASS=20，0 WARN 0 FAIL——本版无协议层改动，铁律保持）；`bash scripts/sync_android.sh --check` 本地通过（双端一致自证）；工作区干净。
---
## 5. 提交规划
| 序 | 提交内容 | 前缀 | 关联 |
| --- | --- | --- | --- |
| C0 | 12 方案落盘 + ROADMAP §1 L14 v0.9.0 状态 🔮→🔵 当前主线/真相源填 12 + §6 范围细化落盘 + README L32 协议链追加 12/v0.8.0 块引用切 ✅ 已发布/新增 v0.9.0 🔵 块引用 + CHANGELOG [Unreleased] v0.8.0 段归档 [0.8.0]（治理指针 3 收口）+ 新建 [Unreleased] v0.9.0 段回填 | docs(roadmap) | 本文件 |
| C1 | T1 scripts/selftest_android.py 增 UI 启动安全静态断言段（三断言）+ 头部注释 | feat(android) | A1/B1 |
| C2 | T2 .github/workflows/build-android.yml 同步步骤后追加 sync --check 自证闸门步骤 + 头部注释 | chore(ci) | A2/B2 |
| C3 | T3 CHANGELOG [Unreleased] v0.9.0 补记 T1/T2 条目 + 五项一致性核验 + verify 全绿（PASS=20）+ tag v0.9.0 收口 + push | chore(release) | 收口 |
每任务独立 commit；C1/C2 期间工作区只允许该任务相关文件变更，避免交叉。C3 push 前须向用户索取新 PAT 并 revoke 旧 token（见 §6 开放问题 6）。注：本版无协议层 Schema/门禁改动（verify.sh 不动），提交步数较 v0.8.0 压缩为 C0–C3；每批次独立 commit + 门禁全绿铁律（CONTRIBUTING §3 四步：verify.sh / unittest / sync_android.sh / py_compile）不变。
---
## 6. 风险与开放问题
1. **静态断言 ≠ 真实启动验证**：T1 三断言为 UI 修复点的文件级静态回归（MDRadioButton 引用在场与否 / subtitle kwarg 在场与否 / CJK 注册调用在场与否），能拦截「修复代码回退」与「同点再犯」，但**不覆盖未来 KivyMD 新 bump 的未知 API 变更类闪退**（新崩溃点静态断言未知，需人工验收后迭代补断言）。真机/模拟器端到端启动验证（Activity onCreate → main.py build() → 五屏渲染）仍靠 APK 装机手动验收，入 CI 留范围外（同缺口⑩脉络）。
2. **--check 闸门与本地工作流摩擦**：开发者改 desktop/src/core 或 03/04/05 后未重跑 `bash scripts/sync_android.sh` 直接 push，CI --check 将 fail（源与仓库内无生成物——fresh checkout 下生成物不在场，check 前必须 sync，故闸门步骤紧跟生成步骤之后）。此即闸门预期行为：把人读纪律（CONTRIBUTING §3 步骤③）升级为 CI 强制；本地修复 = 先 sync 再 push，无歧义。
3. **sync --check 对源目录结构性变化的覆盖**：--check 的 core 段 diff 排除 __pycache__、seed 段逐目录 diff——若源新增目录层级而 sync 的 cp 路径未覆盖（结构性失配），diff 会以「Only in 源」差异形式暴露并 exit 1（新增未同步 = 差异），可被捕获；若源目录被删除而生成物残留，diff 以「Only in 生成物」暴露。覆盖完备性沿用 v0.6.0 已验证实现，本版不扩展。
4. **CI 构建时长与闸门位置**：--check 为只读 diff（毫秒级），置于 sync 后、selftest 前，不改变构建时长量级；若未来 CI 引入 core/seed 缓存复用（当前仅缓存 ~/.buildozer），--check 将承担「缓存生成物 ↔ 当前源」一致性校验，闸门语义自动扩展，无需改造。
5. **CHANGELOG v0.8.0 归档为治理收口动作**：v0.8.0 发布时未执行治理指针 3（归档），本版 C0 补归档（[0.8.0] - 2026-09-04）——属发布遗留收口非回溯改写（原 [Unreleased] v0.8.0 段内容原样迁入，不增删改），不违反「禁止在已发布 tag 上回写」（tag 本身不动，仅 CHANGELOG 文档状态归位）。
6. **推送安全（操作层）**：C3 push 前须向用户索取新 PAT 并 revoke 旧 token（沿用历版流程）；C0–C2 本地 commit 不受影响。origin remote 走 SSH 无密钥，推送用一次性 HTTPS URL + PAT 不写入 config；推送成功后因 fetch 失败致 ref 不更新时，以 `git ls-remote` 核对远端 SHA == 本地 HEAD + `git update-ref` 手动同步（历史明文 token 建议用户 revoke）。
7. **verify.sh 无感知改动**：本版改动文件（build-android.yml / selftest_android.py / 三件套文档）均不在 verify.sh 检查域内（check1–15 覆盖协议层/代码层 unittest/社区包结构），PASS 保持 20 不变；selftest_android.py 改动经 py_compile（CONTRIBUTING §3 步骤④）兜底语法。铁律「0 WARN 0 FAIL 方可提交」不受影响。
8. **v0.9.0 交付锚点核验**：ROADMAP §1 L14 v0.9.0 状态切「🔵 当前主线」+ 方案真相源落 12 文档 + build-android.yml sync --check 自证闸门在场（缺口⑦闭环）+ selftest_android.py UI 启动安全静态断言在场 + CHANGELOG [0.8.0] 归档/[Unreleased] v0.9.0 段在场——五项齐备且 verify.sh（check1–15）全绿方可进入 v1.0.0 立项。
