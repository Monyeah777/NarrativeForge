# DEEP_DIVE · NF 心智模型（给 AI 的深刻理解层）

> 最后更新：2026-09-09
> 位置：AI 入口的“理解层”；读它不是为了会操作，而是为了懂 NF 为什么这样设计。操作路径见 `AGENT_START.md` / `AI_ROUTING.md`。

## §1 本质

NF 是**内容契约层**，不是叙事生成器。它定义“AI 稳定产出长内容”的协议、质量与资产标准：定内容，不定模型。叙事只是官方第一个域包（P02/P03 最成熟），协议本体域中立。

三层心智模型：

- 协议层：01-07 定义结构、契约、注册表、执行协议——所有零件长什么样。
- 资产层：community 域包与 05 资产库提供数据/知识本体；资产键经五接口访问，模块不直读散文件。
- 执行层：desktop core、CLI、MCP、drill/verify 让协议可装载、可质检、可复现。

## §2 质量观

决策函数只有一个变量：质量。使用量、star、影响力不算目标。内部法官链是 `verify → audit → drill → 真实战例`；外部只作机制借鉴，不作为质量背书。

门禁绿不是顶尖，只是底线。动态可执行比静态可核验更深：文本说对不算对，AI 真按协议做对才算。

## §3 结构地图

| 件 | 管什么 | 谁消费 |
|---|---|---|
| 01 核心协议 | 模块/管线/资产/注册表契约 | 装配师、verify、runtime |
| 02 联动注册表 | 模块/挂载/订阅/依赖真源 | check13/16/24 |
| 03-05 | 管线/模块/资产库 | 装配器、资产五接口 |
| 06 Agent执行协议 | 运行时怎么装载 | 执行 agent |
| 07 官方导航 | 预设/目录/入口 | 人 + AI |
| protocol/WORLD_MODEL.md + world_slots.json | 确定性抽象状态契约与 M00 槽位投影 | check32、WorldModelRuntime、nf worldmodel |
| STRATEGY | 方向与裁决准绳 | 所有协作者 |
| desktop/core | 逻辑本体 | CLI/MCP/壳 |
| community | 领域包内容 | 装配器 |
| library | 云端成品馆 | 任意 AI |

## §4 运行心智

AI 消费 NF 的完整循环：**读 → 判 → 配 → 装 → 执 → 验**。读入口（AGENT_START/组装指令包）→ 判需求（nf assemble 澄清/预设/自定义）→ 配模块管线资产 → 装成完整版 → 执行/生成 → 用 `nf assemble --check`、`verify`、drill 验收。对带 `world_model` 的调度模块，可用 `nf worldmodel --run [--state STATE.json]` 验证其抽象状态相位图、槽位映射与轨迹摘要；每一步都要引用真实件，禁止凭记忆。

## §5 纪律红线

零编造：编号/资产键/依赖拿不准就留空并标注。计划不外发：计划/草案留本地，公开仓只放结果、状态、正式协议代码。引用不等于背书：外部项目/工具是候选建议，不是 NF 承诺。

## §6 深读路径

先读 STRATEGY → AGENT_START → 01 → 02 → 06 → 07；需要操作再读 `agent_组装指令包_v0.2.md`、`docs/mcp.md`、`AI_ROUTING.md`。看每个文件时：读“是什么”和“怎么被校验”，不逐行背方案正文。

## §7 自检十问

1 NF 是什么？2 为什么叙事不是本体？3 什么是三正交？4 什么算质量？5 内部法官是谁？6 模块/管线/资产怎么交互？7 AI 消费完整循环是什么？8 哪些不能编造？9 计划文档去哪？10 外部链接/工具意味着什么？读后能用自己的话答出 = 真懂。
