.DS_Store
*.swp
*.tmp
build/
dist/
__pycache__/
*.pyc

# 工具本地缓存（mypy / pylint / pytest 等；可再生，不入仓、不进门禁扫描）
.mypy_cache/
.pylint.d/
.pytest_cache/

# 本地 AI 协作工作区（天枢运行时状态与项目标记，不入库）
.rivet/
.rivet.md

.coverage

# 本地模型权重与运行环境（决策层候选；体积 GB 级，绝不入仓）
checkpoints/
*.safetensors
*.gguf
