# 17 · v2.0.0 导出层立项：CCV3 首发出口 + 插件架构（2.0 E1 收口）

> **需求提炼（用户原话）**：「2.0 = 所有 AI 协议标准的上游生成器」「默认交付 CCV3 出口（SillyTavern/Chub 当前唯一有现成叙事价值闭环）」「验收：校园×西幻组合包：GUI 装配 → 质检 → 导出 .card/.world → SillyTavern 实际可玩——战例不过不收口」。
> **前置已备**：v1.2 IR（内容归一化）→ v1.3 检索 → v1.4 质检门 + check17（E0 三件套，本分支已交付）。本版在 E0 之上实现导出层主命题。
> **非目标**：AGENTS.md/SKILL.md/MCP 插件出口（v2.0.x 序列）；模块市场平台化/协议 GUI 生成器（2.0+）；一次交付全部 5 档导出（插件节奏）。

## 0. 定位与战略验收

导出层 = **IR → 适配器 → 外部标准格式**。CCV3 首发；插件架构让 AGENTS/SKILL/MCP 按需增补（每适配器带目标格式版本锚点）。**验收出口唯一判据**：真实组合包过质检 → 导出 → SillyTavern 实际可玩（战例不过不收口）。

## 1. 问题全景（含立项 A 段 CCV3 语义调研）

- **A1 CCV3 语义错配（本版立项核心发现）**：CCV3 是**单角色卡**（persona/scenario/mes_example + character_book）；NF 装配产物（社区包实证：M01 职业/M04 战斗/M11 魔法/M40 关系/M57 朋友圈……全部是**世界规则/运行系统**模块）本质是**世界规则集**。把 N 系统模块"折进"单角色 persona 是语义错配——正确形态：**persona=主角占位 + character_book/lorebook 承载世界规则**，scenario=世界入口。
- **A2 无导出适配器与格式注册**：E0 三件套（IR/检索/质检）就绪，但 IR → 外部格式无出口。
- **A3 导出无契约校验**：坏产物（chara 字段缺/世界书引用悬空）会进生态——需契约门禁（基线 §6 check17 语义延续，本版落地为导出契约断言）。

## 2. 改进收口

### B1 IR→CCV3 映射草案（A1，立项 A 段成果）

基于真实组合包（校园/西幻/轻混 M91-M92）模块语义，映射规则：

| IR 内容 | CCV3 目标 | 规则 |
|---|---|---|
| 整份装配（世界规则集） | **character_book / 世界书** | 主承载：每个"世界规则/系统"层模块 → 一条 lorebook 条目（key = 模块层·模块名，content = 模块归一正文，常驻激活——世界运行规则应始终在场） |
| 装配 title/管线名 | chara `name` / `description` | name=title；description=管线名+模块数摘要 |
| 入口/剧情模块（M06 剧情/M91 异界桥等，layer≈P30/P40 的叙事入口语义） | chara `scenario` | 装配中显式标记入口的模块内容 → scenario；无则 = 引导语（由 title+description 生成） |
| 主角/人物相关（核心 M13 NPC、资产中角色模板） | `persona` | **默认占位**（"此世界用于叙事前端，主角由你在 SillyTavern 定义"）——NF 装配是世界观非角色，persona 不强占；若有显式角色模块则映射之 |
| 资产 entries（ATTR_TEMPLATES 等） | world 条目素材 | 资产 → 独立 lorebook 条目（key=资产键），供规则条目引用（保持 IR.asset_refs 分离语义，I4） |

**映射不静默丢内容（可信任度）**：所有 IR 层模块必映射为条目；无法语义归位者收"世界杂项"条目；映射后输出"未映射清单"到导出报告。

### B2 导出适配器 + 格式注册（A2）
- `desktop/src/core/exporter.py`：适配器注册表 `export(ir, fmt, dest=None) -> ExportResult`；CCV3 为第一适配器。
- `ccv3_adapter`：IR → chara JSON dict（spec `chara_card_v3`，字段：name/description/personality/scenario/first_mes/mes_example/character_book…）+ world 条目（SillyTavern lorebook entries 结构）；`.json` 直出 + PNG 封装（PySide6 QImage tEXt `chara` 键，零新依赖）。

### B3 导出契约门禁（A3）
- check18 入 verify 段 C：导出契约断言（unittest）——chara v3 字段完整、world 条目键可寻址、无未映射残留（映射不静默丢弃不变式）。

## 3. 任务分解

- **T1 ccv3 映射层（IR→chara/world dict）**（无前置，本版核心）：`ccv3_adapter.py`：`map_ir_to_ccv3(ir) -> dict`（chara JSON）+ `world_entries(ir) -> list[dict]`（lorebook 条目，key 规则 §B1）。单测：真实装配 IR（P01+校园模块+资产）→ chara 字段完整 / world 条目数=规则模块数 / persona 占位语义 / asset 条目独立。
- **T2 exporter + PNG 封装**（依赖 T1）：格式注册表 `export(ir,'ccv3')` 直出 chara.json + world.json；PNG 封装（QImage tEXt）。单测：export 产物含 spec_v3 锚点、world.json 可被解析。
- **T3 战例 + check18 + 收口**（依赖 T2）：真实组合包装载→装配→render_ir→quality_gate→export ccv3→断言（SillyTavern 可消费的字段完整 + .world 键无悬空）；verify check18 入段 C。

## 4. 提交规划

- C0 = 本方案落盘
- C1 = T1（ccv3_adapter 映射层 + 单测）——本波主交付（立项后的第一步实现）
- C2 = T2（exporter + PNG）
- C3 = T3（战例 + check18 + CHANGELOG）
- 每批 verify 全绿方可推（阶段工作流）。

## 5. 验证清单

- `python -m unittest discover -s tests`（96 + 新增全绿）
- `bash verify.sh` PASS 全绿（含 check18 若入）
- 战例断言：真实组合包 → export 产物 chara spec 锚点 + world 键可寻址 + 未映射清单为空
- Windows GUI 人工：装配 → 质量门通过 → 导出 .json/.png（后续波，SillyTavern 实跑留人工终验）

## 6. 瑶光反证

- **断言**：CCV3 单角色卡结构（persona/scenario/character_book）——知识标注：spec v3 字段（chara_card_v3）；**待复核**：精确字段清单以 github morenoid spec 为准（本环境 web 抓取 404，标注未复核，T1 前用 web_fetch 补抓权威 spec 再定字段集）。
- **断言**：NF 社区模块语义 = 世界规则系统（非角色卡）——证据：本会话 ls community 包 modules（M01 职业/M04 战斗/M11 魔法/M40 关系/M57 朋友圈/M58 电话…）✓。
- **断言**：IR 提供层/模块/资产结构可映射——证据：本会话 v1.2 实现的 ir.py（IRModule/IRLayer/IRDocument + asset_refs/asset_missing）✓。
- **待验证假设**：PySide6 QImage 可写 PNG tEXt（Qt setText）——T2 探针实测。

## 7. 范围外

- AGENTS/SKILL/MCP 插件、模块市场、协议 GUI 生成器——v2.0.x / 2.0+。
- SillyTavern 实跑人工验收——导出字段契约由自动化断言兜底，真机/前端人工终验留发布前。
