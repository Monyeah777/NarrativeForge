# NarrativeForge · v0.5.0 优化版方案（落盘执行版）

> **版本定位**：v0.5.0「优化版——吸收外部项目精华、夯实底层能力」
> **基线**：v0.4.0（main=`3c46355`，tag v0.4.0 annotated 已推送，工作区干净）
> **文档状态**：本文件为 v0.5.0 方案落盘真相源（I5）；由对话摘要骨架 + 动笔前全仓实测锚点重建（取证时间 2026-09-04，基线 `3c46355`）
> **编号声明**：方案正文此前仅在对话流中成稿（摘要承载骨架）。本文档以「问题 A → 改进 B → 任务 T」三层自洽编号重建落盘；硬锚点与历史一致：A1 编号越权收口、A3 社交媒介幽灵模块、B5 schema_version 从零引入、T1–T6 任务分解（T1 编号治理收口 → T2 一致性修复 → T3 记忆分层模块 → T4 M80 门禁流水线 → T5 资产工作台化 → T6 社区发布规范）。

---

## 0. 定位与边界

- **做什么**：不新增题材内容；全部改动落在 **治理 / 协议 / 一致性 / 工具链** 四类。吸收外部项目精华（NovelClaw 等七点机制调研，见记忆库）以架构升级方式落地（T3/T4）。
- **不做什么**：不改官方核心 13 件模块的职责语义；不改资产源文内容（资产为源文切片，I3 数据隔离）；不新增社区题材。
- **护航不变式**：I1 三正交分离 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一；两级平台规则 R1（社区包禁互引）/ R2（类别独占）/ R3（装配契约）。
- **改动范围**：根 01/02/07、04_模块库（M00/M50/M80）、05_资产库（如有）、verify.sh、README、community/两包、模板制作指令包.md。

---

## 1. 问题全景（A1–A6，实测取证）

### A1 社区包跨包引用（违 R1）——M65 被西幻包直接引用
- **证据**（grep 实锤）：
  - `community/西幻生存领域包/modules/M10_死亡重生.md:33`：`重要 NPC 死亡 → death_trigger 广播（M65 遗憾/事件:M22 编剧线开启）`
  - `M10_死亡重生.md:53`：`subscribers: [M65, 事件:M22]`
  - `M19_遗传后代.md:28`：`父母死亡（生存:M10）→ 后代可继承遗物与未竟之志（M65 遗憾线延伸）`
  - `M18_混沌事件.md:2`：模块头 `被依赖：M06、事件:M22、生存:M10、M65`
  - `M18_混沌事件.md:31/38`：`subscribers: [M06, 事件:M22, 生存:M10, M65]`、`M65 沉淀遗憾素材`
  - `pipelines/P03_西幻生存流管线.md:72`：`allowed_modules: [生存:M10, M07, M09, M14, M15, M16, M65]`（**裸 M65，未带情感: 类别前缀**）
- **根因**：M65 幽灵遗憾的订阅契约天然横跨「死亡（西幻）」与「遗憾（校园）」两题材（death_trigger/chaos_event 联动），业务需求突破 R1「社区包之间禁止互相引用」。合法通道本应经官方核心中介（02 §3 动态订阅注释：事件:M22 装配校园包时挂接 ghost_event、装配西幻包时挂接 death_trigger），M65 模块头却直接声明订阅西幻事件源。
- **影响**：注册表无法校验跨包依赖；P02/P03 装配契约（R3）静默越界；I5 真相唯一受侵；P03 L72 裸 M65 还会在类别前缀限定扫描（verify.sh check2 逻辑）下漏检。

### A2 装配真相源漂移（违 I5）——M50 硬编码 17 步 vs 02 §6 官方 10 步
- **证据**：
  - `02_联动注册表.md §6` 明文：官方核心装配执行顺序 10 步；**「本执行顺序为官方核心装配的真相源；装配社区题材包后，按社区包索引合并生成最终顺序（I5：禁止硬编码 17 步）」**
  - `04_模块库/通用类/M50_主循环.md §2`：标题即「执行顺序（17 步，与注册表一致）」，正文硬编码 17 步**全装配**（含社区模块 M01/M02/M19/M18/情感:M22/M40/M41/M43/M55/生存:M10/M65…）
- **根因**：M50 把「官方 10 步 + 社区合并」的**动态装配结果**固化为静态 17 步清单，注册表增减社区模块后 M50 即成第二真相源。
- **影响**：任何社区包增删模块都需同步手改 M50；两处不一致时无权威仲裁；I5 直接违例。

### A3 幽灵模块出生证明缺失——M57/M58/M59 及资产侧旧编号残留
- **证据**（校园情感领域包）：
  - 资产侧实锤消费编号：`SOCIAL_FEED.md`「12.2 朋友圈动态流（M57 + SOCIAL_FEED）」、`PHONE_CALL.md`「12.3 电话通讯（M58 + PHONE_CALL）」、`GROUP_CHAT.md`「12.4 群聊系统（M59 + GROUP_CHAT）」
  - `assets/README.md` 映射表：`GOSSIP_TOPICS → M34 / M59 / M57`、`FORUM_POSTS → M57（论坛）`
  - `附_社交系统触发联动.md`：`M57.scan_feeds / M58.scan_calls / M59.scan_group_chats` 完整伪代码 + 执行位次自检（M22.advance_quests 之后、P60 演变前）
  - `附_生活情敌联动结算.md:12`：**「39 模块编号位（在册 41 个含 M65/M57/M58/M59/M24/M25/M26/M43）✓」**
  - 模块侧实况：`community/校园情感领域包/modules/` 仅 **6 文件**（M22/M40/M41/M43/M55/M65）；02 §8.1 登记亦仅 6 模块
  - **延伸残留**（同源）：`M21`（人格演变，BIG5_INFER/EVOLUTION_RULES 消费列）、`M24`（资产侧=压力结算 PRESSURE_RULES，与官方核心 M24=组合规则**双义撞号**）、`M25`（疲劳精力）、`M26`（天气情绪）、`M34`（闲聊素材）、`M62`（风评）、`M70`（梦境素材）——均只见于资产内容/映射表，无模块文件
- **根因**：校园包资产为源文 `校园框架_v0.7_完整合并版.md`（8620 行）按行区间切片（assets/README 自述「未改写」），**资产侧保留源项目旧编号体系**；模块侧已手工收敛到官方两级编号（6 件）。两轨之间缺「对照层」与「出生证明」。
- **影响**：M57/M58/M59 有资产、有伪代码、有执行位次，却无模块文件、不在注册表——平台装配时无法寻址；资产映射表消费列指向不存在模块；「在册 41 个」与官方登记 6 模块出入巨大，验收无权威口径。

### A4 schema_version 全仓缺失（B5 前置确认）
- **证据**：`grep -rn 'schema_version' --include='*.md' .` 全仓 **零命中**（exit 0 无匹配）。
- **影响**：M00 四类实体契约、资产元数据、注册表结构、存档协议均无版本字段；历次扩展（v0.7.x 追加字段）全靠「只增不删」隐式兼容；未来结构性变更（改必填/删字段/类型升级）无迁移判定依据；跨版本存档与跨包资产无法自证兼容。**需从零设计引入**（对应历史 B5）。

### A5 资产-模块双向索引缺口
- **证据**：assets/README 映射表「主要消费模块」列大量指向幽灵编号（见 A3）；模块文件头「依赖」列与资产键之间无反向索引（模块 → 消费资产键声明不全）；无工具或校验能回答「某资产被哪些在册模块消费」「某模块依赖哪些资产键」。
- **影响**：资产生命周期（增删改）无法评估对模块的影响面；A3 类幽灵编号无法被自动检出，只能人工 grep。

### A6 社区发布验收口径松散
- **证据**：附_生活情敌联动结算.md:12 自检声明（「在册 41 个 ✓」）无官方核验入口即宣称通过；`community/模板制作指令包.md` 存在但发布步骤未覆盖「模块出生证明完整性 / 注册表登记对账 / 资产-模块双向索引核验」；verify.sh 段 B（check7-10）仅在场性检查，无注册表在册数与模块文件数一致性校验。
- **影响**：社区包质量依赖人工自觉；「✓」自检不可复核；平台两级结构（R1-R3）缺发布侧强制闸。

---

## 2. 改进收口（B1–B6，与 A1–A6 对应）

| 编号 | 收口主题 | 对应问题 | 一句话收口 |
| --- | --- | --- | --- |
| B1 | 编号治理收口 | A1 | 跨包联动统一走「事件桥」白名单，禁止直接模块 ID 互引；裸 M65 引用全限定化 |
| B2 | 装配真相源归一 | A2 | M50 删除硬编码 17 步，改为引用 02 §6 真相源 + 动态合并说明 |
| B3 | 模块出生证明补齐 | A3 | 补建 M57/M58/M59 模块文件并登记在册；资产映射表加「源编号→官方模块」对照列 |
| B4 | schema_version 从零引入 | A4 | M00 实体契约 / 注册表头 / 资产头 / 存档协议引入版本字段与兼容规则 |
| B5 | 资产工作台化（双向索引） | A5 | asset_register 五接口 + 双向索引表 + 校验脚本，幽灵编号自动检出 |
| B6 | 社区发布规范 | A6 | 发布三步自检（出生证明 / 登记对账 / 双向索引）+ verify.sh 段 B 升版 |

---

## 3. 任务分解（T1–T6，依赖序）

```
T1 编号治理收口 ──→ T2 一致性修复 ──→ T3 记忆分层模块
        （A1/B1）       （A2/A3/A4）      （外部精华 · 架构升级）
                                          ↓
T6 社区发布规范 ←── T5 资产工作台化 ←── T4 M80 门禁流水线
  （A6/B6 + 版本收口）   （A5/B5）        （外部精华 · 架构升级）
```

依赖：T1/T2 无前置；T3 依赖 T2.3（schema_version 协议）；T4 独立可并行；T5 依赖 T2.2（幽灵模块定稿）；T6 收口全部。

---

## 4. 任务明细

### T1 编号治理收口（A1/B1）
- **目标**：消除社区包跨包直接引用；P03 裸 M65 全限定化；R1 增补「事件桥」条款。
- **动作**：
  1. `01_核心协议.md §6`：R1 修订说明——社区包之间禁止直接模块 ID 互引；**跨题材事件联动须经官方核心中介（事件:M22 事件总线）登记为「事件桥」白名单订阅**；模块头 subscribe 声明中跨包事件源一律写事件名 + 注明「经事件桥」，不写对方模块 ID。
  2. `community/西幻生存领域包/modules/M10_死亡重生.md`：L33/L53 的 M65 引用改为事件桥口径（`death_trigger → 事件桥（下游：校园情感包经 ghost_event 通道登记）`），subscribers 列表移除 M65 直引。
  3. `M18_混沌事件.md`：模块头「被依赖」与 L31/L38 subscribers 移除 M65；chaos_event 联动改事件桥描述。
  4. `M19_遗传后代.md:28`：M65 遗憾线延伸改为事件桥口径。
  5. `pipelines/P03_西幻生存流管线.md:72`：allowed_modules 移除裸 M65（或补 `情感:M65` 后移除——P03 属西幻包，M65 属校园包，均不可在 allowed_modules 中出现，应移除并由事件桥覆盖）。
  6. `community/校园情感领域包/modules/M65_幽灵遗憾.md`：模块头订阅段（subscribe: death_trigger/chaos_event）加注「跨包事件经事件桥登记，非直接模块互引」。
- **验收**：西幻包内 grep `M65` 仅剩事件桥口径注释（无订阅/被依赖/管线 allowed 直引）；`情感:M65` 不再出现在 P03 allowed_modules；verify.sh 增 check：社区包 modules/ 内不得出现对方包模块 ID 直引（可先人工 grep 复核）。

### T2 一致性修复（A2/A3/A4 → B2/B3/B4，三个子项）
**T2.1 装配真相源归一（A2/B2）**
- `04_模块库/通用类/M50_主循环.md §2`：标题改「执行顺序（官方核心 10 步为真相源，见 02 §6；社区包按索引动态合并）」；删除 17 步硬编码清单，改为引用 02 §6 官方 10 步 + 说明装配 P02/P03 后按包索引合并（社区模块示例可保留但标注「示例非真相源」）。
- `02_联动注册表.md §6`：保持官方 10 步真相源地位，补一句「M50 模块文件与注册表冲突时以本表为准（I5）」。
- 验收：M50 不再含与 02 §6 冲突的静态 17 步编号；两文件交叉引用闭合。

**T2.2 幽灵模块补证（A3/B3）**
- 补建 `community/校园情感领域包/modules/M57_朋友圈动态.md`（M57 + SOCIAL_FEED/FORUM_POSTS）、`M58_电话通讯.md`（M58 + PHONE_CALL）、`M59_群聊系统.md`（M59 + GROUP_CHAT/GOSSIP_TOPICS）——按模块协议八字段（id/name/layer/inputs/outputs/events/core/replaceable）成文，events 声明与附_社交系统触发联动.md 伪代码一致（scan_feeds/scan_calls/scan_group_chats），挂载 P50 交互执行层。
- 02 §8.1 校园包登记：模块 6 → **9**（+M57/M58/M59）；资产数不变（30 文件）。
- `community/校园情感领域包/README.md`：模块清单同步 9 件；`附_社交系统触发联动.md` 的 M57-59 引用补「模块文件见 modules/」锚点。
- `assets/README.md` 映射表：新增一列「官方模块对照」或在表注说明——消费列旧编号（M21/M24/M25/M26/M34/M57/M58/M59/M62/M70 等）为源文切片残留，模块侧官方编号以本表注释 + 02 §8.1 为准（资产内容不改写，I3）。
- 验收：校园包 modules/ 9 文件与 02 §8.1 在册 9 模块一一对应；assets 映射表消费列每个编号可解析到「官方模块 or 源编号残留（已注释）」之一。

**T2.3 schema_version 从零引入（A4/B4）**
- `04_模块库/通用类/M00_数据结构.md`：四类实体契约（PlayerState/NPCState/WorldState/EventState）各加首字段 `schema_version: "1"`；数据槽总线协议补版本说明。
- `02_联动注册表.md`：文件头加 `registry_schema_version: "1"`。
- 存档协议（校园包附_生活情敌联动结算.md 存档段）：加 `save_schema_version`。
- 兼容规则（写入 01 协议或 M00）：vN 读取 vN-1 须兼容（只增不删）；结构性变更须 bump 版本并在变更记录注明迁移说明。
- 验收：全仓 grep schema_version 至少 4 处命中（M00 实体契约、02 注册表头、存档协议、01 协议兼容规则节）；无历史文件需迁移（首引即 v1）。

### T3 记忆分层模块（外部精华 · 架构升级）
- **来源**：NovelClaw 七点机制之「动态记忆控制（可编辑记忆库 + 记忆写回事件）」（记忆库调研，2026-09-04）；对齐 M00 现有 memory 字段（PlayerState.memories / NPCState.memory）。
- **目标**：把叙事状态记忆升级为**三层分层 + 写回事件**，控制状态膨胀、提升长线一致性（呼应「夯实底层能力」）。
- **动作**：
  1. M00 实体契约：NPCState.memory / PlayerState.memories 细化为三层——`working`（回合内，随回合快照）/ `episodic`（关系史与事件史，带 tick 时间戳）/ `semantic`（长期设定与常识）；各层带 `schema_version`（依赖 T2.3）。
  2. M50 主循环：回合末归档规则——working → 过期清理；重要事件写回 episodic；episodic 提炼沉淀 semantic（写回事件 `memory_writeback`，经 M00 数据槽总线发布）。
  3. M65 遗憾/幽灵（校园包）与 M80 输出门消费 episodic/semantic 的读取接口声明（不越权直接改状态，I4）。
  4. 文档落点：M00 §3 实体契约、M50 §2 调度规则、01 协议（如需要）、校园/西幻包模块头 memory 读写声明抽查。
- **验收**：M00 契约含三层记忆结构；M50 含写回事件与归档规则；全仓模块头无越权直写他模块 memory 字段（I4 扫描）；verify.sh 可加可选 check（文档级 grep 断言）。

### T4 M80 门禁流水线（外部精华 · 架构升级）
- **现状**：M80 已内置 DNA 八条质检三档 gate（pass 直出 / warn 自纠 / fail 重写 + 连续 3 次降级白描）与 P90 共用说明——**门禁能力已有，缺流水线化协议**。
- **目标**：gate 决策声明式、可观测、可插拔（对齐 NovelClaw「可观测运行 / run 目录五件套」思想）。
- **动作**：
  1. M80 模块文件：gate 三档升级为**声明式 gate_action 流水线**——`pass: [direct]` / `warn: [self_correct, log_revision]` / `fail: [rewrite, retry_limit:3, fallback:white_sketch]`；输出附 gate 决策记录（命中 DNA 清单、修订点、重写次数）供外部观测。
  2. gate 决策记录格式写入 M80（结构化行，不进叙事文本——保持「系统日志独立通道」既有边界）。
  3. P90 技术文档流共用门禁：确认 §7.2 与 M80 门禁协议一致（已共用输出门，补 gate_action 声明）。
  4. 01 协议或 M80：门禁为**唯一输出闸**（I2 核心固定，叙事/P90 均不可绕过）。
- **验收**：M80 含 gate_action 流水线声明与决策记录格式；P90 段引用闭合；verify.sh check5（M80 gate 关键词）补 gate_action 断言。

### T5 资产工作台化（A5/B5）
- **目标**：资产-模块双向索引 + 自动对账，幽灵编号自动检出（A3 问题从「人工 grep」升级为「工具断言」）。
- **动作**：
  1. 两包 assets/README 映射表补「官方模块对照/源编号残留」注释列（与 T2.2 联动）；新增**反向索引段**：模块 → 消费资产键（校园 9 模块 × 资产键；西幻 14 模块 × 资产键）。
  2. `scripts/` 新增对账脚本（bash/python 均可，参照 verify.sh 风格）：解析 02 §8.1 在册模块 ↔ modules/ 文件 ↔ assets/README 映射表消费列，输出三类告警——幽灵编号（资产引用无模块文件）、孤儿模块（在册无文件）、未登记模块（文件不在册）。
  3. 05_资产库（官方侧）如存在资产头，同样纳入双向索引（先盘点再定，动作可收敛为文档级）。
- **验收**：脚本在 v0.4.0 基线运行输出 A3 幽灵清单（证明有效）；T2.2 完成后运行零幽灵告警（除已注释源编号残留）；脚本纳入 verify.sh 段 B（缺脚本则 WARN）。

### T6 社区发布规范 + 版本收口（A6/B6）
- **动作**：
  1. `community/模板制作指令包.md`：发布步骤升级为**三步自检闸**——①模块出生证明完整性（modules/ 每件含协议八字段 + 文件数 = 02 §8.1 在册数）；②注册表登记对账（新增模块须登记 02 §8.1 且占 M91-M99 预留段或包内编号段）；③资产-模块双向索引核验（T5 脚本零幽灵）。
  2. verify.sh 段 B 升版：check7-10 增补在册数一致性断言（02 §8.1 模块数与 modules/ 文件数比对）。
  3. `07_官方核心出厂与社区预设导航.md §7` 验收清单：按 v0.5.0 结果升版（社区包在册数 6→9 校园 / 14 西幻；M50 真相源说明；schema_version 首引）。
  4. README.md：协议链与版本说明补 v0.5.0 一行。
  5. 全量 verify.sh 跑绿 → 独立 commit → tag v0.5.0 annotated → push origin/main。
- **验收**：verify.sh 全绿；07 §7 与实况一致；git tag v0.5.0 推送。

---

## 5. 提交规划

| 序 | 提交内容 | 前缀 | 关联 |
| --- | --- | --- | --- |
| C0 | 08_v0.5.0_优化版方案.md 落盘 | docs: | 本文件 |
| C1 | T1 编号治理收口（协议 R1 事件桥 + 西幻包 M65 直引清除） | docs: | A1 |
| C2 | T2.1 M50 真相源归一 | docs: | A2 |
| C3 | T2.2 M57/M58/M59 补证 + 登记 + 映射表对照 | docs: | A3 |
| C4 | T2.3 schema_version 从零引入 | docs: | A4 |
| C5 | T3 记忆分层模块（M00/M50 契约） | docs: | 架构升级 |
| C6 | T4 M80 门禁流水线 | docs: | 架构升级 |
| C7 | T5 资产工作台化（索引 + 对账脚本） | chore: | A5 |
| C8 | T6 发布规范 + 07/README 升版 + verify.sh 升版 | docs: | A6 |
| C9 | 全量 verify 绿 + tag v0.5.0 + push | chore: | 收口 |

每任务独立 commit；C1–C8 期间工作区只允许该任务相关文件变更，避免交叉。

---

## 6. 风险与开放问题

1. **M57/M58/M59 模块正文反推**：需从附_社交系统触发联动.md 伪代码 + 资产字段还原协议八字段；若伪代码与资产字段有出入，以资产（源文切片，不可改）为准，模块文件记录差异。
2. **M24 双义撞号**：官方核心 M24=组合规则 vs 校园资产侧 M24=压力结算（源编号残留）。处置：不重编号资产内容（I3 源文不改写），在映射表注释 + 对账脚本将「M24（资产侧）」标记为源编号残留；官方 M24 语义不动。
3. **事件桥口径落地粒度**：R1 修订为「白名单事件桥」后，需在 02 §3 动态订阅注释同步补事件桥登记示例，防止各包自行发明桥接写法。
4. **T3 记忆分层范围**：本方案定义为 M00/M50 契约级升级（文档层）；若后续要落到运行时代码（desktop/ 或平台层），需另立版本，不在 v0.5.0 文档库范围内扩张。
5. **西幻包资产侧同样存在源编号体系**（assets/ 01-23 文件按西幻骨架编号），T5 对账脚本对西幻包映射表（西幻 assets/README）同样适用，需一并盘点。
