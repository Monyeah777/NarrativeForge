# 社区版预设（开箱即玩）
> 定位：免装配直接运行的**两套官方预设**——预设 A 校园情感流（P02）、预设 B 西幻生存流（P03）。加载到 SillyTavern 等前端即玩。
> 依据：01_核心协议（协议）｜02_联动注册表（真相源）｜03_管线库（P02/P03）｜05_资产库（数据层）｜06_Agent执行协议（运行约束）。
> 终端玩家：按 §4 三步装载；创作者：见 §5 自定义扩展通道；验收：§7 十项清单终验。

---

## 1. 预设总览

| 项 | 预设 A · 校园情感流 | 预设 B · 西幻生存流 |
| --- | --- | --- |
| 管线 | **P02** 校园情感流（9 层线性回卷） | **P03** 西幻生存流（9 层线性回卷） |
| 模块数 | 18（含核心，重号限定） | 24（含核心，重号限定） |
| 资产包 | `校园包`（29 文件：官方 16 键 + 扩展 12 键 + 附机制 2） | `西幻包`（23 文件：M01-M23 整节切片 + 2 段外部完整实体） |
| 主轴 | 关系驱动：人格画像 → 日常事件 → 三冲动 → 关系进阶 | 生存驱动：职业成长 → 事件/混沌 → 战斗/生产 → 世界演变 |
| 时间制 | 校园作息（节 / 课间 / 放学 / 夜） | 分钟制（10 分钟刻，生存消耗逐刻结算） |
| 输出风格 | 短段落 + 白描/诗化交替 + 极简对话（写作 DNA） | 结算可读化 + 状态面板 + 死亡重生叙事 |
| 死亡规则 | 无死亡线；遗憾显形（幽灵） | health=0 → 生存:M10 死亡判定 → 重生（祝福/遗物继承） |
| 存档键 | relationships / active_quests / ghost_data | attributes+vitals / economy / relations / death_blessings / 世界档案 |

---

## 2. 预设 A · 校园情感流（P02）

### 2.1 模块装载清单（按层，18 模块）
| 层 | 挂载（default） | 模块职责 |
| --- | --- | --- |
| P00 数据基座 | M00 | 数据槽结构/存档装载（含 relationships/active_quests/ghost_data） |
| P10 世界推进 | 通用:M10、M08 | 校园作息节拍 + 季节天气 |
| P20 角色状态 | M23、情感:M22 | 认知裁剪 + 三冲动基线（NPC 用；主角豁免） |
| P30 事件生产 | 事件:M22、M06、M13、M12 | 日常事件流 + 任务 + 隐藏事件状态机 |
| P40 关系决策 | 情感:M22、M40、M41、M43、M55 | 三冲动裁决 → 关系深度 → 恋爱进阶/情敌/情书 |
| P50 交互执行 | M12、M13 | 对话执行 + 互动结算（频率→认知、质量→下限线） |
| P60 长期演变 | M40、M65 | 关系网演化 + 遗憾沉淀/幽灵冷却 + 人格演变 |
| P70 叙事素材 | M20、M24 | 世界知识库 + 写作 DNA/组合校验 |
| P80 输出呈现 | M80 | 现实/GAL 双模式渲染（DNA 质检门） |
| 全局 | M50 | 主循环调度（17 步，读注册表） |

> 机制约束：**三冲动仅驱动 NPC**（v0.7.12 冲动-社会关系隔离：只驱动物理位移/动作连带/视线停留，禁止告白/交往/约会/关系推进，驱动权归 M40/M41）；叙事须过认知边界四层管线（v0.7.13）。

### 2.2 资产包挂载（校园包 29 文件）
- **官方区 16 键**（第 5 章 5.1-5.16）：`ATTR_TEMPLATES`（60 条）/ `FEAR_LIBRARY`（9）/ `BIG5_INFER`（18 行为+15 标签）/ `EMOTION_WHEEL`（8 情绪）/ `BEHAVIOR_SIGNALS`（4×3 档）/ `COGNITIVE_LOAD`（4 级）/ `LOCATIONS`（30 条）/ `WEATHER_SEASON` / `GOSSIP_TOPICS` / `FORUM_POSTS` / `WRITING_STYLE`（写作 DNA）/ `EVOLUTION_RULES` / `REGRET_LIBRARY` / `NAME_LIBRARY` / `INTERACTION_DEPTH` / `PERSONALITY_LINK`。
- **扩展区 12 键**（v0.7.7-9，第 11-13 章）：`FAMILY_BACKGROUND` / `DAILY_HABITS`+`SCHEDULE`（合文件 DAILY_HABITS_SCHEDULE.md）/ `GIFT_PREFS` / `HIDDEN_EVENTS` / `SOCIAL_FEED` / `PHONE_CALL` / `GROUP_CHAT` / `RIVAL_INFO` / `PRESSURE_RULES` / `PART_TIME_JOB` / `WEATHER_MOOD`。
- **附机制 2 文件**：`附_社交系统触发联动`、`附_生活情敌联动结算`。

调用示例：`asset_get("ATTR_TEMPLATES","T001")` 取人格模板；`asset_roll("GOSSIP_TOPICS")` 抽课间话题；`asset_match(BIG5_INFER, profile)` 推 NPC 剖面。

### 2.3 默认状态与回合节拍
- 默认开局：高中二年级主角槽 + 空关系网 + 0 遗憾；人格模板先经 asset_get 装载。
- 四拍节拍：**上课节**（少互动，可观察）→ **课间**（闲聊/论坛/偶遇）→ **放学**（深度互动窗口）→ **夜**（电话/聊天/遗憾显形窗口）。
- 每拍一回合；互动后必查 INTERACTION_DEPTH（频率/质量/破坏三效应，M40 §3）。

---

## 3. 预设 B · 西幻生存流（P03）

### 3.1 模块装载清单（按层，24 模块）
| 层 | 挂载（default） | 模块职责 |
| --- | --- | --- |
| P00 数据基座 | M00、M20 | 数据槽 + PlayerState/世界档案（attributes/vitals/economy/relations/death_blessings） |
| P10 世界推进 | 通用:M10、M08 | 分钟制推进 + 生存消耗 + 季节天气修正 |
| P20 角色状态 | M01、M02、M19 | 职业成长（经验/升级）/ 种族天赋 / 遗传后代 |
| P30 事件生产 | M06、M18、事件:M22 | 任务剧情 + 混沌事件 + NPC 交互请求 |
| P40 行为决策 | M04、M11、M24 | 战斗倾向/魔法选择/交易意图/组合校验 |
| P50 交互执行 | M04、M11、M12、M13、M17 | 战斗结算/魔法施放/对话/生产制造 |
| P60 世界演变 | 生存:M10、M07、M09、M14、M15、M16 | 旅行/经济/阵营/帮派/建筑 + 死亡重生 |
| P70 叙事素材 | M20、M24 | 世界知识库常识 + 组合规则 |
| P80 输出呈现 | M80 | 状态面板 + 叙事输出（死亡/重生提示） |
| 全局 | M50 | 主循环调度 |

### 3.2 资产包挂载（西幻包 23 文件）
01 职业成长 JOB（尾附 EXT 36820-37280 **18 职业完整表**）｜02 种族天赋 RACES｜03 技能融合 SKILLS｜04 战斗系统 COMBAT｜05 物品装备 ITEMS｜06 任务剧情 TASKS（8 模板）｜07 区域地图 REGIONS（11 区域/40+ 地点）｜08 天气季节 WEATHER｜09 经济贸易 ECONOMY（37 商品/11 区域系数）｜10 死亡重生 DEATH｜11 文字魔法 MAGIC｜12 NPC 对话 NPC_DIALOGUE｜13 NPC 交互 NPC_INTERACTION｜14 势力阵营 FACTIONS（总表/声望门槛）｜15 帮派势力 GANGS（13 帮派）｜16 建筑据点 SETTLEMENTS（建筑/居民模板）｜17 生产制造 CRAFTING｜18 混沌事件 CHAOS（行为规则）｜19 遗传后代 GENETICS｜20 世界知识 WORLD_KNOWLEDGE（尾附 EXT 28894-29064 **全量实体**）｜21 数据接口 EXTERNAL｜22 事件叙事 EVENT_NARRATIVE｜23 组合扩展 COMBINE。

> 01/20 两文件含**追加的外部完整实体段**（源文为“节选 + 指向外部表”），追加段均带溯源注释，可直接消费；骨架概览区对 M12-M17 的“待填充”标注为过时信息，**以正文区切片为准**（全部 ✅ v1.0）。

### 3.3 运行规则速查
- 生存六维 `vitals{health,hunger,thirst,fatigue,temperature,insanity}` 由 M01 统一按分钟结算；health=0 → 交生存:M10 死亡判定。
- 死亡不结束游戏：进入重生流程（死亡祝福/遗物继承）；经济基准 `1金=100银=10000铜`，M09 唯一货币结算入口。
- 混沌事件可直接调用死亡判定；insanity≥80 → 幻觉事件（事件:M22 编剧）。

---

## 4. SillyTavern 加载指引（三步）

1. **系统提示/角色卡**：把 01_核心协议 → 02_联动注册表（§6 顺序）→ 06_Agent执行协议 → 本文件 §2 或 §3 的装载清单，按序写入系统提示；角色卡按预设装载默认状态。
2. **世界信息（Lorebook）**：将 05_资产库条目按“键名→关键词”导入（校园：`ATTR_TEMPLATES`/`LOCATIONS`/`GOSSIP_TOPICS`…；西幻：`18职业`/`REGIONS`/`ECONOMY`…），命中即注入裁剪后素材，勿整体注入。
3. **正则与扩展**：挂隐藏域/系统数值剥离正则（防泄漏）；骰子/事件判定可由扩展接 `asset_roll(seed)`。

最小系统提示骨架：
```text
你是叙事工坊（NarrativeForge）的执行 Agent。依据联动注册表 17 步顺序推进回合，
绝不直写正文：素材经 M23 认知裁剪后只交 M80 渲染（八条写作 DNA 质检门）。
当前预设：{A 校园 / B 西幻}；资产访问仅经 asset_get/query/match/roll/register 五接口；
系统结算走独立通道，玩家可见文本禁止 JSON/YAML 与数值面板（现实模式）。
每回合：推时刻 → 装载 → 事件 → 决策 → 结算 → 裁剪 → 渲染 → 写档。
```

---

## 5. 创作者扩展通道

- **自定义资产包**：包 ID 从 900+ 起（官方 0-899 保留），文件放 `05_资产库/用户自定义/<包名>/`；每个条目先过 Schema 校验，再 `asset_register(entry)` 注册，注册表可见后五接口可寻址。
- **替换模块**：新模块须满足被替换模块的 inputs/outputs/events 契约（01 §1）；核心模块（M00/M50/M80/注册表）不可替换（I2）。
- **新管线**：按 01 §2 管线规范声明 layers/flow；挂载表写入注册表后 Agent 即按注册表调度，无需改 Agent 代码（I5）。

---

## 6. 预设切换与数据隔离

- 校园/西幻存档键不同（§1 表），**不可互载**：切换预设须开新档。
- 建议同一前端实例一次只跑一个预设；协议注入文本按预设替换 §2/§3 装载块。
- 混装资产包（校园键进西幻流程）会触发 L3 资产可达校验失败——拒绝开跑。

---

## 7. 十项验收清单（交付终验）

| # | 验收项 | 判定标准 | 状态 |
| --- | --- | --- | --- |
| 1 | 目录结构完整 | 01/02/06/07 根级四件 + README；03_管线库 3 文件；04_模块库 32 文件；05_资产库总 README + 校园 29 + 西幻 23 + 用户自定义目录 | ☐ |
| 2 | 05_资产库溯源一致 | 校园包 29 文件 1573 行 / 西幻包 23 文件 4285 行；切片行号与子包 README 溯源表一致 | ☐ |
| 3 | 模块-资产引用可寻址 | 04 引用的资产键（ATTR_TEMPLATES/LOCATIONS/18 职业/world_knowledge 等）在 05 均可经五接口寻址 | ☐ |
| 4 | 重号 ID 全限定 | M10（通用/生存）、M22（情感/事件）在 02/04/05/06/07 全部类别前缀限定书写 | ☐ |
| 5 | 五条不变式落点 | I1 三正交（01 §5）/ I2 核心固定（M00/M50/M80/注册表）/ I3 通信契约（注册表订阅）/ I4 数据隔离（五接口）/ I5 真相唯一（06 §1） | ☐ |
| 6 | 认知边界强制落地 | v0.7.13 四层管线（M00→M40 事实 →快照→M23 裁剪→渲染→锚点回验）在 06 §4 与 M23 一致 | ☐ |
| 7 | 输出质检门 | M80 gate 三档（pass/warn/fail+白描降级）在 M80 与 06 §5 一致 | ☐ |
| 8 | 外部实体追加完整 | 西幻 01 尾 EXT 36820-37280（18 职业）/ 20 尾 EXT 28894-29064（world_knowledge）闭合完整、带溯源注释 | ☐ |
| 9 | v0.7.12 冲动隔离落地 | 三冲动仅驱动物理/动作/视线，关系推进权归 M40/M41——约束写入校园包 README、M22 与 06 §9 | ☐ |
| 10 | 入口可导航 | 总 README 指向 07 预设；07 → 01/02/06 协议链、05 资产、03 管线引用全部可访问 | ☐ |

> 执行方式：§7 清单与文件实况对照终验（行数/锚点/闭合性以终端 grep 复核为准）。

---
*社区版预设至此交付完成：两份预设装载块可直接复制进系统提示开玩；验收动作见 §7。*