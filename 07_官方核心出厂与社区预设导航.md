# 官方核心出厂与社区预设导航
> 定位：官方核心 13 件**出厂装配说明**（标准管线 P01），兼作两级结构的**入口导航**——社区两套预设（A 校园情感流 P02 / B 西幻生存流 P03）的完整装载手册已随各领域包 README 分发，本文件只做索引与协议引导，避免双源漂移。
> 依据：01_核心协议（协议）｜02_联动注册表（真相源）｜03_管线库（P00 通用骨架、P01 官方管线）｜06_Agent执行协议（运行约束）。
> 终端玩家：官方开箱 = §2 装配 13 件；玩社区预设 = 按 §3 到对应领域包 README 三步装载（§4）；创作者：§5；验收：§7 清单。
---
## 1. 官方核心出厂（13 件，平台 v1.1 起两级结构）
| 类别 | 件数 | 模块 |
| --- | --- | --- |
| 通用类 | 6 | M00 数据结构 / 通用:M10 时间推进 / M23 认知边界 / M24 组合规则 / M50 主循环 / M80 输出生成器 |
| 事件类 | 5 | 事件:M22 事件叙事 / M06 任务剧情 / M12 NPC对话 / M13 NPC交互 / M20 世界知识库 |
| 世界类 | 1 | M08 季节天气 |
| 技术文档 | 1 | M90 |
> 装配与订阅见 02_联动注册表（§2 核心模块表 / §3 订阅 / §5 挂载点）；重号限定：M22/M10 须类别前缀（事件:M22、通用:M10 属核心；情感:M22、生存:M10 属社区）。
## 2. 官方核心装配（P01 标准管线）
P00[M00] → P10[通用:M10] → P20[M23] → P30[事件:M22、M06、M13] → P40[空层·optional] → P50[M12] → P60[空层·optional] → P70[M20、M24] → P80[M80]；全局 M50 主循环调度（读注册表）。
> 空层说明：P40/P60 为 optional:true 空层，官方核心装配直通/跳过；社区管线（P02/P03）自 P00 骨架派生后在对应层位追加题材模块，不阻塞官方 P01（R3 装配契约）。
## 3. 社区领域预设索引（开箱即玩入口）
| 预设 | 领域包（装载手册） | 管线 | 装配 | 资产 | 主轴 |
| --- | --- | --- | --- | --- | --- |
| A · 校园情感流 | community/校园情感领域包/README.md | P02 | 18（6 题材 + 12 核心配合） | 29 文件 | 关系驱动 |
| B · 西幻生存流 | community/西幻生存领域包/README.md | P03 | 24（14 题材 default 13 + 核心 11；M03/M23 为 allowed 备选） | 23 内容文件 | 生存驱动 |
> 包内结构：modules（题材模块）/ assets（资产 + assets/README.md 溯源索引）/ pipelines（Pxx 管线）/ README（包速览 → 装载清单 → 资产挂载 → 默认状态或运行规则 → 红线 → SillyTavern 三步 → 包级验收）。
> 07 不再内嵌两套预设的逐模块/逐资产清单——装载细节、默认状态节拍、运行红线均以两包 README 为准（唯一真相，防双源漂移）。
## 4. SillyTavern 加载指引（三步）
1. **系统提示/角色卡**：把 01_核心协议 → 02_联动注册表（§6 顺序）→ 06_Agent执行协议 → 所选领域包 README §2 装载清单，按序写入系统提示；角色卡按包 README §4 默认状态装载。
2. **世界信息（Lorebook）**：将对应包 assets/ 键按「键名→关键词」导入（键表见该包 assets/README.md），命中即注入裁剪后素材，勿整体注入。
3. **正则与扩展**：挂隐藏域/系统数值剥离正则（防泄漏）；骰子/事件判定可由扩展接 asset_roll(seed)。
最小系统提示骨架：
```text
你是叙事工坊（NarrativeForge）的执行 Agent。依据联动注册表 17 步顺序推进回合，
绝不直写正文：素材经 M23 认知裁剪后只交 M80 渲染（八条写作 DNA 质检门）。
当前预设：{A 校园 / B 西幻}；资产访问仅经 asset_get/query/match/roll/register 五接口；
系统结算走独立通道，玩家可见文本禁止 JSON/YAML 与数值面板（现实模式）。
每回合：推时刻 → 装载 → 事件 → 决策 → 结算 → 裁剪 → 渲染 → 写档。
```
## 5. 创作者扩展通道
- **自定义资产包**：包 ID 从 900+ 起（官方 0-899 保留），文件放 05_资产库/用户自定义/<包名>/；条目先过 Schema 校验再 asset_register(entry) 注册。
- **替换模块**：新模块须满足被替换模块 inputs/outputs/events 契约（01 §1）；核心模块（M00/M50/M80/注册表）不可替换（I2）。
- **新领域包/新管线**：以 P00 通用骨架（03_管线库/P00_通用文档生成管线.md）为模板复制派生，制作步骤见 community/模板制作指令包.md；题材模块沿用 R2 类别号段（M91-M99 留第三方），经自带 Pxx 管线装载，登记到 02 §8 社区登记表后 Agent 即按注册表调度（I5）。
## 6. 预设切换与数据隔离
- 校园/西幻存档键不同（见两包 README §1 速览表），**不可互载**：切换预设须开新档。
- 建议同一前端实例一次只跑一个预设；协议注入文本按所选包 README §2 装载块替换。
- 混装资产键（校园键进西幻流程或反之）触发 L3 资产可达校验失败——拒绝开跑。
## 7. 验收清单（两级结构终验）
| # | 验收项 | 判定标准 | 状态 |
| --- | --- | --- | --- |
| 1 | 官方核心完整 | 04_模块库 13 件（通用 6 / 事件 5 / 世界 1 / 技术 1，清单见 §1）；03 含 P00/P01/P90 | ☐ |
| 2 | community 两包完整 | 校园：6 模块 + 29 资产 + P02 + README；西幻：14 模块 + 23 内容资产 + P03 + README（含 EXT 段闭合） | ☐ |
| 3 | 资产溯源一致 | 两包资产行数与各自 assets/README.md 溯源表一致 | ☐ |
| 4 | 模块-资产引用可寻址 | 04 与 community 模块引用资产键经五接口可寻址（键表在两包 assets/README.md） | ☐ |
| 5 | 重号 ID 全限定 | M10（通用/生存）、M22（情感/事件）在 01/02/04/06/两包 README 全部类别前缀限定书写 | ☐ |
| 6 | 五条不变式落点 | I1 三正交 / I2 核心固定 / I3 通信契约 / I4 数据隔离 / I5 真相唯一（01 §5、06 §1） | ☐ |
| 7 | 认知边界与输出质检门 | v0.7.13 四层管线（06 §4 与 M23 一致）；M80 gate 三档（06 §5 与 M80 一致） | ☐ |
| 8 | 社区红线落地 | v0.7.12 冲动隔离在校园包 README §5 与 M22/M40 一致；R1 无跨包依赖；R2 类别独占；R3 装配契约 | ☐ |
| 9 | 入口可导航 | 根 README → 07 → 01/02/06 协议链 + community 两包 README + 05 README 全部可访问 | ☐ |
> 执行方式：清单与文件实况对照终验（行数/锚点/闭合性以终端 grep 复核为准）。
---
