# 19 · v2.0.x-E2 协议定义向导（自定义协议 GUI 化，2.0 E2 目标落地）

> **需求提炼**：「E2 自定义协议 GUI 化：自然语言/界面定义 → 生成协议模板（v0.7 协议的 GUI 化落地）」；实现走严格分阶段（方案审批）。批准方案全文见 .rivet/plans/v2-0-x-e2-自定义协议-gui-化-协议定义向导-protocol-yaml-生成器.md。
> **非目标**：不做真 LLM 自然语言生成（表单驱动 + 启发式）；不改 01/02 协议层（生成合规 protocol.yaml）；不做登记自动写 02/registry（登记沿用 v1 三要件流程）。

## 目标

GUI 向导让不懂 Schema 的用户定义第三方协议——填表 → 自动生成合规 `protocol.yaml`（对齐 01 §6.1 Schema v2 + 实例结构）+ 自检 + 导出，把 v0.7 手写 protocol.yaml 门槛 GUI 化。

## 结构依据（真实实例实证）

`protocol.yaml`（community/通用核心基础包 + 校园情感领域包 逐字段）：
```
protocol.schema_version:"2"  +  package.{id,name,pipeline,module_id_range[],
categories[],dependencies{core_only,core_modules[],cross_package[],references[]},
modules[{id,desc}],assets{count,readme},mount_layers{层名:{default[],available[]}}}
```
用户调字段 = id/name/pipeline/module_id_range/categories/modules(动态行)/core_only/mount_layers；结构字段（schema_version/references[]/cross_package[]/assets 计数）向导自动填。

## 方案（B/C 摘要）

- **B core 生成器** `desktop/src/core/protocol_wizard.py`：`ProtocolForm` dataclass + `build_protocol_yaml(form)->str`（schema_version "2" 等自动）+ `self_check(yaml_text)->list[str]`（yaml 解析/schema_version/module_id_range 非空/modules desc 齐/mount default⊆module_id_range——对齐 check14 ①-⑤ 粗校验）。core_modules 缺省 = 官方核心 12 件配合集。
- **C GUI** zone_g 加「创建自定义协议…」按钮 + 向导对话框：表单字段 + 模块动态行 + 层挂载模板 → 生成预览 → 三动作（保存包骨架/导出协议模板 md/自检报告）。

## 文件
- 新增 `desktop/src/core/protocol_wizard.py` + `desktop/tests/test_protocol_wizard.py`
- 修改 `desktop/src/ui/zone_g_community.py`
- 本文件 19（方案真相源）

## 验证
- `python -m unittest discover -s tests` 全绿（wizard 生成结构/自检/编号）
- 生成样例 → self_check PASS；`bash verify.sh` PASS 保持
- GUI 人工（真机窗口）：向导填表 → 生成 → 保存/导出（小问题范畴，可延后）

## 分波
小 feature（单 core + 单 UI + 单测试域）不分波——一次性实现 + 提交（阶段工作流：先推→方案→实现→完成推）。
