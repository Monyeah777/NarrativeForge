# 27 · v2.1.0-A2 MCP server 定义导出（techdoc/协议定义 → MCP 资源型 server，A 线第四格）

> **需求提炼**：「techdoc/协议定义 → MCP server 定义（JSON 协议导出）」；ROADMAP §7.5 A2。批准方案全文见 .rivet/plans/27-v2-1-0-a2-mcp-server-定义导出-techdoc-协议定义-mcp-资源型-server-a-线第四格.md。
> **范围裁决**：NF techdoc IR 模块为自由 markdown（规则/协议正文），无结构化工具参数——硬映射 Tool 会伪造 inputSchema。**本版做 Resources 型**（MCP 官方定义 Resources = "contextual data attached and managed by the client"）：协议/规则模块 → Resource（uri/name/mimeType/text）。Tool/Prompt 留待结构化参数源。
> **非目标**：不做可执行 Tool（无参数源造假不做）；不做 MCP SDK 集成/网络服务（产 server 定义 JSON 文件，非跑 server）；不改 narrative（拒出同 skill/agents 纪律）；不改 IR 结构。

## 核心

新增 `desktop/src/core/mcp_adapter.py`：`export_mcp(ir, dest_dir, res)`——techdoc IR → mcp.json（`mcp{name:<pipeline_id>-mcp, version, capabilities.resources, resources[]}`；每层每模块 → Resource，uri 层级编码 `nf://<pipeline>/<layer>/<name>-<num>`，text=模块归一正文；extra_modules 也入不静默丢）；narrative 拒出 0 文件 + warnings。注册 exporter._REGISTRY['mcp']；nf.py --fmt 扩 mcp。

## 文件

- 新增 `desktop/src/core/mcp_adapter.py` + `desktop/tests/test_mcp_adapter.py`（3 用例：techdoc→resources 字段齐 / extra 入 / narrative 拒出）
- 修改 `desktop/src/core/exporter.py`（注册 mcp）+ `scripts/nf.py`（--fmt 扩）
- 本文件 27（方案真相源）+ README 协议链 + CHANGELOG [Unreleased]

## 验证

- `python -m unittest discover -s tests` 全绿（155→158：mcp +3）
- 实测：真实 P90+M90 techdoc IR → mcp.json（`P90-mcp`，uri `nf://P90/P90/技术文档结构-M90` 层级编码）；narrative → 拒出 0 文件
- `bash verify.sh` PASS=24 + e2e PASSED

## 分波

单波实现 + 提交（core adapter + exporter 注册 + nf + 测试，112aad1）。GUI 不涉（L2 能力）。
