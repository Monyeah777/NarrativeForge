# 22 · v2.0.x-E5 模块市场雏形深化（community 仓库盘点：可发现 → 可装载）

> **需求提炼**：「E4 深化：接 community 仓库盘点——检索可装载未安装的模块/管线 + 一键装载入库，市场雏形从『本地已装』扩到『可发现可装载』」。E5 把 E4（方案 21）明确非目标的 community 盘点检索翻转为目标：community/* 四包（校园 P02/西幻 P03/轻混 P04/通用 P05）modules+ pipelines 纳入检索，未装项一键装载入库（Store.save_module / pipelines cache merge），之后即走既有装配流。批准方案全文见 .rivet/plans/22-v2-0-x-e5-模块市场雏形深化-community-仓库盘点-可发现-可装载.md。
> **非目标**：不做 GitHub 在线拉取（阶段②占位维持——盘点的是仓库内检出 community/ 目录）；不复制 references 引用模块入源包（I5，E3 只读保持）；不改 E4 四类已装/在册检索语义与 test_retriever 既有用例；不改 01/02/registry.json；不新增主窗口 tab；不做 assets 盘点装载。

## 0. 边界裁决（装载 vs references 只读，I5）

两条不同资源进入路径，E5 属后者：E3 references = 组合包跨包引用「源包模块正文运行时只读入装配，不复制进任何包目录」；E5 装载 = 用户把 community/<pkg>/modules 安装进**自己的桌面工作区** ~/.NarrativeForge/modules（等同 seed_from_repo 导入官方核心 / zone_g 目录导入第三方模块的既有模式）。I5 约束仓库内包间不复制；桌面用户库是消费侧工作区。E5 装载的是各包自带模块，不触碰 references 源包归属，与 E3/check15 正交。

## 核心

新增 `desktop/src/core/community_inventory.py`：`catalog(store)` 盘点 4 包模块（parse 出 full_id，store.get_module 判已装）+ 管线（pipelines cache 判已装）；`install_module`（save_module 幂等）/ `install_pipeline`（cache 按 id merge 去重不覆盖既有）/ `load_community_module|pipeline`（只读源）。`retriever.search` 扩 kind=community_module/community_pipeline（**仅显式指定并入，kind=None 全量不混排**——社区项与已装 module 同 ref 双行噪音大，E4 四类语义保持隔离）；Hit tags=[来源包, ✓已装/可装载]。zone_g kind 下拉加「社区模块/社区管线」两档：未装 → 一键装载入库（module 后 on_modules_changed / pipeline 后 reload_pipelines），已装 → 转 E4 动作（加入装配/设为当前管线）；信息列含来源包 + 装载态；refresh_flags/_on_double 扩 community_module。

## 文件

- 新增 `desktop/src/core/community_inventory.py` + `desktop/tests/test_community_inventory.py`（10 用例）
- 修改 `desktop/src/core/retriever.py`（search 扩 community 源，docstring 同步）+ `desktop/tests/test_retriever.py`（+4 用例）
- 修改 `desktop/src/ui/zone_g_community.py`（kind 两档 + 动作分流 + 信息列）+ `scripts/smoke_zone_g_market.py`（[7][8] 装载冒烟）
- 本文件 22（方案真相源）+ README 协议链补 22 + CHANGELOG [Unreleased] 补 E5 条

## 验证

- `python -m unittest discover -s tests` 全绿（121 → 135：inventory 10 + retriever community 4）
- `QT_QPA_PLATFORM=offscreen python scripts/smoke_zone_g_market.py` PASSED（[7] M55 装载入库/标记翻转/不污染装配 [8] P04 cache merge/③ 下拉含）
- `bash verify.sh` PASS=24 保持（无 verify/协议改动）+ e2e PASSED（core 链路不回归）

## 分波

两波：W1 core（inventory+search+单测，69dd4b2）→ W2 UI+冒烟+文档收口。GUI 真机人工验证按用户惯例延后。

## 瑶光反证·范围外记录

android/app/core/registry.json protocols 仅 2 条（缺轻混/通用），verify check13/14⑦ 不断言 android 条数（check 盲区）——预存异常，本方案范围外不修（涉 android+sync+check 补盲，另立议题）；E5 盘点源不读 registry protocols[]，无耦合。若后续跑 sync_android 会覆盖 android registry，需确认无并发会话依赖旧快照。
