# 29 · v2.1.0-B3 协议自举：check14 ⑦ 元素级校验 + 投影生成器（C 底座 + A 主体；B 留最后一公里）

> **需求提炼**：「C 是底座，A 是主体，B 留给最后一公里」——B3 分序贯：**C（治理加固）先做**，**A（投影生成器）主体**，**B（自动登记 CLI）留最后一公里**。调研实证：check14 ⑦ 只比 module_ids/mount_layers 条数不比内容（同长度漂移检不出）；registry protocols[] 投影全手工编辑（registry_loader 只读、scripts 零生成代码）。批准方案全文见 .rivet/plans/29-v2-1-0-b3-协议自举-check14-元素级校验-投影生成器-c-底座-a-主体-b-留最后一公里.md。
> **非目标**：B（自动写 registry.json 的 nf register——02 §9.2「登记自动化留后续」）；不改 registry_loader（保持只读）；不改 01/02 协议 Schema；新增领域包仍须 02 §8 开段 + verify.sh DOMAIN 登记（领域包专属语义，非 C2 可自动）。

## C 底座（verify.sh check14/check15，本方案 Wave 1）

1. **check14 ⑦ 元素级全序**：module_ids 从 `len(p)!=len(range)` 升为逐条全序；mount_layers 从层数比对升为**键归一后层集 + 每层 default/available 逐项**。键归一关键：protocol.yaml 用长键（`P40 行为决策`）、registry 用短键 `P40`（值含 name 字段、protocol 层值无 name）——比对前 `_layer_key = split()[0]` 归一。
2. **包目录 glob 化**（遍历层自动发现，新增组合/通用包零改 verify.sh）：check14 ① 目录循环、python 遍历层（ALL_PKGS）、降级循环；check15 python 遍历层与降级循环——均 `community/*` 扫含 protocol.yaml 目录。**Windows 分隔符兼容**：glob 返回反斜杠 → `replace('\\','/')` 归一（否则 DOMAIN 正斜杠键 vs data 反斜杠键 KeyError，本方案修复的跨平台缺陷）。
3. **③ R2 领域包类别互斥泛化两两**（DOMAIN 显式登记，原只比 PKGS[0]∩PKGS[1]）。
4. **⑤ 编号段 segmap 按包名段落定位**（原硬编码 §8.1/§8.2 下标，新增领域包自动匹配）。
5. 资产特定段（check8/9/10 等）**不动**——M91-99 不占用检查仅领域包语义（通用 M93-96/轻混 M91-92 合法占段）。

## A 主体（protocol_projection.py，本方案 Wave 2）

`desktop/src/core/protocol_projection.py`：`project_entry(pkg_dir)` 读包根 protocol.yaml → registry protocols[] 应并入条目（module_ids=module_id_range 全序、mount_layers 键归一+层名推断、assets/references/schema_version 透传）；`project_all()` 扫 community/* → 全部条目。产物字段与 check14 ⑦ 元素级断言**同构**——集成自证：产物整体替换 registry protocols[] 后跑 verify.sh，PASS=24 保持。I5：产物是「应并入条目」，人工合并，非自动写文件。

## B（遗留，最后一公里）

nf register 自动登记 CLI 未做——记 ROADMAP §7.5 项 3 遗留。registry.json 现状仍人工合并 projection 产物（半自动可核）。

## 验证

- C 波：PASS=24 保持（元素级=长度超集零误伤）+ RED 复现（同长度漂移 M65→M66 被 ⑦ 抓）+ 单测 169 绿。
- A 波：test_protocol_projection（7 用例）——project_all == registry 现有 4 条目逐字段（回归锚点）；集成自证（产物过真实 check14 ⑦）。
- e2e 未跑（本方案不改 runtime core 行为，projection 为登记辅助纯函数）。

## 文件

- 修改 `verify.sh`（check14 ①③⑤⑦ + check15 遍历层/降级 glob）
- 新增 `desktop/src/core/protocol_projection.py` + `desktop/tests/test_protocol_projection.py`
- 本真相源 + README 协议链补 29 + CHANGELOG [Unreleased]
- B 记 ROADMAP §7.5 项 3 遗留（不做）
