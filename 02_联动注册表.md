# 联动注册表

> **registry_schema_version: "2"** —— 注册表结构版本（v0.5.0 T2.3 首引 "1"；v0.6.0 T1.1 机读投影 registry.json 引入 loader 消费字段，结构性演进 bump 至 "2"，V2 首次实战，迁移记录见 §9.3）。读取旧版本注册表遵循 01 协议 §7 版本与兼容规则：只增不删、结构性变更 bump 并注明迁移。

## 1. 当前活跃管线
- 官方活跃管线：P01（标准管线 · 官方核心装配，03_管线库/P01_标准管线.md）
- 社区领域管线：P02（校园情感流）/ P03（西幻生存流）——随 community 领域包分发，由各包独立装载，装配时按 R3 追加挂载与订阅（见 §8）
## 2. 官方核心模块表（13 件）
> v1.1 起采用「官方核心 + 社区领域包」两级结构（01 协议 §6 R1/R2）。本表仅登记官方核心 13 件（04_模块库 保留件）；题材模块（情感 9 + 西幻 14）已迁出至 community/，去向见 §8。
| 模块ID | 名称 | 类别 | 来源 | 挂载点 |
| --- | --- | --- | --- | --- |
| M00 | 数据结构 | 通用 | 核心 | P00 数据基座（active） |
| 通用:M10 | 时间推进 | 通用 | 核心 | P10 世界推进（active） |
| M23 | 认知边界 | 通用 | 核心 | P20 角色状态（active） |
| M24 | 组合规则 | 通用 | 共享 | P70 叙事素材（active） |
| M50 | 主循环 | 通用 | 核心 | 调度器（全局） |
| M80 | 输出生成器 | 通用 | 核心 | P80 输出呈现（active） |
| 事件:M22 | 事件叙事 | 事件 | 核心 | P30 事件生产（active） |
| M06 | 任务剧情 | 事件 | 共享 | P30 事件生产（active） |
| M12 | NPC对话 | 事件 | 共享 | P50 交互执行（active） |
| M13 | NPC交互 | 事件 | 共享 | P30 事件生产（active） |
| M20 | 世界知识库 | 事件 | 共享 | P70 叙事素材（active）/ P00 数据基座（available） |
| M08 | 季节天气 | 世界 | 共享 | P10 世界推进（available） |
| M90 | 技术文档结构 | 技术文档 | 核心（P90 实证样例） | P90（active） |
注：重号模块以类别前缀限定（通用:M10 时间推进 vs 生存:M10 死亡重生；事件:M22 事件叙事 vs 情感:M22 三冲动驱动），注册表内部引用一律使用限定 ID；M91-M99 预留段留给第三方包（§8.3）。
## 3. 订阅关系（官方核心内部固定订阅）
```yaml
subscriptions:
  # —— 核心内部固定订阅 ——
  tick_day:            # 时间推进（通用:M10 发布）→ 季节天气
    publisher: 通用:M10
    subscribers: [M08]        # 社区侧订阅（生存:M01/M19、M40 等）随社区包登记
  quest_state:          # 任务剧情状态（M06 发布）→ NPC 交互
    publisher: M06
    subscribers: [M13]        # 社区侧订阅（M04、生存:M10）随社区包登记
  narrative_event:      # 事件叙事（事件:M22 发布）→ M80 输出门（M80 唯一叙事输入）
    publisher: 事件:M22
    subscribers: [M80]
  interaction_update:   # NPC 交互更新（M13 发布）→ 事件总线
    publisher: M13
    subscribers: [事件:M22]   # 社区侧订阅（M40、M14）随社区包登记
  # —— 动态订阅（随社区题材包装配，来源见 §8 与各包模块头 events 声明）——
  # 事件:M22 监听事件总线：装配校园包时挂接 relationship_change / romance_state_change /
  #   confession_event / npc_action / ghost_event 等；装配西幻包时挂接 combat_result /
  #   death_trigger / chaos_event / market_event / production_output 等。
```
## 4. 依赖关系（官方核心内部）
```yaml
dependencies:
  M00:
    depends: []
    depended_by: [通用:M10, M50, M23, M24, M20, M90]
  通用:M10:
    depends: [M00]
    depended_by: [M08]
  M50:
    depends: [M00]
    depended_by: [M80]
  M80:
    depends: [M50, 事件:M22]
    depended_by: []
  M20:
    depends: [M00]
    depended_by: [M06, M12, M13]
  M23:
    depends: [M00]
    depended_by: [M13, 事件:M22]
  M24:
    depends: [M00]
    depended_by: []
  事件:M22:
    depends: []                # 监听事件总线；具体事件源随社区题材包装配
    depended_by: [M80]
  M06:
    depends: [M20]
    depended_by: [M13, 事件:M22]
  M12:
    depends: [M20, M13]
    depended_by: [M13]
  M13:
    depends: [M12, M06, M20]
    depended_by: [事件:M22]
  M08:
    depends: [通用:M10]
    depended_by: []            # 社区侧依赖（M07/M04/M09 随西幻包）
  M90:
    depends: [M00]
    depended_by: [M80]         # P90 装配内
```
注：社区包模块对核心 13 件的依赖（如 M01-M04 依赖 M20、生存:M10 经事件总线依赖 事件:M22）在社区包装配时登记并校验；社区包之间禁止互相引用（R1）。
## 5. 挂载点（官方核心装配 · 对齐 P01）
```yaml
mount_points:
  P00 数据基座:    {default: [M00], available: [M00, M20]}
  P10 世界推进:    {default: [通用:M10], available: [通用:M10, M08]}
  P20 角色状态:    {default: [M23], available: [M23]}
  P30 事件生产:    {default: [事件:M22, M06, M13], available: [事件:M22, M06, M13, M12]}
  P40 行为决策:    {default: [], available: []}   # 空层（optional）——题材决策模块随社区包装配追加
  P50 交互执行:    {default: [M12], available: [M12]}
  P60 长期演变:    {default: [], available: []}   # 空层（optional）——世界侧长期模块随社区包装配追加
  P70 叙事素材:    {default: [M20, M24], available: [M20, M24]}
  P80 输出呈现:    {default: [M80], available: [M80]}
```
## 6. 当前执行顺序（官方核心装配 · P01）
```
1.  M00 数据结构（初始化）
2.  M50 主循环（调度开始）
3.  通用:M10 时间推进（P10 世界推进）
4.  M20 世界知识库·常识装载（P00 available 槽位，供 P30/P50/P70 引用）
5.  M23 认知边界（P20 角色状态·视角裁剪）
6.  事件:M22 / M06 / M13（P30 事件生产：候选事件集与 NPC 交互请求）
7.  M12 NPC对话（P50 交互执行）
8.  M20 / M24（P70 叙事素材：设定素材与组合校验）
9.  M80 输出生成器（P80 输出呈现）
10. M50（回卷下一回合）
```
- 空层 P40/P60（optional:true）核心装配下直接跳过，不阻塞回合；题材模块由社区包按 P02/P03 装配后追加进对应层并同步本表（R3）。
- 本执行顺序为官方核心装配的真相源；装配社区题材包后，按社区包索引合并生成最终顺序（I5：禁止硬编码 17 步）。
- **冲突裁决（I5）**：04_模块库/通用类/M50_主循环.md 的模块文件表述与本表不一致时，以本表为准；M50 不得维护与本节冲突的静态编号清单。
## 7. 领域实例登记（P90 技术文档 · 文档工厂实证）
> P90 是 P00 通用骨架在**技术文档**领域的派生实例（README「扩展新领域三步之③」闭环样例）。本段为 P90 挂载与执行顺序的**真相源（I5）**；§5 仅登记叙事实例，两实例互不干扰。引擎不变式：M00 数据槽 / M50 主循环 / M80 输出门为唯一核心门，P90 与叙事共用，不改动。

### 7.1 P90 挂载（层位沿用 P00–P80 位 id，层名/模块为技术文档特化）
```yaml
p90_mount_points:
  P00 文档装载:   {active: [M90], available: [M90]}
  P10 语境推进:   {active: [M90], available: [M90]}
  P20 章节状态:   {active: [M90], available: [M90]}
  P30 片段生产:   {active: [M90], available: [M90]}
  P40 结构决策:   {active: [M90], available: [M90]}
  P50 章节装配:   {active: [M90], available: [M90]}
  P60 一致性:     {active: [M90], available: [M90]}
  P70 规范素材:   {active: [M90], available: [M90]}
  P80 文档输出:   {active: [M80], available: [M80]}   # 核心输出门（gate 唯一出口，与叙事共用）
```

### 7.2 P90 执行顺序（10 步，主循环由 M50 调度）
```
1.  M00 数据结构（初始化）
2.  M50 主循环（调度开始）
3.  M90 文档装载（意图 → 类型判定 → 选 TECH_TEMPLATES 骨架）
4.  M90 语境推进（DocState 语境槽更新）
5.  M90 章节状态（章节骨架展开）
6.  M90 片段生产（章节/表格/代码块增量 doc_delta）
7.  M90 结构决策 + 章节装配（骨架装配）
8.  M90 一致性（TECH_RULES 结构/术语/引用/修订校验）
9.  M80 输出生成器（质检渲染输出）
10. M50（回卷下一轮意图）
```

## 8. 社区领域包登记表（v1.1 迁出模块）
> 依据 01 协议 §6 R1/R2/R3：题材内容随社区领域包分发，社区包只能依赖官方核心 13 件（含 §3 五接口），不得跨包依赖；官方侧本表为登记快照，包内完整模块/资产清单、订阅、依赖、挂载与执行顺序以各包 README 与模块头 events/depends 声明为准。
### 8.1 校园情感领域包（community/校园情感领域包/）
- 管线：P02 校园情感流（pipelines/P02_校园情感流管线.md）
- 模块（9）：情感:M22 三冲动驱动、M40 关系深度、M41 恋爱进阶、M43 情敌系统、M55 匿名情书、M57 朋友圈动态、M58 电话通讯、M59 群聊系统、M65 幽灵遗憾（modules/；M57/M58/M59 为 v0.5.0 T2.2 补建，社交触发源文切片 §12.5-12.7 对应模块落盘）
- 资产（30 文件：29 内容 + README）：人格/情绪/关系/社交/遗憾类题材数据（assets/），经 asset_get 寻址，模块内不写死路径
- 独占类别：情感（R2）；挂载层：P40 行为决策为主，装配时写入 P01 空层与社区管线 P02
### 8.2 西幻生存领域包（community/西幻生存领域包/）
- 管线：P03 西幻生存流（pipelines/P03_西幻生存流管线.md）
- 模块（14）：M01 职业成长、M02 种族天赋、M03 技能融合、M04 战斗系统、生存:M10 死亡重生、M11 文字魔法、M17 生产制造、M19 遗传后代、M07 地图区域、M09 经济贸易、M14 阵营声望、M15 帮派势力、M16 建筑据点、M18 混沌事件（modules/）
- 资产（24 文件：23 内容 + README）：世界/生存题材数据（assets/），经 asset_get 寻址
- 独占类别：生存 / 世界 / 事件（R2）；挂载层：P40/P60 为主，装配时写入 P01 空层与社区管线 P03
### 8.3 第三方协议登记（v0.7.0 升级：登记三要件）
- **登记三要件（v0.7.0 起，依据 01 §6.1 第三方协议声明）**：社区领域包接入平台调度（经门禁识别执行）须三要件齐备——①包根目录携带机读协议声明 `protocol.yaml`（01 §6.1 Schema，必填字段 12 项，check14 ①-⑥ 断言）；②本表 8.1/8.2 在册（含目录/管线/模块/资产/类别登记）；③`registry.json` 的 `protocols[]` 投影在列（§9.1 字段映射）。**三要件缺一即不被平台门禁识别**。
- **M91-M99 编号段**：继续留给后续第三方领域包；新包**新增**模块编号一律落 M91-M99，不得占用官方核心 13 件编号与既有社区包编号；对官方模块与既有社区包模块仅允许「重号限定引用」（如 `情感:M22`、`生存:M10`）。新包须遵循 R1/R2/R3，并按上述三要件登记后方可被平台识别。
- 扩展新领域三步范式见根 README 与 community/模板制作指令包.md。
- **通用类整包登记实况（v1.1.0 起，通用核心基础包战例）**：通用核心基础包（community/通用核心基础包/）——通用类整包（官方核心底座依赖引用 + 自带模块落 M91-M99 社区预留段；独占类别：通用，R2，与既有 [情感]/[生存]/[世界]/[事件]/[轻混] 均无交集）。
  - 管线：P05 核心基础流（pipelines/P05_核心基础流管线.md；九层 linear flow P00→P80→P00 回卷，P00/P10/P40/P80 四驻留层各挂 M93/M94/M95/M96，官方核心模块不搬移只叠加）
  - 模块（4，M91-M99 社区预留段）：M93 通用状态快照（P00 数据基座，订 通用:M10 节拍）、M94 通用节拍桥（P10 世界推进，节拍折算缓冲）、M95 通用决策汇总（P40 行为决策，订官方 M06/M13 + 本包 M93/M94）、M96 通用输出润色（P80 输出呈现，订官方 事件:M22 + 本包 M95）（modules/）
  - 依赖（R1）：core_only true + core_modules 12 件官方核心配合（M00/通用:M10/M08/M23/事件:M22/M06/M12/M13/M20/M24/M50/M80——官方文件不复制进包，仅依赖引用，I5 单一真相源）；cross_package [] 恒空、references [] 零跨包引用（v2 必含键，空表同构于组合包引用通道）
  - 资产（0 内容文件 + README.md）：机制包无题材资产（同轻混组合包零资产先例，I4；与组合包 asset_readonly 只读借阅不同，本包零借阅）
  - 挂载层：P00 default [M93]、P10 default [M94]、P40 default [M95]、P80 default [M96]——与官方核心同层 default 取交集为空（官方四驻留层 default 均空数组，Zero-overlap 实证）→ 只叠加不搬移合规
  - 三要件：①包根 protocol.yaml（schema_version "2"）✅ ②本表在册（本节）✅ ③registry.json protocols[] 第 4 条投影在列 ✅（§9.1）——三处一致由 check14 ⑦ / check15 ⑤ 逐条断言
- **技术文档题材域包登记实况（v2.1.0-C-b 起，技术文档域包战例）**：技术文档域包（community/技术文档域包/）——非叙事题材域包（官方核心 + 自带模块落 M91-M99 社区预留段；独占类别：技术文档，R2，与既有 [情感]/[生存]/[世界]/[事件]/[轻混]/[通用] 均无交集）。本包是 community 第 5 包、第一个非叙事题材域包——README「扩展新领域三步」完整战例（C 线 techdoc 域）。
  - 管线：P06 技术文档题材装配流（pipelines/P06_技术文档题材装配流管线.md；九层 linear flow P00→P80→P00 回卷，P40/P60 两驻留层各挂 M97/M98，M90 官方结构模块 core 依赖协作不搬移）
  - 模块（2，M91-M99 社区预留段）：M97 术语管理（P40 行为决策，维护 DocState.terms）、M98 修订记录（P60 长期演变，维护 DocState.revision）（modules/）
  - 依赖（R1）：core_only true + core_modules 13 件官方核心配合（含 M90 技术文档结构——本包题材底层；官方文件不复制进包，仅依赖引用，I5 单一真相源）；cross_package [] 恒空、references [] 零跨包引用
  - 资产（0 内容文件 + README.md）：机制增强域包无题材资产（同通用核心基础包零资产先例，I4）
  - 挂载层：P40 default [M97]、P60 default [M98]——与既有社区包同层 default 取交集为空（校园 P40 含 情感:M22/M40/M41/M43/M55、P60 含 M40/M65；西幻 P40 含 M04/M11、P60 含 生存:M10 等；M97/M98 新编号唯一）→ 只叠加不搬移合规
  - 三要件：①包根 protocol.yaml（schema_version "2"）✅ ②本表在册（本节）✅ ③registry.json protocols[] 第 5 条投影在列 ✅（§9.1）——三处一致由 check14 ⑦ / check15 ⑤ 逐条断言

### 8.4 组合管线登记（v0.8.0 引入：模块组合）
> **组合包定义**：组合包 = 经 `protocol.yaml` `references`（01 §6.1，schema v2 起）声明跨包模块引用、将多个源包模块组合装配进自带管线的第三方包。组合包**不自带被引模块副本**——被引模块仍归源包（I5 真相唯一，防双源漂移），references 仅承载跨包引用白名单；references 为唯一跨包引用通道，`dependencies.cross_package` 恒空（deprecated 兼容字段，v1 声明零改动向后兼容）。
- **登记判定四规则（v0.8.0 起，check15 ①-③ 断言）**：①**在册可寻址**——`references[].source_package` 须在 §8.1/8.2（或 registry `protocols[]`）在册可寻址、`module_id` 须在源包 `modules[]`/`module_id_range` 在列（全限定 id 引用如 `校园:M55`，原模块不改号）；②**依赖闭包闭合**——以 references 为起点沿源包 `dependencies.core_modules`（递归 references）展开依赖闭包，不得成环/悬空，叶节点 ⊆ 官方核心 13 件（依赖闭包必须闭合到官方核心层）；③**同层 default 唯一**——组合包 `mount_layers` 各层 default 与源包同层 default 取交集非空即冲突（跨题材叠加只允许互斥层位/空槽/available 槽位装配）；④**契约断裂 FAIL 阻断 + 自动仲裁（v1.0.0 已收口）**——跨题材组合存在题材契约差异（各源包红线/约束互不相同），装配前门禁对跨题材 references 自动仲裁题材契约一致性；**仲裁判据 = 模块头机读契约（01 §1.1 `machine_contract` 的 inputs/outputs/events，C1/T1-1 已落地 13 件全覆盖）+ 01 §1 events publish⊆subscribe 语义**——相邻装配须满足「源包发布面 ⊆ 邻居订阅面」（publish ⊄ subscribe 即契约断裂）。判据全过 = 契约一致，放行装配；任一判据不满足 = 契约断裂，门禁对断裂 references 以 **FAIL 级阻断装配**并输出断裂明细（违约模块全限定 id、越界/缺失事件、判据依据）；原「装配前人读复核」降为装配辅助提示（WARN 级备注，不参与阻断判断）。机读断言由 check16 契约仲裁承担（随 T1-3 落地、verify.sh v2.6 入段 C 起无条件执行；入段前装配门禁按本规则人工复核执行，PASS=20 中间态保持）。
- **与 §8.1/8.2 整包登记的关系**：组合包（无论纯引用还是自带模块 + 引用）同样须三要件——①包根 `protocol.yaml` 在场（schema_version "2"；references 为 protocol.yaml 内部字段，非独立文件）；②本表在册（组合声明登记于本节，或整包登记于 §8.1/8.2）；③`registry.json` `protocols[]` 投影在列（§9.1）。三要件缺一即不被平台门禁识别（同 §8.3）。
- **组合登记实况（v1.0.0 起，完整战例）**：校园西幻轻混组合包（community/校园西幻轻混组合包/）——校园 × 西幻轻混跨题材组合（自带管线 + 自带模块 + references 引用源包；独占类别：轻混，R2 组合类，与源包 [情感]/[生存] 无交集）。
  - 管线：P04 轻混装配流（pipelines/P04_轻混装配流管线.md；九层 linear flow P00→P80→P00 回卷，P40/P50 各挂 M91/M92，与源包管线同节拍并行，只叠加不搬移）
  - 模块（2，M91-M99 社区预留段）：M91 异界身份桥（P40 行为决策）、M92 轻混装配执行（P50 交互执行）（modules/）
  - references（2，跨包引用白名单）：校园情感领域包 M55 匿名情书 / 西幻生存领域包 M17 生产制造（source_schema_version "2" + asset_readonly true；原模块不改号仍归源包，I5 真相唯一）——契约一致性由规则④自动仲裁（check16：M92 subscribe ⊇ M17 publish、M55 publish=[] 无越界，闭合通过）
  - 资产（0 内容文件 + README.md）：源包资产经 references.asset_readonly 只读授权借阅（asset_get），不复制（I4）
  - 挂载层：P40 行为决策 default [M91]、P50 交互执行 default [M92]——与源包同层 default 取交集为空（校园 P40 含 情感:M22/M40/M41/M43/M55、西幻 P50 含 M04/M11/M17）→ 规则③同层 default 唯一合规
  - 三要件：①包根 protocol.yaml（schema_version "2"，references 内部字段）✅ ②本表在册（本节）✅ ③registry.json protocols[] 第 3 条投影在列 ✅（§9.1）——三处一致由 check14 ⑦ / check15 ⑤ 逐条断言
- **Schema 说明示例**（纯引用型组合的文档级 YAML 骨架，验证 Schema/门禁可接受；**完整可玩战例已落盘上述实况条目**——11 §6 开放问题 3 已由本组合包兑现，不再留档延期）：
```yaml
# protocol.yaml 组合声明骨架（文档级示例，未实际落盘）——跨题材组合：校园 × 西幻轻混
protocol:
  schema_version: "2"            # 组合声明 Schema v2（references 支持）
package:
  id: 组合示例包                  # 组合包目录名（示例；真包须按三要件登记）
  name: 组合示例包（校园 × 西幻轻混）
  pipeline: P04                   # 自带组合管线示例名（示例；未在 03_管线库落盘）
  module_id_range: []             # 组合包不自带新增模块（模块全部经 references 引用源包）
  categories: []                  # 组合包不独占类别（R2 类别权归源包各自声明）
  dependencies:                   # R1 依赖边界
    core_only: true               # 只依赖官方核心层
    core_modules: [M00, 通用:M10, M08, M23, 事件:M22, M06, M12, M13, M20, M24, M50, M80]   # 官方核心 13 件子集（依赖闭包闭合官方核心层）
    cross_package: []             # deprecated 兼容字段（恒空）；references 为唯一跨包引用通道
  references:                     # v2 跨包引用白名单（登记判定四规则：在册可寻址/闭包闭合/同层 default 唯一/契约断裂 FAIL+自动仲裁）
    - {source_package: 校园情感领域包, module_id: M55, source_schema_version: "2", asset_readonly: true}   # 匿名情书（原模块不改号；M55 无重号，裸号引用，同 §8.1 登记）
    - {source_package: 西幻生存领域包, module_id: M17, source_schema_version: "2", asset_readonly: true}   # 生产制造（原模块不改号；M17 无重号，裸号引用，同 §8.2 登记）
  modules: []                     # 不自带模块（I5：被引模块仍归源包，防双源漂移）
  assets:                         # 资产声明
    count: 0                      # 不自带资产（源包资产经 references.asset_readonly 只读授权）
    readme: README.md
  mount_layers:                   # 挂载层叠加声明（规则 ③：与源包同层 default 取交集为空方合规）
    P20 角色状态: {default: [], available: []}          # 无叠加（源包层位互斥）
    P30 事件生产: {default: [], available: []}          # 无叠加
    P40 行为决策: {default: [], available: [M55]}   # 校园源包 P40 default 已含 M55 → 入 available 槽（规避 default 叠加冲突；裸号，同 §8.1 登记）
    P50 交互执行: {default: [], available: [M17]}   # 西幻源包 P50 default 已含 M17 → 入 available 槽（同上；裸号，同 §8.2 登记）
    P60 长期演变: {default: [], available: []}          # 无叠加
```
---
## 9. 机读投影 registry.json（v0.6.0 T1.1 引入）
> **投影声明**：`desktop/src/core/registry.json`（仓库内；android/app/core/ 下为 sync_android.sh 生成物，不入库）是 **02 文档的机读投影**——单一事实源仍为本文档（I5 不变式：文档层 + 机读投影双校验唯一）；json 只承载 §2 模块表 / §3 订阅 / §5 挂载点的结构化等价物，供 desktop/android Runtime 装载器（registry_loader.py，T1.2 引入）只读消费，不反向写回本文档。M90/P90 技术文档领域实例（§7）与动态订阅（社区包装配挂接）不纳入投影。
### 9.1 字段映射（02 ↔ registry.json）
| 02 节 | 02 表述 | registry.json 字段 |
| --- | --- | --- |
| 头部 | registry_schema_version "2" | `registry_schema_version` |
| §2 模块表 | 模块ID/名称/类别/来源/挂载点 | `modules[]`：id / name / category / source / mounts（layer+status，active=默认挂载、available=可用槽位）；重号模块用限定 ID（通用:M10、事件:M22）；M50 无层挂载以 `scheduler: true` 标记 |
| §3 订阅 | 核心内部固定订阅五组（publisher/subscribers） | `subscriptions`：键名 tick_day/quest_state/narrative_event/interaction_update，publisher/subscribers/note |
| §5 挂载点 | P00–P80 层 default/available + 空层说明 | `mount_points`：default / available / optional（P40/P60 空层 optional=true） |
| §8 社区登记 | 社区包登记条目（包目录/管线/类别/模块/资产/挂载层） | `protocols[]`：id / name / pipeline / categories / module_ids / assets / mount_layers / schema_version（v0.7.0 引入，随社区包登记） |
| §8.4 组合登记 | 组合包 references 组合清单（跨包引用白名单；跨题材契约一致性由规则④自动仲裁——判据 = 模块头机读契约 `machine_contract` inputs/outputs/events + 01 §1 events publish⊆subscribe，不新增投影字段） | `protocols[].references[]`：source_package / module_id / source_schema_version / asset_readonly（v0.8.0 引入，随组合声明登记） |
### 9.2 同步纪律
- 改 02 的 §2/§3/§5（或头部版本号）**必同步** registry.json，反之亦然；二者一致性由 verify.sh check13 断言（T2.3 实现，入段 C 无条件执行）。
- 注册表内容变更一律经 02 文档发起（json 为派生投影）；直接改 json 不经 02 = 违约（I5）。
- 字段级改动遵循 01 §7 V1（只增不删）/ V2（结构性变更 bump+迁移记录）规则；新增可忽略字段属 V1 兼容演进。
- **社区登记同步（v0.7.0 起）**：新包登记 / 既有包协议变更（protocol.yaml 字段或 02 §8 条目）必同步 `registry.json` `protocols[]`，反之亦然；二者一致性由 verify.sh check14 ⑦ 断言（v2.4 实现，入段 C 无条件执行）。
- **组合同步（v0.8.0 起）**：02 §8.4 组合登记 ↔ protocol.yaml references ↔ registry protocols[] references 三处一致（source_package / module_id / source_schema_version / asset_readonly 逐条比对），反之亦然；一致性由 verify.sh check15 ⑤ 断言（v2.5 实现，入段 C 无条件执行）。
- **契约仲裁收口（v1.0.0 起，T1-2）**：02 §8.4 规则④已由「契约 WARN 提示」升级为「契约断裂 FAIL 阻断 + 自动仲裁」——组合登记跨题材 references 的契约一致性由装配门禁自动仲裁（判据 = 04 模块头机读契约 machine_contract inputs/outputs/events + 01 §1 events publish⊆subscribe）；仲裁**不新增 registry 投影字段**（protocols[].references[] 四字段不变，check13/check15 ④ 中间态不受动），机读断言由 check16 契约仲裁承担（随 T1-3 落地、verify.sh v2.6 入段 C 起无条件执行，PASS 20→22；入段前装配门禁按规则④人工复核执行）。
- **协作流程纪律（v1.0.0 起，T2-1）**：社区包**登记 = 第三方协作流程终点的机读落地**——人读流程走 CONTRIBUTING「社区包协作流程」（提交 → PR 评审 → 登记 → 发布五步，含前置协议合规自检；第三方从提交到登记发布的完整协作流程自此有文档可循，兑现 10 §6 开放问题 4「流程治理留待 v1.0.0 生态收口」）；登记动作的机读断言仍由上文各 check 承担（check14 ⑦ `protocols[]` 投影一致性 / check15 ⑤ references 三处一致 / check16 契约仲裁）。流程治理本版 = 文档化，不做平台化/工具化（PR 机器人 / 自动登记流水线留后续）。
- **本地登记助手（v2.1.0-B3-B 起，nf register，30 方案）**：在包已本表 §8 在册（登记三要件②）且 protocol.yaml 经 check14 ①-⑥ 合规的前提下，`nf register --apply` 把 protocol.yaml 派生投影同步入 registry.json `protocols[]`（id 命中更新 / 缺失追加 / 只增不删，V1 底线；合并后仍须跑 `bash verify.sh` 由 check14 ⑦ 元素级断言自证）——属 02 发起登记的**机读落地**（内容源头 = 02 文档 + protocol.yaml，json 仅派生同步），非独立内容变更，不违本节「直接改 json 不经 02」句。仍是**人工触发**本地命令；PR 机器人 / 自动登记流水线（外部事件自动触发写 json）仍留后续，本节「平台化/工具化」边界不破。
### 9.3 迁移记录（v1 → v2，V2 首次实战战例）
按 01 §7 V2 规则（结构性变更必须 bump 并注明迁移说明），本次迁移四步：
1. **现状快照**：v1 注册表 = 02 文档头部携带 `registry_schema_version: "1"`，内容为纯人读 Markdown（§2 表格 / §3 YAML / §5 YAML），无任何机读投影文件，代码层不消费（desktop/android 各自按文件系统约定读取 03/04/05）。
2. **bump 声明**：`registry_schema_version: "1"` → `"2"`（02 头部 + registry.json 同步），依据 = 机读化引入 loader 消费字段（modules[].mounts、mount_points[].optional、subscriptions）属**新增结构性字段**的演进，触发 V2。
3. **迁移说明**：v1→v2 期间对既有数据的兼容性——**只增不删**（V1 底线）：v2 完全覆盖 v1 语义（§2 13 件模块 ID/类别/来源不变，仅挂载点列由人读字符串机读化为 mounts 数组；§3 五组固定订阅键名/发布者/订阅者不变；§5 P00–P80 九层 default/available 逐条不变）；v1 读者（人读文档流程）不受影响，v2 新增的 registry.json 仅追加消费入口，不改变任何既有模块职责。
4. **校验回读**：registry.json `registry_schema_version` == 02 头部版本号；json modules 与 02 §2 逐条一致（check13 断言，T2.3 实现）；本迁移记录即 check13「bump 实体必有迁移记录」的 v1→v2 满足项。
