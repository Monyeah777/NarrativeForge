# 35 · v2.4.0 外部规范同步核查报告（A1/A2/A3 三连）

> **性质**：一次性实证核查（35 方案 Wave1）——文档落盘不建机制。对照 NF 三个 export 适配器产物 vs 外部最新规范，逐字段实证差距 + 裁决。
> **取证方式**：A1 规范经 agentskills.io/specification 实抓（HTTP 200）；A2 规范经 SillyTavern 官方仓库 `src/validator/TavernCardValidator.js` SSH clone 实读（权威 validator）；A3 规范经 openai/agents.md 官方仓库 SSH clone 实读 README。NF 产物字段经 read_file 三个 adapter 源码。清单侦察给的「kwaroran CCV3 v3」地址经 SSH 验证**仓库不存在**（ERROR: Repository not found）——A2 权威源修正为 SillyTavern 官方 validator。

## A1 · SKILL 出口 vs agentskills.io 规范

规范实测字段（agentskills.io/specification）：

| 字段 | 必填 | 约束 |
|---|---|---|
| name | 是 | ≤64 字符，仅小写字母/数字/连字符，不得首尾连字符，禁连续 `--`，必匹配父目录名 |
| description | 是 | ≤1024 字符，非空，含关键词 |
| license | 否 | 许可证名或引用 bundled license 文件 |
| compatibility | 否 | ≤500 字符，环境要求 |
| metadata | 否 | 任意 string→string 映射 |
| allowed-tools | 否 | 空格分隔预批准工具串（Experimental） |

NF `skill_adapter._build_skill_md` 现状（desktop/src/core/skill_adapter.py L27-52）：frontmatter 仅 `name` + `description` 两字段。

**差距裁决**：
- G1 name 约束：NF `_slug`（L22-23 `re.sub(r"[^\w\u4e00-\u9fff-]+", ...)`）**保留中文**，规范要求 name 仅 `a-z/0-9/-`——NF 产物若 title 含中文，name 违规。⚠️ 差距。
- G2 description：NF 生成描述为流水拼接（pipeline 名 + 计数），符合「非空 + 含关键词」基本要求，但未必 ≤1024（长装配可能超）。⚠️ 需机读上限检查。
- G3 license/compatibility/metadata/allowed-tools：NF 均未产——**四字段全缺**。其中 metadata（author/version）与 license 属低风险缺省（可选），allowed-tools（Experimental）可后置；**license 建议补**（NF 产物分发时应声明许可）。
- G4 body 渐进披露：规范建议 body<500 行、<5000 token、主文件拆 references——NF 层序正文可能超，属建议性（非硬 fail）。

**裁决**：A4 机读化 G1（name 只允许小写连字符）+ G2（description ≤1024）+ G4（body 行数/长度警告）为硬约束；G3 license 补默认字段（Proprietary 或 bundled LICENSE），compatibility/metadata 留 exporter 可选参数。

## A2 · CCV3 出口 vs SillyTavern V3 validator

规范实测（SillyTavern TavernCardValidator.js L144-153）：

| 项 | 约束 |
|---|---|
| spec | 必须 `'chara_card_v3'` |
| spec_version | `Number(spec_version) >= 3.0 && < 4.0`（数值字符串） |
| V2 character_book | entries[] + extensions 字段在场 |

NF `ccv3_adapter.map_ir_to_ccv3` 现状（L95-125）：`spec="chara_card_v3"`（✅ 正确）、**`spec_version="v3"`（字符串 "v3"）**。

**差距裁决**：
- G1 **spec_version 致命差距**：NF 写 `"v3"`，SillyTavern validator 用 `Number("v3")` = `NaN`，`NaN >= 3.0` 为 false → **ST 导入校验会 fail**。正确应为 `"3.0"`（数值字符串）。这是本次核查发现的最实质 bug 级差距，A4 必须机读化。
- G2 spec 字段值：`"chara_card_v3"` 正确，无差距。
- G3 character_book.entries：NF 产 entries（world_entries），但 V3 下 character_book 是否仍要求 extensions 字段需实测确认——V2 段要求 `extensions` 在场，V3 段（L144+）只查 spec/spec_version 未见 extensions 硬要求，待 A4 实测 validator 全链路确认。

**裁决**：A2 结论 = spec_version 改 "3.0"（一处改，修复 ST 导入失败）；world.json key/插入顺序字段差距留 B3（依赖本结论再动，不属本波）。

## A3 · AGENTS.md 出口 vs openai/agents.md

规范实测（openai/agents.md 官方 README）：AGENTS.md 是「README for agents」开放格式——**无强制 frontmatter、无硬字段约束**，最小示例为自由 Markdown 章节（Dev environment tips/Testing instructions/PR instructions）。

NF `agent_rules_adapter._build_agents_md` 现状：`# Agent Operating Rules` 标题 + 层/模块章节组织正文，自由 Markdown。

**差距裁决**：基本一致（都是自由 Markdown 指导文件）。NF 产物无硬字段不合规风险。唯一可留意的趋势：openai/agents.md 生态建议按「环境提示/测试/PR」等可预测段落组织——NF 按 IR 层组织，语义等价但非其惯例，**无需改**。MDA Open Spec 趋势记录：sno-ai 仓库（★616）agent 面向文档的 Markdown 超集，跟踪不落地（C 级）。

## 结论与 Wave2 改点汇总

1. **A2 是唯一 bug 级差距**：spec_version "v3"→"3.0"（修复 ST 导入 fail），A4 check22 机读化拦截回归。
2. **A1 三个硬约束入 A4**：name 仅小写字母/数字/连字符（禁中文/首尾连字符/连续 `--`/≠父目录）、description ≤1024、body 行数/token 警告。
3. **A3 无硬差距**：AGENTS.md 开放格式，NF 产物合规。
4. A4 check22 落地清单 = 上述 A1+A2 硬约束机读化到 export_schema + verify。

## 证据索引

- A1：https://agentskills.io/specification（web 实抓 2026-09，HTTP 200，name/description/license/compatibility/metadata/allowed-tools 字段表）。
- A2：SillyTavern/SillyTavern `src/validator/TavernCardValidator.js`（SSH clone 实读，L144-153 V3 spec/spec_version 约束；清单侦察的 kwaroran/CCV3 仓库不存在已修正）。
- A3：openai/agents.md 官方仓库 README（SSH clone 实读，开放格式无硬字段）。
- NF 产物：desktop/src/core/skill_adapter.py L27-52、ccv3_adapter.py L95-125、agent_rules_adapter.py。
