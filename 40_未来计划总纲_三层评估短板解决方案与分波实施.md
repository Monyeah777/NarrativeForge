# 40 · 未来计划总纲（v1.4 · 2026-09-07 用户批准 · 增补至云端公共线 Y9′–Y12′）

> **需求提炼**（用户裁决链）：「你可以拉取其他同类项目对比…项目内每一个内容点都进行专业评估…站在更广阔的梯度来最终评价」→ 三层专业评估输出（2026-09-06）→「自审之后就知道应该怎么继续更新了，全部都给我弄成未来计划，每一个不足的地方都给我弄出解决方案」→ **本总纲 = 三层评估全部非 A 级短板 → 解决方案 → 分波实施路线**，作为后续基础层版本演进主线 + 壳集中波的总索引。
> **节奏原则**（贯穿全部）：基础层高频推进（无壳版本继续发布）；端壳与基础层彻底分开，壳端最后集中一次性做（v2.6 资产 = 技术地基，保留不删）；脉冲式接线、按需装配、波后回冻结。

---

## 第一部 · 短板全景与解决方案总表

按优先序排列（P0 = 立即 / P1 = v2.7 / P2 = v2.8 / P3 = 壳集中波 / P∞ = 常态机制）。

| # | 短板（评估引用） | 现状实况 | 影响 | 解决方案 | 优先 |
|---|---|---|---|---|---|
| S1 | **外部验证为零 + 消费闭环未验证**（D / #9、#16） | star=1；README 声称「输出交给 SillyTavern 等使用」，**从未实测**；全部验证为 AI 产→AI 验自我闭环 | 全项目最大结构性风险：不可替代性停留在声称；对外宣称与事实存在差距 | **外部验证波**：E1 CCV3 导出→SillyTavern 装载实测（第一锚点）/ E2 SKILL→Claude Code、AGENTS→Claude Code 实测 / E3 MCP serve→标准 MCP 客户端实测；产出 = 兼容性实证报告 + 差距修复 + README 从「声称」改「实测」 | P1 |
| S2 | **资产供应链机制全缺**（C− / #13，机制体检最弱项） | 官方 0 资产；作者侧供应链全缺（生产/入库/溯源/浏览/版本/淘汰六环断）；现有 = 校验+寻址中段半成品 | 货架没有进货工具，工厂无产值；资产只能靠手写两包 | **nf asset 族补全**：add / verify / inventory / ls --pkg --tier / rm / deprecate + 溯源键表自动生成 + 资产版本位 + 供应链闭合新 check 入 verify | P1 |
| S3 | **货架空转 + 第三方上架无先例**（B / #15） | community 5 包 102 文件全是自家货，无第三方上架实战 | 工厂机制无外部货品流入，生态未启动 | 首批商品按货架逻辑生产（tier 可发现/可装载/可淘汰）+ **第三方上架 SOP 首次实战**（A7 上架元数据 + 评审流程真实走一遍） | P1–P2 |
| S4 | **管线生产端脚手架缺位**（B / #11） | P00 骨架设计优良（派生即用、引擎零改动），但官方仅 3 条；无 `nf pipeline new` | 新领域管线生产靠手抄复制，生产端断环 | **nf pipeline new --from P00 --domain \<d\>** + protocol.yaml 管线槽位 + P00 派生骨架目录；验收：新领域从命令到可跑 ≤ N 分钟 | P2 |
| S5 | **模块淘汰流转缺位**（B+ / #12） | 13 官方 + 23 社区题材模块，登记即调度；无 stable→deprecated→retired 通道 | 模块只进不出，库会随演进腐化 | 模块头 status 位（active/deprecated/retired）+ `nf module deprecate/restore` + check 门禁（引用 deprecated 模块 WARN→FAIL 策略）+ 首例流转战例 | P2 |
| S6 | **CI 打包触发与壳后置冲突**（A− / #5） | build-desktop/build-android 当前随 v* tag 触发 → v2.6 产了用户本不需要的壳资产 | 违背「壳端与基础层分开节奏」哲学；每次基础层发布重复产壳 | build workflow 触发改为**仅手动 dispatch + 专用壳波 tag**（如 shells-v*）；普通 v* tag 不再产壳 → **v2.7 = 首个无壳发布版本，分离哲学首次在发布线兑现**；README/38 文档同步口径 | P0 |
| S7 | **产品上手门槛**（C− / #18） | 无壳 = 无图形入口；CLI 对非技术作者门槛高 | 外部验证与用户增长受阻于开箱体验 | 近端（基础层可做，不依赖壳）：CLI 引导分级（nf --help 按作者/开发者分层）、`nf demo` 一键生成演示世界、README 五分钟快速开始；远端 = 壳集中波解决 | P2 / P3 |
| S8 | **安全纪律薄弱**（C / #8） | 历史 token 明文暴露教训存在；无密钥轮换 SOP、无 secret 扫描、workflow 权限未最小化 | 进社区前的高危隐患 | ① 密钥轮换 SOP（旧 token 弃用记录）② secret 扫描入 CI（常见 token 前缀 grep）③ CONTRIBUTING 安全章程小节 ④ workflow permissions 最小化（contents: read）⑤ Release 发布 sha256 校验和 | P0 部分 / P1 全量 |
| S9 | **双上下文漂移风险**（第三层教训） | 执行线在对话外独立完成 v2.6 全量 33 提交，与对话规划吻合但非同一过程 | 规划与实际分叉 → 评估错位（已实际发生一次：#17 用户暴批） | **执行对账清单化**：大动作前 fetch 核对；规划文档 = 唯一真相源（本总纲即第一例）；波次完成对照表（计划项 vs 实际提交）入 CHANGELOG（扩展已有计数机制为对账格式） | P0 |
| S10 | **自我验证闭环无外部评审**（第三层） | 所有验证 AI 产→AI 验；无外部评审缓冲 | 盲区只能靠 M_AUDIT 自审，仍属内循环 | 每波发布前强制 audit full 建档（机制已有，执行纪律化）+ 每波结束后「评审窗」：邀请第一个外部读者试用（作者社交圈/相关社区） | P∞ |
| S11 | **治理收尾遗留**（评估遗留） | M_AUDIT 三开放问题未裁（模板 Prompt 落盘时机/audit.md 存放位/steelman_ref 字段层级）；38 合并稿缺 A→B→T 段未转正式；APK 定位措辞未确认 | 悬而未决的小决策滞留 | 三项小决策列出待拍板（见第三部）；38 方案按「壳分离」原则补段转正式；APK 措辞二选一裁决（「能力对齐、交互从简」vs「浏览/预览/轻导出级」） | P0 |
| S12 | **壳与产品未成形**（B− / #17 有意为之） | PySide6 + KivyMD 技术地基就位（v2.6 已留）；产品形态未做（对齐能力、交互从简） | 按用户裁决这**不是短板**——壳最后集中做 | 维持冻结；纳入 P3 壳集中波（引用 38 方案合并稿），触发条件 = 基础层进入收敛期或用户裁定 | P3 |
| S13 | **对外宣称口径风险**（第三层衍生） | README/仓库描述部分能力宣称未经外部实测（如「交给 SillyTavern 使用」） | 诚实性风险：被外部用户第一眼发现差距 | 未实测宣称统一标注 [待实测]；S1 外部验证波完成后逐项转正 | P1 |

---

## 第二部 · 分波实施路线

```
时间轴（基础层高频）              （壳线冻结 ← 单独触发）
────────────────────────────────────────────────────────────
P0 治理修订波 ──► v2.7 基础层波A ──► v2.8 基础层波B ──► … ──► 壳集中波(P3)
   无tag          tag v2.7.0          tag v2.8.0            （38 方案）
   S6/S8①④/S9/S11  无壳发布首例         S4/S5/S7近端/内容续
                   S1/S2/S3/S8全量
```

### 波 0 · 治理修订（立即，不产版本 tag）
- **S6**：build-desktop.yml / build-android.yml 触发策略改为「workflow_dispatch 手动 + shells-v* tag」；README L3 段与 38 文档同步口径。
- **S8**：旧 token 弃用记录入 CHANGELOG 安全段；workflow permissions 最小化（contents: read + 构建 job 单独提权）；发布步骤追加 sha256。
- **S9**：执行对账清单模板落 `.rivet/`（fetch 核对 → 规划对照 → 差异说明三段式）；本总纲登记为 v2.7+ 唯一真相源。
- **S11**：三项小决策请用户拍板（第三部）；38 方案补 A→B→T 段按「壳分离」原则转正式。
- 验收：ROADMAP 新增「未来计划」节指向本总纲；verify PASS=31 不变。

### 波 A · v2.7 基础层「供应链与外部验证」（下一个基础层版本）
- **S1 外部验证波**（波 A 主产出）：
  - E1：取现有 CCV3 导出产物（校园/西幻包任一模块）→ 按 SillyTavern 角色卡导入实测 → 记录：加载成功 / 字段映射偏差 / 修复清单。**成败均为资产：成功 = 全项目第一个外部验证锚点；失败 = 暴露出口层真实差距（比自证更值钱）**。
  - E2：SKILL 导出产物 → Claude Code 装载实测；AGENTS 产物 → Claude Code 项目级实测。
  - E3：`nf serve`（MCP stdio）→ 任一支持 MCP 的客户端（Claude Desktop 类）连接实测。
  - 产出物：`docs_external-validation-v2.7.md` 实证报告（每出口：环境/步骤/结果/差距/修复）。
- **S2 nf asset 族**（波 A 机制主产出）：子命令 add / verify / inventory / ls / rm / deprecate；溯源键表随 add 自动生成；资产头 version/status 位；verify 新增供应链闭合 check（每资产可溯源、可发现、键无孤儿）；单测随行（verify 保持 PASS=31+）。
- **S3 首批商品**：按货架逻辑上架 2–3 个官方小资产集（与校园/西幻包联动，tier 分级），作为 `nf asset add` 的首批入库战例。
- **S8 全量**：secret 扫描入 ci-verify；CONTRIBUTING 安全章程小节。
- **S13**：实证报告完成后 README 宣称逐项转正。
- 收口：**tag v2.7.0 = 首个无壳基础层版本**（验证分离节奏在发布线上跑通）；CHANGELOG 对账表落地（计划项 vs 实际提交）；audit full 建档。
- 验收：verify PASS=31+ 全绿；外部实证报告 ≥1 出口成功或明确差距修复；Release 无壳资产（对比 v2.6 的刻意变化）。

### 波 A 增补 · 外部借鉴五项筛选并入（40 增补 v1.1 · 2026-09-06 用户提供清单）

> 来源：用户提供外部借鉴清单（OpenSpec / Spec Kit / SpecForge / awesome-design.md 等方向）。以下为逐项专业筛选结果——按 NF 实况适配度裁决，不照搬外部模式。参考项目 GitHub 实测：github/spec-kit 133,658★、VoltAgent/awesome-design-md 114,377★、obra/superpowers 282,241★、MingYuePop/SpecForge 263★。

| 借鉴项 | 宣称来源 | 筛选结论 | 并入方式 | 理由（NF 实况对照） |
|---|---|---|---|---|
| X1 产出物可用性门禁 | SpecForge | ✅ **采纳（并入 S1，不单列）** | S1 收口动作：E1–E3 外部实证结论**沉淀为回归资产**——fixtures（SillyTavern 加载样例 / Claude 装载样例 + 断言）入测试库，防出口层回归 | NF check19/check22 已机检导出 schema；真正缺口 = 外部消费端实测（=S1）与其资产化；可机检部分已覆盖，无需铺新机制 |
| X2 MCP Server 正式化 | SpecForge 子代理衔接 | ✅ **采纳（升级并入 S1-E3）** | E3 从「客户端实测」升级为「MCP 正式化」：实测 + `docs/mcp.md` 公开文档 + README 能力宣告转正 + 版本说明 | mcp_runtime（只读白名单）+ `nf serve` + 桌面 W9 入口均已落地，缺公开化；MCP 属基础层能力，**不依赖壳**——正合分离哲学，可随 v2.7 无壳发布 |
| X3 协议市场社区化 | Spec Kit 生态 | ✅ **部分采纳（并入 S3，范围收敛）** | S3 补充动作：贡献者入口文档化（第一个第三方：做什么/怎么提交/验收什么）；**不照搬 spec-kit 代码市场模式** | NF 是内容/域包市场（tier/溯源/可装载），机制必须按内容货架设计；现有 CONTRIBUTING 五步 + nf market 已具雏形，缺的是首个第三方实战而非新架子 |
| X4 协议增量 delta 机制 | OpenSpec | ⏸️ **暂缓（不入 v2.7，登记候选）** | 候选登记：触发条件 = 首个第三方贡献者出现 或 v3.0 协议重构时；届时做轻量版（协议文档头版本变更段 或 `nf spec changelog`） | NF 已有等效覆盖约 90%：每版方案文档 = 变更记录 + CHANGELOG 版本段 + README 真相源追溯链；当前零第三方贡献者，delta 收益无人消费——按需装配哲学：不产没人消费的东西 |
| X5 DESIGN.md 设计规范 | awesome-design.md | ⏸️ **暂缓（不入 v2.7，登记壳波前置项）** | 候选登记：壳集中波做 UI 时，先按 NF 自己的工厂方式产 UI 设计协议（顺带成为 P90 techdoc 域新战例），而非直接抄 DESIGN.md | 壳冻结中（L3_FROZEN），设计规范当前**无消费端**；UI 设计 = 全新领域扩展需全套管线/模块/资产，成本高且偏离 v2.7 主线；同构点已记录：DESIGN.md 模式 = NF 工厂能产的东西，不是 NF 要学的东西 |

**筛选注记（对清单文本的三处事实修正）**：
1. 「协议栈 37 个版本（01 到 37_v2.5.0）」不实——01–36 是**方案文档**（36_v2.5.0 为最后一份），37 是执行线提交线引用而非文档；协议本体 = 01/02/06/07 四份 + 07 导航；方案文档是变更记录，不是「协议版本」。
2. 「Spec Kit 121,000+ Stars」方向正确但数字过时——实测 133,658★（2026-09-06）。
3. 清单所称 SpecForge「产物门禁/渐进加载/子代理衔接」与真实仓库对不上（sgl-project/SpecForge 是投机解码模型工具；MingYuePop/SpecForge 263★ 是中文文档驱动工作流）——引用源不可靠，故 NF 侧仅取其方法论要点独立评估，不绑定原项目。

### 波 A 增补 v1.2 · Agent 自助组装核心线（Y1–Y5 · 用户 2026-09-06 拍板：与社区版并列核心功能）

> 用户原话：「并入！这个是核心功能啊，跟社区版同样重要」。**NF 两大使用通道**：通道 A 社区版（桌面工具导入勾选组装）/ 通道 B Agent 自助组装（任何用户的任何 agent，仅凭仓库地址即自助取货组装「完整版」，无需桌面工具）。至此仓库 = 可被 agent 自助消费的协议市场，供给侧生态位闭环成型。

| # | 动作 | 产出 | 验收 |
|---|---|---|---|
| Y1 | 「完整版」产物规格定义 | 自包含单文件世界文档（注册表投影段 + 管线段 + 模块段 + 资产段 + 装配记录），任意 agent 按 06 协议直接装载开跑；规格定稿入 07 导航（消费侧引用） | 规格文档可被任意 LLM 复述执行 |
| Y2 | AGENT_START.md（根目录） | 使用者 agent 的三句话入口：你是装配师 → 读组装指令包 → 产出完整版；与 AGENTS.md（开发者纪律）分置不混 | 任何 agent 读到即知角色与路径 |
| Y3 | community/组装指令包.md | 可复制指令：装配师角色 / 读 07→02→01 选件规则 / 编号·层号·五字段铁律 / 完整版模板骨架 / 组装后自检清单 / 官方样例；任意免费客户端 AI（豆包/Kimi/DeepSeek 等）可用 | 指令包单独粘贴即可驱动组装 |
| Y4 | 实证（对话内） | AI 扮演「仅知仓库地址的外部 agent」现场组装首个完整版样本，落盘手机可读 | 样本通过自检清单，可直接装载 |
| Y5 | 用户复测 | 样本交任意免费 AI 装载开跑或继续扩展——第一个「agent 用户」外部验证锚点（并入 S1 口径） | 复测结果记录入外部验证报告 |

**渠道地位**：README + 07 导航双通道标注（社区版桌面工具 / Agent 自助组装）；与模板制作指令包（单件生产）构成「单件生产 → 整装」两级指令族。

### 波 A 增补 v1.3 · 免费客户端零门槛通道（B2 粘贴档 · Y6–Y8 · 用户 2026-09-06 拍板）
> 用户愿景（#19 澄清通道 B 最终形态）：「非 API 平台（免费客户端，如豆包 / DeepSeek / 千问），只需要贴上我的项目地址，就可以让用户组装输出 md——虽然没有动手能力但能输出规范」。这些 AI 不能 clone / 不能跑代码 / 联网抓 GitHub 时灵时不灵，但**能输出规范文本**——NF 需保证「纯粘贴 + 纯文本」即可完成组装。
> **定位条款**：B2 产出 = **协议创作档**——AI 拿不到仓库模块原文，按 NF 协议**原创生成**内容（模块八要素照做、骨架照做、资产键表自建），非取件装配；由此天然规避第三方内容版权——**NF 只供协议标准，内容由用户自产**，生态最干净（TLV 版权事件为直接触发）。
| # | 动作 | 产出 | 验收 |
|---|---|---|---|
| Y6 | 「粘贴即用 · 迷你协议卡」 | ≤3KB 自包含单文件：装配师角色认知 + 完整版骨架八段 + 模块八要素简版 + 创作档铁律 + 自检 7 项简版 + **分片输出规则**（骨架先行 → 模块分批续写「回复『继续』」→ 用户合并）；用户复制粘贴给任意免费 AI 即完成「取货」，**零依赖 AI 联网 / 工具能力** | 仅凭协议卡文本即可驱动组装 |
| Y7 | 无联网实测（对话内） | AI 扮演「收不到仓库的免费客户端」——仅收到协议卡 + 一句需求（如「组装修仙题材完整版」），现场输出规范完整版样本 | 产物过自检清单；样本交真实免费客户端复测（Y5 同法） |
| Y8 | 通道 B 拆档 + 入口修订 | B1 自主档（能访问仓库 → agent_组装指令包 v0.1）/ B2 粘贴档（免费客户端 → 迷你协议卡）；AGENT_START 增加无联网分流话术；README 通道标注升级三通道（A 社区版 / B1 自主 / B2 粘贴） | 用户三句话内到达正确路径 |
**约束与前置**：① 分片输出规则为 Y6 内置条款（免费档上下文有限，完整版须可分片吐完）；② 免费模型对 schema 区块遵循打折 → 自检清单为唯一防线，必要时启用「双 AI 互验」变体；③ 本增补不改变 Y1–Y5 / S 线既有范围，随 v2.7 波 A 收口一并落地；④ B2 为增量通道，不替代 B1——取件精确性仍以 B1 为准。

### 波 A 增补 v1.4 · 云端公共线（Y9′–Y12′ · 2026-09-07 用户拍板：Y 线全权开工）

> 背景与对齐根因：Y9–Y12 此前为 `.rivet/plans` 本地档案（**不被 git 跟踪**）——双 agent 两端夹击（头部从基础层、尾部从公共层）时头部 pull 不到 Y 线档案，两端参照不同源（S9 教训重演）。**本增补 = 云端公共线唯一真相源**，头部 agent 动基础层前先读本节（尤其 Y9′ 连通协议窗口与编号制式 v0.4）。
> 已落地（commit `90ab277`，双端同步）：Y11′ 编号制式 v0.4 + Y12′ 代收站 V2，verify PASS=31 实测全绿。

**定位条款**：Y11′/Y12′ 属**公共层**（library/.github/README 社区段），随 main 直接推送、不产基础层版本 tag、不动协议层（01–07/registry/verify 零改动）；Y9′ 连通协议 = 云端公共线与基础层的**唯一接缝**，登记面消费既有 `nf import --register`（v2.5），无协议 schema 改动。

**Y11′ 云端公共产物馆**（已上线）：建构件 + 成品同馆同档（同属编号第二级档位段，不设官方/公共等级差）；入馆唯一硬标准 = **自包含可召回**（文件自带「是什么 + 怎么用」，AI 单文件即正确使用；引用式公开档不再收，历史样本 NF-1 例外在馆注明）。

**编号制式 v0.4（定稿契约，任何消费方引用此条）**：
- 四级结构：`NF - 档位段 - 自定义段 - 36进制序号`（示例 `NF-WORLDCAMPUS-Monyeah777-1`）；不填档位/自定义 = 默认形 `NF-1`。
- 档位段 = 建构件/成品类型（WORLD / CCV3 / PKG / PROTO / PIPE / TPL…**自由复合拼写**，不依赖词表）；自定义段 = 用途/署名；两段各 ≤16 字符、仅字母数字、段内不含连字符（连字符为段分隔符）、大小写原样保留。
- 序号 = 36 进制流水：每位 0-9 → A-Z（数字排完上字母），从 1 起、无前导零、不定长、低位满进位（1,2,…9,A,…Z,10,1A,…），**同前缀各自计数**（车牌同段同号区）。
- 硬约束：固定 4 段（AI 数段定位序号）；前缀小写键全馆唯一（禁止仅大小写不同并存——转译无歧义）。
- AI 识别协议：快路径（记得准直接拼 raw）/ 转译路径（大小写拿不准 → 全小写化 → `library/ALIAS.md` 匹配真实编号）/ 慢路径（读 INDEX 重选）；复制优先，禁止凭记忆拼链接。
- 寻址：编号 = 文件名 = raw 链接一部分（GitHub raw + Gitee raw 同规则）。

**Y12′ 云端代收站 V2**（已上线）：Issue【NF投稿】→ 机器人自动：校验白名单（**仅作者本人**——S3 第三方实战前不开放）→ 解析模板元信息 → 分配编号（同前缀 36 进制）→ 写文件 → INDEX 插行 → **ALIAS 全量重建** → 推送 → 回评双端链接 → 关 Issue；段位不配对/字符非法/前缀小写撞键 → 回评说明并关闭。

**Y9′ 回流双段式**（设计文档已落 `.rivet/plans`，登记面随基础层排期）：云端提交面（Y12′/library，raw 可达）──连通──▶ 基础层登记面（`nf import --register` → 内容库 → market 可发现可装配）。**头部 agent 侧待办窗口**：v2.7 或后续基础层版本内评估「云端编号产物直接收录登记」的支持（若排期冲突则仅文档化，不阻塞）。

**Y10′ README 入口层重组**（草案已落 `.rivet/plans`，实施待与头部协调）：字母菜单编排既有通道/能力（A 社区版 / B1 自主 / B2 粘贴 / 云端馆 / 投稿 / 自定义协议…），不新造机制；README 为共享根文档——**实施前与头部对齐改动面，避免双改冲突**。

**双 agent 衔接契约（更新版）**：① 分区铁律：头部动基础层（协议/代码/核心文档正文），尾部动公共层（library/.github/README 社区段）——Y9′ 连通协议为接缝，任何一侧动它先通告；② 推前先拉；③ Gitee 镜像由尾部兜底同步（令牌不共享）；④ 40 总纲 = 唯一共享真相源，Y 线档案已并入本节，不再依赖 .rivet/plans。

### 波 B · v2.8 基础层「脚手架与流转」（下一波，触发 = 波 A 完成后积压或用户驱动）
- **S4**：`nf pipeline new --from P00 --domain <d>` + protocol.yaml 管线槽位 + 骨架目录；战例：用命令派生第 4 条官方管线或一个演示领域管线；验收「从命令到可跑」计时。
- **S5**：模块 status 位 + deprecate/restore 命令 + 引用门禁 + 首例流转战例（演示 M9x 或真实弃用项）。
- **S7 近端**：nf --help 分级、`nf demo` 一键演示世界、README 五分钟快速开始。
- **S3 续**：第三方上架 SOP 首次实战（如有候选人）或第二个官方题材包按 tier 标准补种。
- 收口：tag v2.8.0（无壳）；audit 建档；对账表续。
- 验收：verify 全绿；新脚手架命令有实测记录；模块流转战例在册。

### 波 C–N · 基础层常态（P∞，脉冲式）
- 每波 = 一次审计建档 + 一次对账表 + 一次回冻结；外部评审窗（S10）每 1–2 波开一次。
- 内容补种按货架逻辑持续；外部首个真实用户用例作为波内随机事件优先承接（S10 的活水）。

### 波 Σ · 壳集中波（P3，末位 · 单独触发 · 引用 38 方案）
- 触发：基础层进入收敛期或用户裁定开始。
- 内容：38 方案合并稿（治理版 + 架构版）五 Stage 执行：W1–W9 接线 + APK 降级 + MCP serve 合体 POC + 交互重构裁决（38-X 独立候选项）。
- 产壳 tag：shells-v1（专用 tag，不影响基础层 v* 线）→ 此后壳资产随壳线版本走，**永远不再混入基础层发布**。
- 本波完成后：S7 远端（产品上手）与 S12 一并消解。

---

## 第三部 · 裁决记录（2026-09-06 用户批准定稿）

| # | 裁决项 | 状态 |
|---|---|---|
| 1 | 本总纲批准与编号（40 正式文档，入库根目录） | ✅ **照准** |
| 2 | S1 范围：E1 先行，E2/E3 视 E1 结果推进（总纲正文即按此序） | ✅ **照准** |
| 3 | S2 nf asset 族子命令集与溯源粒度（文件级） | ✅ **照准** |
| 4 | v2.7.0 = 首个无壳基础层 tag（分离节奏发布线首验） | ✅ **照准** |
| 5 | 第三方首例上架无候选人 → 官方续补第二题材包代替（tier 货架标准） | ✅ **照准（默认路径）** |
| 6 | APK 定位措辞：「能力对齐、交互从简」vs「浏览/预览/轻导出级」 | ⏸️ **挂账**——不阻塞 v2.7；待 38 方案正式文档化时一并裁决（默认按执行线落地事实「能力对齐、交互从简」为壳波验收基准措辞） |
| 7 | M_AUDIT 三开放问题（模板 Prompt 落盘时机 / audit.md 存放位 / steelman_ref 字段层级） | ⏸️ **挂账**——不阻塞 v2.7；建议随 M_AUDIT 使用实践自然收敛后裁决 |
| 8 | 三层评估文档去留 | ✅ **默认：不入库**——40 总纲已完整承载评估结论，头部引用为归档依据（评估日期 2026-09-06） |
| 9 | 外部借鉴筛选照准：X1/X2/X3 并入、X4/X5 暂缓登记候选 | ✅ **照准**——v2.7 波 A 范围 = S1+S2+S3+X1+X2+X3 |
| 10 | **Agent 自助组装核心线**（Y1–Y5：完整版规格/AGENT_START/组装指令包/实证/复测）——与社区版并列核心功能 | ✅ **照准**（2026-09-06 追加）——v2.7 波 A 范围更新 = S1+S2+S3+X1+X2+X3+Y1+Y2+Y3+Y4+Y5 |
| 11 | **免费客户端零门槛通道**（B2 粘贴档：Y6 迷你协议卡 / Y7 无联网实测 / Y8 通道 B 拆档+入口修订）——覆盖非 API 免费客户端用户群（豆包/DeepSeek/千问等） | ✅ **照准**（2026-09-06 追加）——v2.7 波 A 范围更新 = S1+S2+S3+X1+X2+X3+Y1+Y2+Y3+Y4+Y5+Y6+Y7+Y8 |
| 12 | **云端公共线 Y 线全权开工**（Y9′–Y12′ 重制方案 v0.4 定稿：四级编号制式 + 36 进制车牌式序号 + ALIAS 大小写转译站 + 自包含唯一硬标准 + Y9 连通基础层双段式 + Y10 README 入口重组 + 撤回三线分流——建构件与成品同馆同档） | ✅ **照准**（2026-09-07，用户拍板「Y 线由你全权开工」，裁决 A/B1/C/D/E/F 按建议默认执行）——线上落地 commit `90ab277` |
| 13 | 编号制式四段式定稿：`NF-档位段-自定义段-36进制序号`（档位段自由复合拼写；序号每位 0-9→A-Z、1 起、不定长、同前缀各自计数；段长 ≤16；前缀小写键全馆唯一）——替代 2026-09-06 裁决链中的旧编号形态 | ✅ **照准**（2026-09-07 定稿）——Y11′/Y12′ 全线上线即用此制式 |

---

## 附 · 与既有治理的接线

- 本总纲 = 38 方案（壳线）的**基础层姊妹篇**：38 管壳、40 管基础层，同一「分离哲学」的两半。
- L3_FROZEN 契约不变：壳线冻结持续，脉冲触发条件重计（本次不触发——S6 之后壳资产不再随基础层产生，积压口径改为「待接线项」计数）。
- 四根基机制体检结论（协议最完善、资产最薄弱）已映射：S2（资产）、S4（管线）、S5（模块）三项补全全数入列；协议层无短板，仅外部规范同步周期化（35 A5 → 常态）挂账。
- 验证基线：verify PASS=31 为红线，任何波不得低于该基线；新增 check 只增不减。
